async function r(i){i.main;let t=document.getElementById("log");for(let e of document.querySelectorAll("is-org-chart"))e.addEventListener("is-select",o=>{if(!t)return;let n=o.detail;t.textContent=`[${e.id}] ${n.node.title||""}: ${n.node.name}
`+t.textContent}),e.addEventListener("is-toggle",o=>{if(!t)return;let n=o.detail;t.textContent=`[${e.id}] toggle ${n.id} \u2192 collapsed=${n.collapsed}
`+t.textContent}),e.addEventListener("is-open-viewer",()=>{t&&(t.textContent=`[${e.id}] abrir visor
`+t.textContent)})}function l(){}export{r as mount,l as unmount};
