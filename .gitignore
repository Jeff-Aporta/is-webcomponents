node_modules/
.DS_Store
Thumbs.db
*.log
.env
.env.*
.gitsecret/
.vscode/
.idea/
dist/**/*.map
.impeccable/

# Estado parcial de descarga de iconos (se regenera con scripts/download-icons.mjs)
src/assets/icons/.state/

# Tests locales: el repo commitea los .test.mjs pero ignora artefactos
# generados (snapshots, reports, node:test --test-reporter outputs).
tests/*.tmp
tests/coverage/
tests/.cache/
tests/_author-index.json

# Salidas de scripts / auditorías locales
logs/

# Iconos descargados: commiteamos solo las dos colecciones mas usadas del repo
# (mdi y tabler). El resto se regenera bajo demanda con `node scripts/download-icons.mjs`.
src/assets/icons/*
!src/assets/icons/mdi/
!src/assets/icons/tabler/
!src/assets/icons/mdi.json
!src/assets/icons/tabler.json
!src/assets/icons/manifest.json

# dist/cdn/assets/ NO se ignora: GitHub Pages sirve el sistema de iconos desde
# ahi (is-icon resuelve dist/cdn/src/assets/icons/<prefix>/<name>.svg). La regla
# `src/assets/icons/*` de arriba esta anclada a la raiz y no alcanza esta ruta.

# Scratch local (fuentes vendorizadas, tarballs de terceros).
tmp/
.tmp/

scripts/tmp-is-code-audit/

