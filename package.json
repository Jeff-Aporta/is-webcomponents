{
  "name": "app-webcomponents",
  "version": "0.1.0",
  "private": true,
  "description": "Galería de Web Components de Insoft — shell de preview con componentes vanilla.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "icons:download": "node scripts/download-icons.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --import ./scripts/ts-resolve-hook.ts --test tests/*.test.ts"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.62.1",
    "typescript": "^5.9.0"
  }
}
