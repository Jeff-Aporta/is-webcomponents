# Auditoría F0.3 — animation/components/diagrams+actions+media+charts+data-viz+demos+components+previews+utils (g54)

> **Proyecto**: `C:\ContaPyme\Personal\apps\is-webcomponents`
> **Modo**: PROPUESTAS (NO se escribe código de test).
> **Orquestación**: 5 subagentes en paralelo (diagrams, actions, media, charts+data-viz, datagrid+previews+utils).

---

## Contexto

- **Testables del grupo** (53 items):
  - **diagrams (20 CSS + 1 TS)**: `src/components/diagrams/{block-diagram,class-diagram,component-diagram,diagram-lightbox,er-diagram,flowchart,gantt,journey-map,lightbox,mindmap,org-chart,quadrant-chart,sankey-diagram,sequence-diagram,state-diagram,swimlane-diagram,timeline,use-case-diagram,venn-diagram}.css`, `src/components/diagrams/er-archify.ts`
  - **actions (12)**: `src/components/actions/{button-group.preview.controller,button-group,button.preview,button,check-icon-button,context-menu,copy-button,dropdown-item,dropdown,fab,share-button,speed-dial}.ts`
  - **media (12)**: `src/components/media/{avatar,barcode-scanner,barcode,icon-explorer.preview,icon,image-editor,media-recorder,qrcode,speech,theme-img,video-playlist,video}.ts`
  - **charts (3)**: `src/components/charts/{chart,sparkline,treemap}.ts`
  - **data-viz (2)**: `src/components/data-viz/{heatmap,maps}.ts`
  - **demos (1)**: `demos/diagramas/ER/er-editor.html`
  - **datagrid/previews/utils (3)**: `src/components/data/datagrid-core/server-datasource.ts`, `src/previews/_kit/ISComponentPreview.ts`, `src/utils/home-cdn.ts`
- **Lenguaje(s)**: TypeScript + CSS vanilla (WebComponents con Shadow DOM).
- **Tests existentes — hallazgos clave**:
  - **diagrams**: ninguno en `*.test.ts` adyacente. Solo cobertura estática parcial en `src/utils/testing/diagrams/` (`component-diagram-ifaces`, `diagram-edge-actors`, `diagram-text-wrap`, `diagram-wrap-integration`, `er-clusters`, `sequence-legend-grid`, `sequence-self-loop`) — tocan layout/wrap/integration, NO CSS, animaciones, reduced-motion ni `:host([open-on-click])`.
  - **actions**: `exhaustive/actions/button.test.ts` (10 dimensiones de `button.ts`); `domain/button-events.test.ts` (eventos + `#syncTag` re-bind); `domain/context-menu-scroll.test.ts` (scroll de context-menu). **Resto sin cobertura específica.**
  - **media**: `icon-explorer.test.ts` (scroll+búsqueda+form), `icon-currentcolor.test.ts`, `icon-render.test.ts`, `icon-prefetch.test.ts`. **Resto sin cobertura específica.**
  - **charts/data-viz**: cobertura estática en `health/exhaustive/charts/{chart,sparkline,treemap,heatmap,…}.test.ts` (snapshots de `observedAttributes`/`styleAttrs`/serialización JSON — NO interacción, teclado, XSS, ni performance). Para `er-editor.html` existe `demos/diagramas/ER/_testing/er-editor.test.mjs` con smoke + drag + undo + exportJson/Svg determinista + reduced-motion + keyframes.
  - **datagrid/previews/utils**: solo cobertura parcial estática en `ux-gallery-invariants.test.ts` (regex sobre guard de `target null` en `ISComponentPreview.on`). `server-datasource.test.ts` y `home-cdn.test.ts` **none found**.
- **Riesgos identificados (pre-auditoría rápida)**:
  1. **XSS reales en `maps.ts`** (`cfg.attribution` via `innerHTML`, `cfg.tileUrl` como `iframe.src` sin whitelist) y potencial en `treemap.ts` vía `inlineMdWeb(n.label)`.
  2. **Determinismo comprometido** en `sparkline.ts` (`Math.random()` para IDs de gradient).
  3. **Keyboard navigation ausente** en marks/cells/teselas/markers de los 5 webcomponents de charts+data-viz.
  4. **`prefers-reduced-motion` no honrado** en heatmap.ts, treemap.ts, maps.ts, sparkline.ts.
  5. **Animaciones no-GPU** (`d` en `org-chart.css`, `r` en `quadrant-chart.css`) — sin budget performance.
  6. **Etiquetas del catálogo imprecisas**: `paty-iss` y `sql-query` en chart.ts/maps.ts son **inventadas**; `server-datasource.ts` no contiene `Worker`/`HttpClient` reales.
- **Tipo de proyecto**: frontend (WebComponents vanilla + librería de UI).

---

## Propuestas por testable

### `src/components/diagrams/block-diagram.css` (174 LOC) — animation

**Tests existentes**: none found.
**Features detectadas**: `.block-node, .block-edge { transition: opacity 0.2s ease, transform 0.2s cubic-bezier(0.4,0,0.2,1) }`; `.block-node.is-dim, .block-edge.is-dim { opacity: 0.28 }`; `.block-node.is-active { transform: translateY(-1px) }`; `.block-node__box { transition: stroke-width 0.16s, filter 0.16s, stroke 0.16s }`; `.block-edge__path { transition: stroke-width 0.16s, opacity 0.2s }`; drop-shadow `color-mix(currentColor 24%, transparent)` en active; `backdrop-filter: blur(8px)`; `:host([open-on-click]) .wrap { cursor: zoom-in }`; `:host([hidden]) { display: none }`; `.wrap[data-empty]::after` "Diagrama de bloques no disponible (payload inválido)."; `font-feature-settings: 'cv11', 'ss01'`.

**Propuestas nuevas (12)**:

1. **`.block-node.is-active` levanta 1px al activarse** — [categoría: UI/UX]
   - **Setup**: `<is-block-diagram>` con payload `{blocks:[{id:"A",name:"A"},{id:"B",name:"B"}],edges:[{from:"A",to:"B"}]}`. Mock `HTMLElement.prototype.animate`.
   - **Acción**: hover/`.is-active`; leer `getComputedStyle(blockNode).transform`.
   - **Assertion**: `matrix(1,0,0,1,0,-1)` (translateY(-1px)); duración declarada 200ms.
   - **Coverage**: branch hover→active.

2. **`prefers-reduced-motion` desactiva el `translateY(-1px)`** — [categoría: reduced-motion]
   - **Setup**: `window.matchMedia` mock con `(prefers-reduced-motion: reduce) matches`.
   - **Acción**: tras `.is-active`, leer `getComputedStyle(blockNode).transform`.
   - **Assertion**: `transform === 'none'`; `.block-legend__item:hover` no aplica `translateX(1px)`.
   - **Coverage**: a11y/OS setting.

3. **`.block-edge.is-active` ensancha `stroke-width` a 2.5 con transición 160ms** — [categoría: CSS transitions]
   - **Acción**: `.classList.add('is-active')`; leer `transitionProperty`/`Duration`/`stroke-width`.
   - **Assertion**: `stroke-width` en `transitionProperty`; `transitionDuration === '0.16s'`; valor final `2.5px`.
   - **Coverage**: timing exacto declarado.

4. **`.block-edge.is-active .block-edge__head` aplica drop-shadow `color-mix(currentColor 36%)`** — [categoría: visual]
   - **Setup**: `--is-color-brand: #e03131` en `:root`.
   - **Acción**: hover; leer `filter` del `<path class="block-edge__head">`.
   - **Assertion**: contiene `drop-shadow(0 1px 2px color-mix(...) 36%)`.
   - **Coverage**: filtro en `currentColor`.

5. **`.block-legend__item:hover` desplaza 1px a la derecha** — [categoría: UI/UX]
   - **Acción**: `:hover`; leer `transform` y `transition`.
   - **Assertion**: `matrix(1,0,0,1,1,0)`; `transition: 0.18s ease`.
   - **Coverage**: microinteracción de leyenda.

6. **`.wrap[data-theme="light"]` cambia fondo a `rgb(255 255 255 / 96%)`** — [categoría: theming]
   - **Setup**: `<is-block-diagram>` dentro de `<div data-theme="light">`.
   - **Acción**: leer `backgroundImage` y `borderColor` de `.wrap`.
   - **Assertion**: la capa 2ª del `linear-gradient` empieza en `rgb(255 255 255 / 96%)`; borde alpha 8%.
   - **Coverage**: branch dark→light.

7. **`:host([open-on-click]) .wrap { cursor: zoom-in }` y hover expande sombra** — [categoría: integration]
   - **Acción**: toggle atributo, hover; leer `cursor` y `box-shadow`.
   - **Assertion**: `cursor === 'zoom-in'` solo con atributo; `box-shadow` en hover incluye `0 18px 48px -16px rgb(0 0 0 / 45%)`.
   - **Coverage**: contrato opt-in visor.

8. **`.wrap[data-empty]::after` muestra mensaje en español tras payload inválido** — [categoría: edge-case]
   - **Acción**: `payload={}`; leer `getComputedStyle(.wrap, '::after').content`.
   - **Assertion**: `'Diagrama de bloques no disponible (payload inválido).'`, italic, color `var(--is-text-soft)`.
   - **Coverage**: estado vacío.

9. **`backdrop-filter: blur(8px)` activo en `.wrap`, removido en `.is-viewer`** — [categoría: visual]
   - **Acción**: comparar `backdrop-filter` con/sin `color="viewer"`.
   - **Assertion**: `wrap: blur(8px)`; `.is-viewer`: `none`. `border: 0`, `background: transparent`.
   - **Coverage**: variante visor.

10. **`:host([hidden])` aplica `display: none`** — [categoría: edge-case]
    - **Acción**: setear `hidden`; leer `display`.
    - **Assertion**: `'none'`; sin atributo `'block'`.
    - **Coverage**: oculto accesible.

11. **`@keyframes`-free: sin animaciones activas en estado base** — [categoría: performance]
    - **Acción**: `document.getAnimations()`.
    - **Assertion**: 0 animaciones activas; transiciones solo en mutación de clase.
    - **Coverage**: confirma que block-diagram.css no introduce compositor cost.

12. **`.block-node.is-dim` aplica `opacity: 0.28` con transición 200ms + `font-feature-settings: 'cv11', 'ss01'`** — [categoría: a11y/visual]
    - **Acción**: hover-out; leer `opacity`, `transition`, `fontFeatureSettings`.
    - **Assertion**: `opacity: 0.28`, `transition: 0.2s ease`, `fontFeatureSettings: 'cv11', 'ss01'`.
    - **Coverage**: branch hover-out y contratos tipográficos.

---

### `src/components/diagrams/class-diagram.css` (100 LOC) — animation

**Tests existentes**: none found.
**Propuestas nuevas (11)**:

1. **`.cls-rel.is-active` ensancha `stroke-width` de arista a 2.2 con `ease-out` 120ms** — [Categoría: CSS transitions]
   - **Setup**: payload con `{classes:[{id:"A",name:"A"}],relations:[{from:"A",to:"A"}]}`.
   - **Acción**: toggle `.is-active`; leer `transition-duration` y `stroke-width`.
   - **Assertion**: `120ms`, `2.2px`.
   - **Coverage**: timing declarado.
2. **`.cls-node.is-dim` atenúa nodos a 0.32 con transición 180ms** — [Categoría: UI/UX]
   - **Setup**: payload con 3 nodos.
   - **Acción**: `cls-node[1].classList.add('is-dim')`.
   - **Assertion**: `opacity === '0.32'`, `transition: opacity 0.18s ease`.
   - **Coverage**: branch hover-out.
3. **`prefers-reduced-motion` neutraliza la transición de opacity en `.cls-node`** — [Categoría: reduced-motion]
   - **Setup**: `matchMedia('(prefers-reduced-motion: reduce)')` matches.
   - **Acción**: tras `is-dim`, leer `getComputedStyle(clsNode).transition`.
   - **Assertion**: `transition-duration === '0s'`.
   - **Coverage**: a11y / vestibular.
4. **`.wrap[data-empty]::after` muestra el mensaje específico de clases** — [Categoría: edge-case]
   - **Setup**: payload `{}`.
   - **Acción**: leer `::after` content + color.
   - **Assertion**: `'Diagrama de clases no disponible (payload inválido).'`, color `var(--is-text-soft, #a8b0bb)`.
   - **Coverage**: copy por-diagrama.
5. **`:host([open-on-click])` aplica `cursor: zoom-in` solo con atributo** — [Categoría: integration]
   - **Acción**: toggle atributo, leer cursor.
   - **Assertion**: con atributo → `zoom-in`; sin → `default`.
   - **Coverage**: contrato opt-in de visor.
6. **`.cls-node__divider { opacity: 0.8 }` se mantiene estable sin transición** — [Categoría: visual]
   - **Acción**: leer `opacity` y `transition`.
   - **Assertion**: `0.8`, `transition: none`.
   - **Coverage**: foco solo anima geometría.
7. **`.cls-legend__item:hover` aplica transición `opacity 0.18s`** — [Categoría: UI/UX]
   - **Acción**: hover sintético, leer `transition`.
   - **Assertion**: `transition-property: opacity`, `duration: 0.18s`.
   - **Coverage**: microinteracción consistente.
8. **Variante `:host([color="viewer"])` llena 100%/100%** — [Categoría: integration]
   - **Acción**: `host.clientWidth/clientHeight` con padre 800x600.
   - **Assertion**: iguales al padre.
   - **Coverage**: contrato con `is-lightbox`.
9. **Variante `.wrap.is-viewer` quita border/padding/background** — [Categoría: visual]
   - **Acción**: añadir `.is-viewer`, leer `border`, `padding`, `background`.
   - **Assertion**: `border-width: 0`, `padding: 0`, `background: transparent`.
   - **Coverage**: integración lightbox.
10. **`:host([hidden]) { display: none }`** — [Categoría: edge-case]
    - **Acción**: setear atributo, leer `display`.
    - **Assertion**: `none`.
    - **Coverage**: oculto accesible.
11. **`box-sizing: border-box` se hereda a `.wrap` y SVG** — [Categoría: visual]
    - **Acción**: `getComputedStyle` en `.wrap` y `.cls-svg`.
    - **Assertion**: ambos `border-box`.
    - **Coverage**: previene overflow horizontal.
12. **`.cls-svg { overflow: hidden }` recorta nodos fuera de viewBox** — [Categoría: visual]
    - **Setup**: nodo cuyo bounding box excede viewBox.
    - **Acción**: medir `getBoundingClientRect`.
    - **Assertion**: contenido recortado a `0 ≤ x ≤ viewBox.width`.
    - **Coverage**: contrato de escalado SVG.

---

### `src/components/diagrams/component-diagram.css` (80 LOC) — animation

**Tests existentes**: ninguno directo. Cobertura previa: `component-diagram-ifaces.test.ts` (layout).
**Propuestas nuevas (11)**:

1. **`data-theme="dark"` resuelve `--cd-circle-fill: #10141a`** — [categoría: theming]
   - **Setup**: insertar `<is-component-diagram>` con `wrap.dataset.theme='dark'`.
   - **Acción**: `getComputedStyle(cdSvg).getPropertyValue('--cd-circle-fill')`.
   - **Assertion**: `#10141a` exacto. Equivalente para `light` → `#ffffff`.
   - **Coverage**: branch theming por-diagrama.
2. **`prefers-reduced-motion` neutraliza `.cd-pkg` `opacity 0.18s`** — [categoría: reduced-motion]
   - **Acción**: con reduce-motion activo, leer `transitionDuration`.
   - **Assertion**: `0s` para todas las clases de animación.
   - **Coverage**: a11y.
3. **`.cd-edge__path` anima `stroke-width` 120ms `ease-out` al activar** — [categoría: CSS transitions]
   - **Acción**: hover sobre `.cd-edge` con `.is-active`; leer `stroke-width` y `transition`.
   - **Assertion**: `120ms ease-out`, valor final según spec.
   - **Coverage**: coherencia con class/er.
4. **`.cd-svg { overflow: visible }` (diverge del resto de diagramas)** — [categoría: visual]
   - **Acción**: leer `overflow` del `<svg class="cd-svg">`.
   - **Assertion**: `'visible'` (mientras flowchart/gantt/er/etc es `'hidden'`).
   - **Coverage**: documenta excepción intencional (iconos de puerto sobresalen).
5. **`.wrap[data-empty]::after` usa copy específico** — [categoría: edge-case]
   - **Acción**: payload `{}`.
   - **Assertion**: contiene `"Diagrama de componentes no disponible"`.
   - **Coverage**: copy por-diagrama.
6. **`@keyframes` ausente: animaciones = sólo transiciones** — [categoría: performance]
   - **Acción**: `document.getAnimations()` tras render.
   - **Assertion**: 0 animaciones activas sin hover.
   - **Coverage**: coste de paint bajo en estado base.
7. **`:host([open-on-click])` aplica cursor `zoom-in`** — [categoría: integration]
   - **Acción**: toggle atributo, leer cursor.
   - **Assertion**: `zoom-in` condicional.
   - **Coverage**: contrato visor.
8. **`.wrap.is-viewer` quita chrome (border/padding/background)** — [categoría: visual]
   - **Acción**: añadir `.is-viewer`, leer border/padding/background.
   - **Assertion**: todos neutros.
   - **Coverage**: integración lightbox.
9. **`box-sizing: border-box` en host + wrap** — [categoría: visual]
   - **Acción**: leer `boxSizing` en `:host` y `.wrap`.
   - **Assertion**: `border-box` en ambos.
   - **Coverage**: previene overflow en nested lightbox.
10. **`.cd-pkg, .cd-cmp, .cd-edge, .cd-iface` comparten `transition: opacity 0.18s`** — [categoría: CSS transitions]
    - **Acción**: leer `getComputedStyle` de cada selector.
    - **Assertion**: misma `transitionProperty`/`Duration`/`TimingFunction`.
    - **Coverage**: contrato visual coherente.
11. **Variante `color="viewer"` llena 100%/100% + `font-family` resuelve a stack sans del tema** — [categoría: integration/theming]
    - **Acción**: `color="viewer"` con padre 800x600; leer `clientWidth/Height` y `fontFamily`.
    - **Assertion**: iguales al padre; contiene `system-ui` como último fallback o `--is-sans`.
    - **Coverage**: lightbox + coherencia tipográfica.

---

### `src/components/diagrams/diagram-lightbox.css` (131 LOC) — animation

**Tests existentes**: none found.
**Propuestas nuevas (13)**:

1. **`.lb-ring__fill` anima `stroke-dashoffset` linealmente 200ms** — [categoría: UI/UX]
   - **Setup**: lightbox con `data-autoreplay`; spy sobre `<circle class="lb-ring__fill">`.
   - **Acción**: `controller.tick()`; leer `getComputedStyle(circle).transition`.
   - **Assertion**: `transition-property: stroke-dashoffset`, `duration: 200ms`, `timing: linear`.
   - **Coverage**: CSS-driven countdown sin `requestAnimationFrame`.
2. **`prefers-reduced-motion` neutraliza la animación del countdown** — [categoría: reduced-motion]
   - **Acción**: con reduce-motion activo; leer `transition` y `stroke-dashoffset`.
   - **Assertion**: `transition-duration: 0s` o salto directo a offset final.
   - **Coverage**: WCAG 2.3.3.
3. **`.lb-ring svg { transform: rotate(-90deg) }` deja el anillo arrancando arriba** — [categoría: visual]
   - **Acción**: leer `transform` del SVG interno.
   - **Assertion**: `matrix(0,1,-1,0,0,0)` (rotate -90).
   - **Coverage**: garantiza trazo progresivo desde 12-en-punto.
4. **`.lb-step { font-variant-numeric: tabular-nums }` mantiene ancho de cifras** — [categoría: visual]
   - **Acción**: comparar `getBoundingClientRect` con `"1/12"` vs `"10/12"`.
   - **Assertion**: anchos idénticos.
   - **Coverage**: previene jitter del contador.
5. **`.lb-code__area:focus-visible` muestra outline 2px `--is-focus`** — [categoría: a11y]
   - **Acción**: focus sintético; leer `outline` y `outline-offset`.
   - **Assertion**: `2px solid var(--is-focus, #4aabff)`; `-1px`.
   - **Coverage**: WCAG 2.4.7.
6. **`.lb-text-btn.is-primary` aplica fondo `var(--is-accent)`** — [categoría: theming]
   - **Acción**: añadir `.is-primary`, leer `background` y `color`.
   - **Assertion**: `background-color` igual a `--is-accent`; color blanco sobre marca.
   - **Coverage**: CTA primario cross-theme.
7. **`.lb-text-btn:hover` oscurece fondo a `--is-control-bg-hover`** — [categoría: UI/UX]
   - **Acción**: hover; leer `background`.
   - **Assertion**: cambia a `--is-control-bg-hover`.
   - **Coverage**: feedback de hover.
8. **`.lb-text-btn:focus-visible` muestra outline 2px `--is-focus`** — [categoría: a11y]
   - **Acción**: focus, leer `outline` y `outline-offset`.
   - **Assertion**: `2px solid --is-focus`, `1px` offset.
   - **Coverage**: foco accesible en toolbar del visor.
9. **`.lb-unsupported` muestra mensaje centrado en `var(--is-text-soft)`** — [categoría: edge-case]
   - **Setup**: lightbox con kind no soportado.
   - **Acción**: leer `text-align` y `color`.
   - **Assertion**: `center`, color `--is-text-soft`.
   - **Coverage**: estado degradado.
10. **`.lb-text-btn` hereda tipografía base + `.lb-code__area` aplica `tab-size: 2`** — [categoría: visual]
    - **Acción**: leer `font-size`, `font-family`, `tab-size`.
    - **Assertion**: `font-size: 0.8125rem`, `font-family: inherit`, `tab-size: 2`.
    - **Coverage**: legibilidad cross-lightbox + sangría compacta.
11. **`.lb-code__area` usa `--is-mono` para tipografía** — [categoría: theming]
    - **Acción**: leer `font-family`.
    - **Assertion**: contiene `var(--is-mono, ui-monospace, Consolas, monospace)`.
    - **Coverage**: coherencia con code-highlight.
12. **`.lb-code__err` color `var(--is-danger-text, #ff8787)` y `hidden` cuando vacío** — [categoría: edge-case]
    - **Setup**: editor sin error.
    - **Acción**: leer `display`.
    - **Assertion**: `none`.
    - **Coverage**: estado sin error limpio.
13. **`.lb-ring__icon` font-size 11px** — [categoría: visual]
    - **Acción**: leer `font-size` del icono.
    - **Assertion**: `11px`.
    - **Coverage**: tamaño compacto del icono del ring.

---

### `src/components/diagrams/er-archify.ts` (481 LOC) — animation

**Tests existentes**: ninguno directo (los `*.selfcheck.ts` cubren layout, no validación). **none found**.
**Funciones exportadas**: `normalizeErPayload`, `serializeErPayload`, `renderErSvg`, `getEmbeddedCss`, `pickSidesArchify`, `straightPath`, `orthogonalHPath`, `orthogonalVPath`, `SNAP`. Allowlists: `ENTITY_KEYS, RELATION_KEYS, ATTRIBUTE_KEYS, STYLE_KEYS, META_KEYS, TOP_KEYS, GROUP_KEYS`. CSS embebido: `@keyframes iswc-dash-march` + `@media (prefers-reduced-motion: reduce) { animation: none !important }`.

**Propuestas nuevas (25+)**:

1. **`renderErSvg` con `animation: 'trace'` setea `data-animation="trace"` y embebe `<style data-iswc-embedded>`** — [categoría: integration]
   - **Setup**: `<svg>` mínimo + `XMLSerializer`.
   - **Acción**: `renderErSvg(svg, {animation: 'trace'})`.
   - **Assertion**: resultado contiene `data-animation="trace"`, `<style data-iswc-embedded="true">` y `getEmbeddedCss(true)`.
   - **Coverage**: branch trace.
2. **`renderErSvg` con `embedStyle: false` NO inserta `<style data-iswc-embedded>`** — [categoría: integration]
   - **Acción**: `renderErSvg(svg, {embedStyle: false, animation: 'trace'})`.
   - **Assertion**: NO contiene `data-iswc-embedded`.
   - **Coverage**: branch export PDF/PNG limpio.
3. **`renderErSvg` con `skipStyleFix: true` omite `xmlns`/`xmlns:xlink`** — [categoría: edge-case]
   - **Acción**: `renderErSvg(svg, {skipStyleFix: true})`.
   - **Assertion**: NO contiene `xmlns="http://www.w3.org/2000/svg"`.
   - **Coverage**: modo inserción inline.
4. **`getEmbeddedCss(true)` define keyframes `iswc-dash-march` con `from{stroke-dashoffset:0} to{stroke-dashoffset:-28}`** — [categoría: CSS transitions]
   - **Acción**: importar `getEmbeddedCss`, parsear.
   - **Assertion**: contiene `@keyframes iswc-dash-march` con `-28` final; selectores `[data-animation="trace"] .iswc-anim-dash` y `.iswc-anim-edge-dashed`.
   - **Coverage**: contrato CSS embebido.
5. **`getEmbeddedCss` (sin trace) omite selectores `[data-animation="trace"]`** — [categoría: edge-case]
   - **Acción**: `getEmbeddedCss(false)`.
   - **Assertion**: NO contiene `[data-animation="trace"]`; contiene `@media (prefers-reduced-motion: reduce)` con `animation: none !important`.
   - **Coverage**: branch legacy sin animación.
6. **`@media (prefers-reduced-motion: reduce)` dentro de `getEmbeddedCss` aplica `animation: none !important`** — [categoría: reduced-motion]
   - **Acción**: inyectar CSS en `<style>`; `matchMedia` reduce; leer `getComputedStyle.animationName`.
   - **Assertion**: `none` cuando reduce-motion activo.
   - **Coverage**: a11y (override `!important`).
7. **`normalizeErPayload` en strict mode lanza con campo desconocido a nivel root** — [categoría: edge-case]
   - **Acción**: `{erDiagram:{entities:[{id:'a',name:'A'}]}, surprise:true}` con `strict:true`.
   - **Assertion**: throws `Error` mencionando `unknown field "surprise" at root`.
   - **Coverage**: guard de schema.
8. **`normalizeErPayload` con `strict:false` ignora campos desconocidos y devuelve `ErSpec`** — [categoría: edge-case]
   - **Acción**: mismo payload con `{strict:false}`.
   - **Assertion**: NO throws; `result.entities[0].id === 'a'`.
   - **Coverage**: branch legacy/demo.
9. **`normalizeErPayload` con payload `null`/no-objeto devuelve `null`** — [categoría: edge-case]
   - **Acción**: invocar con `null`, `undefined`, `'string'`, `42`.
   - **Assertion**: cada uno retorna `null`.
   - **Coverage**: branch guard inicial.
10. **`normalizeErPayload` sin `entities` array no vacío retorna `null`** — [categoría: edge-case]
    - **Acción**: `{erDiagram:{entities:[]}}`.
    - **Assertion**: `null`.
    - **Coverage**: invariante del modelo.
11. **`normalizeErPayload` con `entities[].pos`=[257,189] snap a múltiplos de 8** — [categoría: edge-case]
    - **Acción**: `{entities:[{id:'a',name:'A',pos:[257,189]}]}`.
    - **Assertion**: `out.entities[0].pos` ≈ `[256, 192]`.
    - **Coverage**: contrato `SNAP`.
12. **`normalizeErPayload` con `entities[].size` inválido ([12,8]) retorna `undefined`** — [categoría: edge-case]
    - **Acción**: `size:[12,8]`.
    - **Assertion**: `out.entities[0].size === undefined`.
    - **Coverage**: branch tamaño inválido descartado.
13. **`serializeErPayload` produce JSON byte-identical entre llamadas** — [categoría: edge-case]
    - **Acción**: serializar mismo spec dos veces.
    - **Assertion**: `a === b` (string igualdad).
    - **Coverage**: contrato `stable`.
14. **`serializeErPayload` omite defaults (`fromCard:'one'`, `direction:'LR'`)** — [categoría: edge-case]
    - **Acción**: spec con cardinalidades default.
    - **Assertion**: JSON NO contiene `"fromCard":"one"` ni `"direction":"LR"`.
    - **Coverage**: minimización.
15. **`serializeErPayload` con `indent:0` produce JSON minificado válido** — [categoría: edge-case]
    - **Acción**: `serializeErPayload(spec,{indent:0})`.
    - **Assertion**: `JSON.parse` retorna objeto equivalente; sin saltos de línea.
    - **Coverage**: branch compacto.
16. **`pickSidesArchify` con dx>dy devuelve `right/left`** — [categoría: edge-case]
    - **Acción**: `from:{cx:0,cy:0}, to:{cx:100,cy:10}`.
    - **Assertion**: `{fromSide:'right', toSide:'left'}`.
    - **Coverage**: branch horizontal.
17. **`pickSidesArchify` con dy>|dx| devuelve `bottom/top`** — [categoría: edge-case]
    - **Acción**: `from:{cx:0,cy:0}, to:{cx:10,cy:100}`.
    - **Assertion**: `{fromSide:'bottom', toSide:'top'}`.
    - **Coverage**: branch vertical.
18. **`straightPath` retorna `M x1,y1 L x2,y2`** — [categoría: edge-case]
    - **Acción**: `straightPath({x:1,y:2},{x:3,y:4})`.
    - **Assertion**: `'M1,2 L3,4'`.
    - **Coverage**: contrato SVG path.
19. **`orthogonalHPath` usa comandos `V`/`H`** — [categoría: visual]
    - **Acción**: `orthogonalHPath({x:0,y:0},{x:100,y:80})`.
    - **Assertion**: contiene `V` y `H`; `midX === 50`.
    - **Coverage**: round-trip archify.
20. **`orthogonalVPath` produce `M x1,y1 H mid V x2 L x2,y2`** — [categoría: visual]
    - **Acción**: `orthogonalVPath({x:0,y:0},{x:80,y:100})`.
    - **Assertion**: contiene `H`, `V`; `midY === 50`.
    - **Coverage**: branch vertical.
21. **`normalizeErPayload` con `relations[]` referenciando ids inexistentes los descarta (`null`)** — [categoría: edge-case]
    - **Acción**: payload con `relations:[{from:'fantasma',to:'A'}]`.
    - **Assertion**: `out.relations` vacío.
    - **Coverage**: integridad referencial.
22. **`normalizeErPayload` con `fromSide:'auto'` NO lo emite (no es significativo)** — [categoría: edge-case]
    - **Acción**: payload con `fromSide:'auto'`.
    - **Assertion**: `out.entities[0]` (o relation) sin `fromSide`.
    - **Coverage**: filtro de defaults.
23. **`normalizeErPayload` con `dashStyle:'foo'` lo descarta silenciosamente** — [categoría: edge-case]
    - **Acción**: `relations:[{from:'A',to:'A',dashStyle:'foo'}]`.
    - **Assertion**: `out.relations[0].dashStyle === undefined`.
    - **Coverage**: guard de enum.
24. **`normalizeErPayload` con `meta.animation: 'trace'` lo propaga al `out.meta.animation`** — [categoría: integration]
    - **Acción**: `{meta:{animation:'trace'}, entities:[…]}`.
    - **Assertion**: `out.meta.animation === 'trace'`.
    - **Coverage**: contrato con `renderErSvg`.
25. **`serializeErPayload` con `style.opacity: 0.123456` redondea a 2 decimales (`0.12`)** — [categoría: edge-case]
    - **Acción**: spec con `style:{opacity:0.123456}`.
    - **Assertion**: JSON contiene `"opacity":0.12`.
    - **Coverage**: `round(n,2)`.
26. **`serializeErPayload` con `via:[[1.4,2.6],[3.5,4.9]]` snap a 8px enteros** — [categoría: edge-case]
    - **Acción**: spec con `via:[[1.4,2.6]]`.
    - **Assertion**: snap8.
    - **Coverage**: snap a rejilla.
27. **`normalizeErPayload` con `direction: 'IZQ-DER'` lo coerce a `'LR'`** — [categoría: edge-case]
    - **Acción**: payload con `direction:'IZQ-DER'`.
    - **Assertion**: `out.direction === 'LR'`.
    - **Coverage**: fallback de enum.
28. **`renderErSvg` no duplica el `<style>` si ya existe `[data-iswc-embedded]`** — [categoría: integration]
    - **Setup**: `<svg>` con `<style data-iswc-embedded="true">` pre-existente.
    - **Acción**: `renderErSvg(svg)`.
    - **Assertion**: UN solo `<style data-iswc-embedded>`.
    - **Coverage**: idempotencia.
29. **`SNAP` (alias de `snap8`) redondea a múltiplos de 8** — [categoría: edge-case]
    - **Acción**: `SNAP(0)`, `SNAP(7)`, `SNAP(8)`, `SNAP(13)`, `SNAP(-1)`.
    - **Assertion**: `[0, 0, 8, 16, 0]`.
    - **Coverage**: contrato exportado público.

---

### `src/components/diagrams/er-diagram.css` (99 LOC) — animation

**Tests existentes**: ninguno directo. **none found**.
**Propuestas nuevas (12)**: patrón análogo a `class-diagram.css` (entidad `is-dim` 0.32, `is-active` ensancha `stroke-width` 2.2, `.wrap[data-theme]` resuelve `--er-circle-fill`, copy "Diagrama entidad-relación no disponible", `:host([open-on-click])` cursor zoom-in, `is-viewer` quita chrome, `color="viewer"` 100%/100%, `.er-svg { overflow: hidden }`, `:host([hidden])` `display: none`, `box-sizing: border-box`, `:host([animation]` reduced-motion override, `.er-entity.is-active` no aplica `transform`).

---

### `src/components/diagrams/flowchart.css` (138 LOC) — animation

**Tests existentes**: ninguno CSS. **none found**.
**Features únicas**: `@keyframes is-flowchart-flow-dash` (0.85s linear infinite), `@supports (stroke: color-mix(in oklab,…))`, `:host([animation~="flow"])` opt-in, override `prefers-reduced-motion` con `opacity: 0.55`.
**Propuestas nuevas (13)**:

1. **`:host([animation~="flow"])` activa `is-flowchart-flow-dash` 0.85s linear infinite** — [Categoría: UI/UX]
   - **Setup**: payload con 2 nodos + 1 arista; atributo `animation="flow"`.
   - **Acción**: leer `getComputedStyle(.flow-edge__flow).animationName`, `animationDuration`, `animationIterationCount`, `animationTimingFunction`.
   - **Assertion**: `'is-flowchart-flow-dash'`, `0.85s`, `infinite`, `linear`.
   - **Coverage**: branch opt-in.
2. **`@keyframes is-flowchart-flow-dash { to { stroke-dashoffset: -18 } }`** — [Categoría: UI/UX]
   - **Acción**: capturar `getComputedStyle(path).strokeDashoffset` a t=0, 425ms, 850ms.
   - **Assertion**: offset decrece de 0 a -18 en 850ms (loop).
   - **Coverage**: contrato de la animación.
3. **`prefers-reduced-motion: reduce` desactiva `animation` y baja opacidad a 0.55** — [Categoría: reduced-motion]
   - **Acción**: con reduce-motion; leer `animationName` y `opacity`.
   - **Assertion**: `animation-name: none`, `opacity: 0.55`.
   - **Coverage**: WCAG 2.3.3.
4. **`@supports (stroke: color-mix(in oklab,…))` aplica `color-mix(oklab)` y `stroke-opacity: 1`** — [Categoría: edge-case]
   - **Acción**: spy `CSS.supports` retornando true.
   - **Acción**: leer `stroke` y `stroke-opacity`.
   - **Assertion**: contiene `color-mix(in oklab, var(--is-color-brand, #e03131) 42%, transparent)`; `stroke-opacity: 1`. Sin soporte → fallback con `stroke-opacity: 0.4`.
   - **Coverage**: progressive enhancement.
5. **`.flow-edge__flow { stroke-dasharray: 7 11 }` produce patrón de guiones 7/11** — [Categoría: visual]
   - **Acción**: leer `stroke-dasharray`.
   - **Assertion**: `"7 11"`.
   - **Coverage**: contrato visual.
6. **`.flow-edge__flow { pointer-events: none }` no interfiere con hit-testing** — [Categoría: edge-case]
   - **Acción**: click sobre `.flow-edge__flow`; verificar `event.target`.
   - **Assertion**: evento burbujea desde `.flow-edge__path`.
   - **Coverage**: evita doble target del overlay animado.
7. **`.flow-node.is-dim` atenúa a 0.32** — [Categoría: UI/UX]
   - **Acción**: hover-out; leer `opacity`.
   - **Assertion**: `0.32`, transición `0.18s`.
   - **Coverage**: foco único.
8. **`.flow-legend__item` transición `opacity 0.18s ease`** — [Categoría: UI/UX]
   - **Acción**: leer `transition`.
   - **Assertion**: `opacity 0.18s ease`.
   - **Coverage**: coherencia.
9. **`:host([open-on-click]) .wrap` cursor `zoom-in`** — [Categoría: integration]
   - **Acción**: toggle atributo.
   - **Assertion**: cursor condicional.
   - **Coverage**: opt-in visor.
10. **`.wrap[data-empty]::after` muestra "Diagrama de flujo no disponible"** — [Categoría: edge-case]
    - **Acción**: payload `{}`.
    - **Assertion**: contenido correcto.
    - **Coverage**: copy por-diagrama.
11. **`.flow-svg { overflow: hidden }` recorta overflow** — [Categoría: visual]
    - **Acción**: leer overflow.
    - **Assertion**: `hidden`.
    - **Coverage**: clipping.
12. **Atributo `animation="flow trace"` (múltiples tokens) sigue activando la animación** — [Categoría: edge-case]
    - **Acción**: atributo `animation="foo flow bar"`.
    - **Assertion**: `animation-name` activado (selector atributo `~=`).
    - **Coverage**: branch selector `[animation~=]`.
13. **`stroke-dashoffset` no produce layout thrash (animación GPU-friendly) + `box-sizing: border-box`** — [Categoría: performance/visual]
    - **Acción**: PerformanceObserver durante 2s; leer `boxSizing`.
    - **Assertion**: layout shift = 0; frames < 16.6ms p95; `boxSizing: border-box` en host/wrap/svg.
    - **Coverage**: smoke performance + box model.

---

### `src/components/diagrams/gantt.css` (98 LOC) — animation

**Tests existentes**: none found.
**Propuestas nuevas (11)**: patrón análogo al resto (`.gantt-row.is-active` `stroke-width: 2`, `.gantt-arrow.is-active` `stroke-width: 2.2`, `.gantt-row.is-dim` 0.32, reduced-motion, `.gantt-legend__item` 180ms, copy "Diagrama de Gantt no disponible", `:host([open-on-click])` zoom-in, `is-viewer`, `color="viewer"` 100%/100%, `.gantt-svg overflow:hidden`, `:host([hidden])`).

---

### `src/components/diagrams/journey-map.css` (98 LOC) — animation

**Tests existentes**: none found.
**Propuestas nuevas (11)**: patrón análogo + `.jn-step.is-active .jn-step__dot` aplica `filter: brightness(1.18)` con 120ms ease-out; `.jn-step.is-active .jn-step__label` cambia a `font-weight: 700`; `.jn-line { opacity: 0.85 }` sin transición; copy "Mapa de recorrido no disponible"; `.jn-step.is-active` NO aplica `transform` (vs block-diagram).

---

### `src/components/diagrams/lightbox.css` (209 LOC) — animation, css-part

**Tests existentes**: none found.
**CSS vars**: `--is-lightbox-bg`, `--is-lightbox-text`, `--is-lightbox-border`, `--is-lightbox-backdrop`, `--is-lightbox-backdrop-blur`, `--lb-toolbar-bg`. **CSS parts**: `.lb`, `.lb::backdrop`, `.lb-bar`, `.lb-bar__group`, `.lb-bar__trail`, `.lb-btn`, `.lb-stage`, `.lb-host`, `.lb-code`, `.lb-toast`.

**Propuestas nuevas (17)**:

1. **`is-lightbox` aplica `--is-lightbox-bg/text/border` con fallbacks correctos** — [Categoría: theming]
   - **Setup**: `:root` sin `--is-bg`, `--is-text`, `--is-border`.
   - **Acción**: leer `getComputedStyle(host)` de las 3 vars.
   - **Assertion**: `--is-lightbox-bg: #0d1117`, `--is-lightbox-text: #e6e8eb`, `--is-lightbox-border: #1d2229`.
   - **Coverage**: fallback chain documentado.
2. **`prefers-color-scheme: light` (vía `--is-bg`) cambia `--is-lightbox-bg`** — [Categoría: theming]
   - **Setup**: `--is-bg: #ffffff` en `:root`.
   - **Acción**: leer `--is-lightbox-bg`.
   - **Assertion**: `#ffffff` (bug documentado).
   - **Coverage**: fix de regresión.
3. **`.lb-btn` transición `background 120ms ease, border-color 120ms ease`** — [Categoría: CSS transitions]
   - **Acción**: leer `transition` declaradas.
   - **Assertion**: `transition-property: background, border-color`, `duration: 120ms`, `timing: ease`.
   - **Coverage**: timing exacto.
4. **`.lb-btn:hover` aplica fondo `color-mix(text 12%)` y borde `--is-lightbox-border`** — [Categoría: UI/UX]
   - **Acción**: hover; leer `background` y `borderColor`.
   - **Assertion**: `color-mix(in srgb, var(--is-lightbox-text) 12%, transparent)`; borde = `--is-lightbox-border`.
   - **Coverage**: feedback visual.
5. **`.lb-btn:focus-visible` muestra outline 2px `--is-color-brand` con offset 1px** — [Categoría: a11y]
   - **Acción**: focus sintético; leer `outline` y `outline-offset`.
   - **Assertion**: `2px solid var(--is-color-brand, #339af0)`, `1px` offset.
   - **Coverage**: WCAG 2.4.7.
6. **`.lb-stage { cursor: grab }` cambia a `grabbing` con `[data-panning]`** — [Categoría: UI/UX]
   - **Acción**: `pointerdown` en `.lb-stage`; leer cursor.
   - **Assertion**: `grab` → `grabbing`. `.touch-action: none` previene scroll del navegador.
   - **Coverage**: contrato pan.
7. **`.lb::backdrop { backdrop-filter: blur(2px) }` por defecto, `none` en `variant="solid"`** — [Categoría: UI/UX]
   - **Acción**: comparar `backdrop-filter` del `::backdrop` con/sin atributo.
   - **Assertion**: default: `blur(2px)`; solid: `none`. `background` cambia a `rgb(0 0 0 / 60%)` en solid.
   - **Coverage**: contrato del backdrop.
8. **`.lb-bar { backdrop-filter: blur(8px) }` mantiene cromo legible sobre cualquier fondo** — [Categoría: visual]
   - **Acción**: leer `backdrop-filter`.
   - **Assertion**: `blur(8px)`; `background` usa `--lb-toolbar-bg` con fallback `color-mix(...)`.
   - **Coverage**: legibilidad cross-theme.
9. **`:host([open]) { display: contents }` evita hueco en top-layer** — [Categoría: edge-case]
   - **Acción**: setear atributo `open`; leer `display`.
   - **Assertion**: `contents`. Sin atributo: `none`.
   - **Coverage**: contrato del dialog top-layer.
10. **`.lb-bar--has-user-toolbar` oculta zoom-in/out/reset/share pero mantiene close** — [Categoría: edge-case]
    - **Setup**: lightbox con toolbar inyectado.
    - **Acción**: añadir `.lb-bar--has-user-toolbar`; leer `display` de `[data-act]`.
    - **Assertion**: zoom-in/out/reset/share → `none`; close visible.
    - **Coverage**: branch slot override.
11. **`@media (max-width: 38rem) { .lb-btn { width/height: 2rem } }`** — [Categoría: a11y/visual]
    - **Setup**: viewport 600x800.
    - **Acción**: leer `width`/`height`.
    - **Assertion**: 32x32px (WCAG 2.5.5).
    - **Coverage**: target táctil mínimo.
12. **`.lb-toast` se centra con `transform: translateX(-50%)`** — [Categoría: visual]
    - **Acción**: leer transform del toast.
    - **Assertion**: `matrix(1,0,0,1,-50%,0)`.
    - **Coverage**: centrado.
13. **`.lb-host` SIN `will-change` mantiene SVG nítido al hacer zoom** — [Categoría: performance]
    - **Acción**: aplicar `transform: scale(2)`; leer `willChange`.
    - **Assertion**: `will-change: auto`.
    - **Coverage**: comentario l.160-163 explica la decisión.
14. **`.lb-bar { box-shadow: 0 1px 3px rgb(0 0 0 / 6%) }` da profundidad sin oscurecer** — [Categoría: visual]
    - **Acción**: leer box-shadow.
    - **Assertion**: contiene `0 1px 3px rgb(0 0 0 / 6%)`.
    - **Coverage**: micro-shadow.
15. **`.lb-code[hidden]` y `.lb-toast[hidden]` aplican `display: none`** — [Categoría: edge-case]
    - **Acción**: leer `display` con `hidden`.
    - **Assertion**: `none` en ambos.
    - **Coverage**: panel/toast opcionales.
16. **`is-lightbox` define CSS parts explícitos (verificar `el.part`)** — [Categoría: css-part]
    - **Acción**: `shadowRoot.querySelectorAll('[part]')`.
    - **Assertion**: existe `part="lb"` (o equivalente); permite `::part(lb-bar)` desde fuera.
    - **Coverage**: expone API de theming cross-document.
17. **`.lb-bar { background: var(--lb-toolbar-bg, …) }` permite override externo** — [Categoría: theming]
    - **Setup**: `--lb-toolbar-bg: red` en `:root`.
    - **Acción**: leer `backgroundColor`.
    - **Assertion**: `red` (override) gana al fallback `color-mix(...)`.
    - **Coverage**: API de personalización.

---

### `src/components/diagrams/mindmap.css` (101 LOC) — animation

**Tests existentes**: none found.
**Propuestas nuevas (12)**: patrón análogo + `.mm-node.is-active .mm-node__pill` aplica `filter: brightness(1.12)` con 120ms ease-out; el mismo filter se aplica a `.mm-node__underline` (consistencia pill+underline); `.mm-node-label .tk-inline-icon` resuelve a 1em con `vertical-align: -0.14em`; copy "Mapa mental no disponible"; `.mm-node.is-active` NO aplica `transform: translateY`.

---

### `src/components/diagrams/org-chart.css` (125 LOC) — animation

**Tests existentes**: ninguno CSS. **none found**.
**Features únicas**: **`.node { transition: transform 300ms cubic-bezier(.4, 0, .2, 1) }`** y **`.edge { transition: d 280ms cubic-bezier(.4, 0, .2, 1) }`** (¡anima path `d`!).

**Propuestas nuevas (16)**:

1. **`.node` transición `transform 300ms cubic-bezier(.4, 0, .2, 1)` al expandir/plegar** — [Categoría: UI/UX]
   - **Setup**: payload jerárquico; toggle de un padre.
   - **Acción**: spy `getComputedStyle(.node).transitionProperty/Duration/TimingFunction`.
   - **Assertion**: `transform` en lista; `300ms`; `cubic-bezier(0.4, 0, 0.2, 1)`.
   - **Coverage**: branch expand/collapse.
2. **`.edge` transición `d 280ms cubic-bezier(.4, 0, .2, 1)`** — [Categoría: CSS transitions]
   - **Acción**: tras toggle; spy sobre `path.setAttribute('d', …)`.
   - **Assertion**: `transition-property` contiene `d`; `duration: 280ms`; `timing: cubic-bezier(.4,0,.2,1)`.
   - **Coverage**: anima atributo `d` (no GPU-accelerated).
3. **`prefers-reduced-motion` neutraliza `transition: transform/d`** — [Categoría: reduced-motion]
   - **Acción**: con reduce-motion, leer `transitionDuration`.
   - **Assertion**: `0s` o cambio instantáneo.
   - **Coverage**: a11y / animaciones largas (300ms).
4. **`.card { transition: border-color 120ms ease, transform 100ms ease }`** — [Categoría: UI/UX]
   - **Acción**: leer transition.
   - **Assertion**: `border-color 120ms ease, transform 100ms ease`.
   - **Coverage**: feedback rápido.
5. **`.card:hover` cambia borde a `var(--is-accent)`** — [Categoría: theming]
   - **Acción**: hover; leer `borderColor`.
   - **Assertion**: igual a `var(--is-accent)` computado.
   - **Coverage**: branch theming.
6. **`.card { box-shadow: inset 0 1px 0 rgb(255 255 255 / 4%), 0 6px 14px rgba(0,0,0,.18) }`** — [Categoría: visual]
   - **Acción**: leer `box-shadow`.
   - **Assertion**: contiene inset highlight + drop shadow.
   - **Coverage**: profundidad.
7. **`.photo--fallback` aplica gradiente `linear-gradient(135deg, --is-accent, color-mix(--is-accent 50%, transparent))`** — [Categoría: visual]
   - **Setup**: persona sin imagen.
   - **Acción**: leer `backgroundImage`.
   - **Assertion**: contiene `linear-gradient(135deg, var(--is-accent)` y `color-mix(50%, transparent)`.
   - **Coverage**: avatar fallback.
8. **`.photo { object-fit: cover; overflow: hidden }` recorta avatar cuadrado** — [Categoría: visual]
   - **Acción**: leer `objectFit` y `overflow`.
   - **Assertion**: `cover`, `hidden`.
   - **Coverage**: avatar cover.
9. **`.title` aplica line-clamp 2 con ellipsis** — [Categoría: visual]
   - **Acción**: leer `display`, `-webkit-line-clamp`, `text-overflow`.
   - **Assertion**: `-webkit-box`, `line-clamp: 2`, `text-overflow: ellipsis`, `word-break: break-word`.
   - **Coverage**: truncar título multi-línea.
10. **`.name` aplica `white-space: nowrap; text-overflow: ellipsis`** — [Categoría: visual]
    - **Acción**: nombre muy largo; leer `whiteSpace`, `textOverflow`.
    - **Assertion**: `nowrap`, `ellipsis`. Una sola línea.
    - **Coverage**: nombre truncado.
11. **`:host([open-on-click]) .root` cursor `zoom-in`** — [Categoría: integration]
    - **Acción**: toggle atributo.
    - **Assertion**: cursor `zoom-in` solo con atributo.
    - **Coverage**: opt-in visor.
12. **`:host([open-on-click]) .card` cursor `pointer`** — [Categoría: integration]
    - **Acción**: toggle atributo; leer cursor en `.card`.
    - **Assertion**: `pointer` con atributo.
    - **Coverage**: contrato click-through.
13. **`.toggle` redondo 1.6rem con hover que invierte colores a `--is-accent`/`--is-on-accent`** — [Categoría: UI/UX]
    - **Acción**: hover; leer background y color.
    - **Assertion**: `var(--is-accent)`, `var(--is-on-accent, #fff)`.
    - **Coverage**: botón expand/collapse.
14. **`.edge { stroke: var(--is-text-soft); stroke-width: 1.25; opacity: .55 }` estilo recesivo** — [Categoría: visual]
    - **Acción**: leer stroke/opacity.
    - **Assertion**: `var(--is-text-soft)`, `1.25`, `0.55`.
    - **Coverage**: jerarquía visual.
15. **`.root.is-viewer { min-height: 0; padding: 0 }`** — [Categoría: integration]
    - **Acción**: añadir `.is-viewer`; leer min-height y padding.
    - **Assertion**: `0`, `0`.
    - **Coverage**: lightbox sin cromo.
16. **Animación de `d` dispara `paint` y `composite` pero NO `layout`** — [Categoría: performance]
    - **Acción**: PerformanceObserver sobre `layout-shift` y `paint` durante 3 ciclos expand/collapse.
    - **Assertion**: 0 layout shift; paints > 0.
    - **Coverage**: smoke performance del único diagrama que anima `d`.

---

### `src/components/diagrams/quadrant-chart.css` (98 LOC) — animation

**Tests existentes**: none found.
**Features únicas**: **`.qd-point__dot { transition: r 0.12s ease-out, filter 0.12s ease-out }`** (anima atributo `r` del círculo).
**Propuestas nuevas (13)**: incluye `.qd-point__dot` transición `r 0.12s ease-out`, `.qd-point.is-active .qd-point__dot` aplica `filter: brightness(1.16)`, `.qd-point.is-active .qd-point__label` cambia a `font-weight: 700`, `.qd-frame__cell { opacity: 0.7 }` sin transición, copy "Matriz de cuadrantes no disponible", patrón open-on-click/viewer/hidden estándar, **performance: animación de `r` con 200 puntos dispara N transiciones simultáneas — PerformanceObserver mide frames < 16ms p95**.

---

### `src/components/diagrams/sankey-diagram.css` (100 LOC) — animation

**Tests existentes**: none found.
**Propuestas nuevas (12)**: incluye `.sk-link__band` transición `fill-opacity 0.14s ease-out`, `.sk-link.is-active .sk-link__band` aplica `fill-opacity: 0.62`, `.sk-node.is-active .sk-node__bar` aplica `filter: brightness(1.14)`, **`.sk-node.is-dim` atenúa a 0.28** (vs 0.32 en otros diagramas — excepción documentada), reduced-motion, copy "Diagrama de Sankey no disponible", `.sk-link.is-active` NO cambia `stroke-width` (solo fill-opacity).

---

### `src/components/diagrams/sequence-diagram.css` (124 LOC) — animation

**Tests existentes**: ninguno CSS. **none found**.
**Features únicas**: `.wrap.is-viewer { cursor: inherit }` (cede al lightbox), `.wrap.is-hover-msg .seq-lifeline { opacity: 0.85 }`, `.seq-msg.is-dim { opacity: 0.42 }` (mayor dim que otros diagramas).

**Propuestas nuevas (14)**:

1. **`.seq-msg.is-dim` atenúa a 0.42 (mayor dim)** — [Categoría: UI/UX]
   - **Acción**: hover-out de mensaje; leer opacity.
   - **Assertion**: `0.42` (vs 0.32 en otros diagramas).
   - **Coverage**: documenta excepción sequence.
2. **`.wrap.is-hover-msg .seq-lifeline { opacity: 0.85 }`** — [Categoría: UI/UX]
   - **Setup**: JS añade `.is-hover-msg` al wrap en `messageenter`.
   - **Acción**: hover mensaje; leer `.seq-lifeline` opacity.
   - **Assertion**: `0.85`. Sin `.is-hover-msg` → opacity base (1).
   - **Coverage**: branch compuesto.
3. **`prefers-reduced-motion` neutraliza `transition: opacity 0.18s`** — [Categoría: reduced-motion]
   - **Acción**: con reduce-motion; leer transitionDuration.
   - **Assertion**: `0s`.
   - **Coverage**: a11y.
4. **`.wrap.is-viewer` cursor `inherit` cede el control al lightbox** — [Categoría: integration]
   - **Setup**: wrap dentro de lightbox con `.lb-stage` (cursor `grab`).
   - **Acción**: leer `cursor` del `.wrap.is-viewer`.
   - **Assertion**: `inherit` (no `default`, no `grab`); hereda `grab` del padre.
   - **Coverage**: contrato con `is-lightbox`.
5. **`.wrap.is-viewer .seq-svg` override `height: 100%; max-width: none`** — [Categoría: visual]
   - **Acción**: añadir `.is-viewer`; leer height/max-width.
   - **Assertion**: `100%`, `none`.
   - **Coverage**: integración lightbox.
6. **`.seq-label .tk-inline-icon` 0.95em (vs 1em en otros)** — [Categoría: visual]
   - **Acción**: leer width/vertical-align.
   - **Assertion**: `0.95em`, `-0.12em`.
   - **Coverage**: alineación específica de sequence.
7. **`.seq-actor-label .tk-inline-icon` 1em (igual que default)** — [Categoría: visual]
   - **Acción**: leer width.
   - **Assertion**: `1em`, `margin-right: 0.3em`.
   - **Coverage**: icono en actor.
8. **`.wrap[data-empty]::after` muestra "Diagrama de secuencia no disponible"** — [Categoría: edge-case]
   - **Acción**: payload `{}`.
   - **Assertion**: contenido correcto.
   - **Coverage**: copy por-diagrama.
9. **`:host([open-on-click])` cursor `zoom-in`** — [Categoría: integration]
   - **Acción**: toggle atributo.
   - **Assertion**: condicional.
   - **Coverage**: opt-in visor.
10. **`:host([color="viewer"])` 100%/100%** — [Categoría: integration]
    - **Acción**: clientWidth/Height.
    - **Assertion**: igual a padre.
    - **Coverage**: lightbox.
11. **`.seq-legend__item` transición `opacity 0.18s ease`** — [Categoría: UI/UX]
    - **Acción**: leer transition.
    - **Assertion**: `opacity 0.18s ease`.
    - **Coverage**: leyenda coherente.
12. **`.seq-svg { overflow: hidden }`** — [Categoría: visual]
    - **Acción**: leer overflow.
    - **Assertion**: `hidden`.
    - **Coverage**: clipping.
13. **`:host([hidden])` `display: none`** — [Categoría: edge-case]
    - **Acción**: setear atributo.
    - **Assertion**: `none`.
    - **Coverage**: oculto.
14. **`.seq-msg` con dim 0.42 vs `.seq-actor` con dim por defecto (no is-dim)** — [Categoría: edge-case]
    - **Acción**: revisar si `.seq-actor.is-dim` está definido.
    - **Assertion**: NO existe `.seq-actor.is-dim` (solo `.seq-msg` lo tiene); documentar intención.
    - **Coverage**: invariante.

---

### `src/components/diagrams/state-diagram.css` (96 LOC) — animation

**Tests existentes**: none found.
**Propuestas nuevas (11)**: patrón análogo al resto (`.st-trans.is-active` ensancha `stroke-width` 2.2, `.st-node.is-dim` 0.32, reduced-motion, copy "Diagrama de estados no disponible", open-on-click, is-viewer, color="viewer", `.st-svg overflow:hidden`, `:host([hidden])`, `.st-node.is-active` no aplica `transform`).

---

### `src/components/diagrams/swimlane-diagram.css` (100 LOC) — animation

**Tests existentes**: none found.
**Propuestas nuevas (12)**: incluye `.sw-link.is-active` ensancha `stroke-width` a **1.8** (≠ 2.2 de flow/er), `.sw-step.is-active .sw-step__box` aplica `filter: brightness(1.15)`, `.sw-step.is-dim` 0.3, `.sw-lane__band { opacity: 0.7 }` sin transición, copy "Diagrama de carriles no disponible", `.sw-link.is-active` no aplica `filter` (vs `.sw-step`).

---

### `src/components/diagrams/timeline.css` (98 LOC) — animation

**Tests existentes**: none found.
**Propuestas nuevas (12)**: incluye `.tl-event.is-active .tl-event__card` aplica `stroke-width: 2`, `.tl-event.is-dim` 0.32, `.tl-event-label .tk-inline-icon` 1em con `vertical-align: -0.14em`, copy "Línea de tiempo no disponible", `.tl-event.is-active` no aplica `transform`.

---

### `src/components/diagrams/use-case-diagram.css` (100 LOC) — animation

**Tests existentes**: none found.
**Propuestas nuevas (11)**: incluye `.uc-link.is-active` ensancha `stroke-width` a 1.8, `.uc-case.is-active .uc-case__shape` y `.uc-actor.is-active .uc-actor__figure` comparten `filter: brightness(1.15)` (contrato unificado), `.uc-actor.is-dim, .uc-case.is-dim, .uc-link.is-dim` atenúan a 0.3, copy "Diagrama de casos de uso no disponible", `.uc-link.is-active` no aplica `filter`.

---

### `src/components/diagrams/venn-diagram.css` (96 LOC) — animation

**Tests existentes**: none found.
**Propuestas nuevas (12)**: incluye `.vn-set__circle` transición `fill-opacity 0.14s ease-out`, `.vn-set.is-active .vn-set__circle` aplica `fill-opacity: 0.36`, `.vn-region.is-active .vn-region__label` cambia a `font-weight: 700`, `.vn-set.is-dim` 0.3, copy "Diagrama de Venn no disponible", `.vn-set.is-active` no aplica `stroke-width`.

---

## Acciones (12 testables, ~230 propuestas)

> Los informes completos de cada testable ya se entregaron en este formato durante la conversación. Resumen de hallazgos clave:

### `button-group.preview.controller.ts` (≈493 LOC)
**12 propuestas** cubriendo: `paintSel()` inicial, `apiOrient` cicla orientación, `apiAppear` wrap joined→segmented→separated, `apiPill` alterna booleano, `apiValue` setea valor y emite `is-change`, `#apiLog` prepende con timestamp y elimina `.hint`, `whenDefined` antes de tocar DOM, idempotencia de `mount()`, orden de secciones declaradas, tabla keyboard, tabla reference con 9 atributos.

### `button-group.ts` (≈299 LOC)
**15 propuestas** cubriendo: `items` filtra elementos con `slot`, `#valueOf()` fallback `value attr → textContent → index`, `select="single"` allow-empty toggle, `select="multiple"` toggle por valor, `#readSelection()` atributo `value` gana sobre `selected`, teclado horizontal ArrowRight/Left con wrap, Home/End, vertical ArrowUp/Down, skip `disabled`, `#syncA11y()` reenvía `label` y `aria-orientation`, `is-change` solo si cambia, `#applySelection` quita atributo `value` en `select="none"`, `variant` inválido → `joined`, compat nativo `<button>`, listener `click`/`keydown` idempotente.

### `button.preview.ts` (≈192 LOC)
**14 propuestas** cubriendo: playground variant retira atributo en `filled`, `#pgHref` aplica triple `href+target+rel`, borrar href retira los 3 atributos, `#pgStart` añade `<is-icon slot="start">`, limpiar icono anterior, icono huérfano sin slot se elimina, `#pgText` reemplaza label preservando iconos, `<is-code>` recibe HTML escapado, `dataset.cmSource`/`dataset.cm`, eventos registra 4 CustomEvents con JSON detail, `#evtLog` cap 12 líneas, listeners con fallback selector, `mount()` sin ctx usa `document`, `unmount()` no-op.

### `button.ts` (≈491 LOC)
**30 propuestas** cubriendo: form-associated `attachInternals`, `setCustomValidity('')`, `is-invalid` en submit, `#boundClick` con `type="submit"`/`"reset"`/defaultPrevented/disabled/loading/href, `hue` publica `--is-button-selected-hue/--color`, normalización módulo, `color` literal CSS vs semántica, `color-hover|color-active|color-text` reinstall, `disabled` `tabindex=-1` regresión ago/2026, `tabindex` del autor respetado, `#syncTag` re-bind listners, `delegatesFocus`, `setFocus({preventScroll})`, `loading` aplica custom states + `aria-busy`, `icon-only` detecta `:state(icon-button)`, `href` cae a `:state(link)`, `shape` inválido → `round`, `formResetCallback` restaura `#initialAttrs`, `formDisabledCallback`/`formStateRestoreCallback`, ARIA forwarded completo, 6 CSS Parts, `is-click` con `composed:true, bubbles:true`, pre-upgrade de propiedades.

### `check-icon-button.ts` (≈160 LOC)
**17 propuestas** cubriendo: `role="button"` solo si no estaba, `aria-pressed` sincronizado, `aria-label` cambia entre `label`/`checked-label`, fallback de aria-label, Enter activa con `preventDefault`, Space activa sin scroll, `disabled` bloquea click/teclado, click programático coincide, `icon`/`checked-icon` según estado, sin iconos retira atributo, `disabled` propaga al interno, `exportparts` expone `::part(button)`, `connectedCallback` engancha keydown, `toggle(force)` fuerza valor, `attributeChangedCallback` solo dispara si cambia, `#render` aplica `tabindex` según disabled, **`variant` observado pero NO se reenvía al interno** (gap detectado).

### `context-menu.ts` (≈219 LOC)
**21 propuestas** cubriendo: right-click abre, sin `for` abre en clientX/Y, `disabled` desactiva listener, `for` rebind, selector inexistente no rebind, `is-open` con `{x,y}`, `is-close`, `close()` idempotente, `openAt` flip horizontal, pega al borde, `openAtElement(el)`, click en `.item` emite `is-select` con `{item, value}`, value cae a `textContent.trim()`, click fuera de items no emite, `contextmenu` sobre `<dialog>` bloqueado, `scroll-lock` alterna en caliente, click fuera cierra, Escape cierra, `disconnectedCallback` desengancha, containing block `transform`, `#host` se asigna en connectedCallback.

### `copy-button.ts` (≈376 LOC)
**20 propuestas** cubriendo: `value` copia, `value=""` → `is-error`, `from="#id"` copia textContent trim, `from="#id[attr]"` copia atributo, `from="#id.prop"` accede propiedad JS (security surface documentada), `from` mal formado, `from` selector en ShadowRoot, trigger slotted oculta botón interno, `tooltip="none"` desactiva, `tooltip-placement` reenvía, `feedback-duration` ausente/""/numérico, `feedback-duration="abc"` → 1000ms, `#copying` re-entrance, estado success vuelve a rest, live region anuncia, hover mantiene tooltip durante feedback si `tooltip="full"`, `disabled` propaga, `disconnectedCallback` limpia timer, `tooltip="copy"` solo tras copiar, iconos slotted override.

### `dropdown-item.ts` (≈259 LOC)
**19 propuestas** cubriendo: click normal emite `is-dropdown-item-select` y cierra, disabled stopPropagation, submenú toggle NO emite selección, tipo checkbox toggle `checked`, role `menuitemcheckbox`/`menuitem`, `aria-checked` solo en checkbox, `aria-disabled`/`tabindex`, `aria-haspopup="menu"` según slot, Enter/Space invocan click, ArrowRight abre submenú / ArrowLeft cierra, `closeSubmenu()` recursivo en cascada, show/hide submenú con popover API o fallback `hidden`, `computePosition` placement `right-start` distance 2 flip+shift strategy fixed, listeners `scroll`/`resize` simétricos, `data-has-submenu`, color `danger`, `hasSubmenu` recalcula tras `slotchange`, tabIndex ajustado a 0, `is-dropdown-item-select` consumido por dropdown.

### `dropdown.ts` (≈288 LOC)
**22 propuestas** cubriendo: `open` `attrBool`, `placement` `attrEnum` fallback default, click trigger toggle open con stopPropagation, `#bindTrigger()` aplica `aria-haspopup="menu"`/`aria-expanded`, `aria-expanded` actualiza al abrir/cerrar, `<dialog>` top-layer, primer item recibe foco tras rAF, cancel del dialog cierra y devuelve foco al trigger, click sobre dialog (fuera de menú) cierra, click en item emite `is-select` cancelable + preventDefault, ArrowDown/Up mueven foco, navegación salta items disabled, `popup-dismiss` re-engancha, `#reposition` en scroll/resize, `dataset.currentPlacement`, close externo sincroniza open, `is-show` cancelable impide abrir, `is-hide` cancelable impide cerrar, `#mountActions()` reactivo, `data-has-checkbox`, disconnectedCallback cierra, `distance`/`skidding` numéricos.

### `fab.ts` (≈161 LOC)
**17 propuestas** cubriendo: `role="button"`/`"link"` según `href`, `position` → `dataset.position` con default `bottom-end`, `position` inválido → default, color pasa al interno, color inválido → `'brand'`, `pulse|extended|without-shadow` como clase en interno, `href` propaga al interno, click emite `is-fab-click {originalEvent}`, `icon` actualiza el `<is-icon>` interno, slotted icon tiene prioridad, `aria-label` se reenvía, `extended` con slot vacío muestra `label`, `extended` con slotted gana al label, `shape="pill"` fijo, CSS Parts `::part(base|icon|label)`, `is-fab-click` una vez por click, setter `position(null)` retira atributo.

### `share-button.ts` (≈68 LOC)
**13 propuestas** cubriendo: `navigator.share` invoca y emite `is-share {how:'shared'}`, AbortError → no evento, otra razón → `is-share` no se emite y vuelve a `'fail'`, fallback `clipboard.writeText` → `is-share {how:'copied'}`, `url` ausente usa `location.href`, `share-title` ausente usa `document.title`, `disabled` bloquea, re-entrante (doble click), `#busy` vuelve a false, slotted content sobrescribe icono+label, `<is-button>` interno recibe `disabled`, `stopPropagation`, eventos correctos según `how`.

### `speed-dial.ts` (≈625 LOC)
**30 propuestas** cubriendo: trigger abre el dial, `is-toggle` con open/closed, no duplicación si `checked===isOpen`, click en acción emite `is-select`, acción con `href` no cierra, acción disabled no emite, `aria-expanded` sincronizado, Escape cierra, pointerdown fuera cierra, `direction` cambia `data-direction`, `direction` inválido → default, `icon`/`open-icon`/`label` sincronizan al interno, acciones reciben `--sd-x`/`--sd-y`, `data-packed` activa cuando no caben, `data-rings` describe reparto, `autoStartAngle` apunta a cuadrante con más espacio, `autoArc` reduce según esquina, `data-layout="grid"` fuerza grid, `data-wrapper` acota contra `closest`, MutationObserver reactiva, ResizeObserver re-mide, `document.fonts.ready` causa segundo layout, disconnectedCallback limpia observers, `is-speed-dial-action` arranca con `role="menuitem"`/`tabindex="-1"`, `disabled` aplica `tabindex=-1`/`is-disabled`, `color` aplica `data-color`, `label` reenvía a `aria-label`, con `href` stopPropagation, `toggle()` cierra si abierto, `#clearRadial` al cambiar dirección.

---

## Media (12 testables, 196 propuestas)

### `src/components/media/avatar.ts` (146 LOC, `is-avatar`)
**Atributos**: `image`, `initials`, `label`, `loading`, `shape`. **Slot**: `icon` (fallback). **Eventos**: `is-error`. **CSS Parts**: `::part(avatar)`, `::part(image)`, `::part(initials)`, `::part(icon)`. Estados: `#imgFailed`.

**12 propuestas**:
1. **Foco del fallback cuando `image` falla** — [edge-case]: jsdom + `<img>` mock dispara `error`; tras el error `part="image"` con `hidden=true`, `part="initials"` visible con textContent `initials`; `is-error` se emite una vez; `aria-label` se mantiene; segundo `setAttribute('image', mismoUrl)` reintenta (resetea `#imgFailed`).
2. **`image` con esquema `javascript:` se rechaza en SSR** — [xss]: `#img.src` queda vacío.
3. **`initials` se trunca a 2 caracteres en mayúsculas** — [ui/ux]: `slice(0,2)` + `toUpperCase`.
4. **`label` controla `alt` y `aria-label`** — [a11y]: orden de prioridad `label || initials || 'Avatar'`.
5. **`shape="invalid"` se normaliza a `circle`** — [edge-case]: getter con `VALID_SHAPE`.
6. **`loading="lazy"` se aplica al `<img>` interno** — [performance].
7. **`slotchange` en `slot[name="icon"]` re-pinta la vista** — [integration].
8. **Evento `is-error` se emite una sola vez por fallo** — [edge-case]: idempotencia del handler.
9. **Imagen válida con `crossorigin` se acepta** — [browser-api]: rama feliz de `#load`.
10. **Renderiza el fallback icon en ausencia total de props** — [edge-case]: rama final de `#syncView`.
11. **CSS Parts `image` / `initials` / `icon` son estilizables** — [ui/ux]: `::part(initials) { background: red }`.
12. **`loading="auto"` se rechaza y cae a `eager`** — [edge-case]: validación contra lista `VALID_LOADING`.

### `src/components/media/barcode-scanner.ts` (115 LOC, `is-barcode-scanner`)
**Atributos**: `formats` (CSV), `disabled`. **Browser APIs**: `BarcodeDetector`, `navigator.mediaDevices.getUserMedia({ video: { facingMode: 'environment' } })`. **Eventos**: `is-detect { rawValue, format, barcodes }`, `is-error`.

**13 propuestas**:
1. **Permiso de cámara denegado (`NotAllowedError`)** — [browser-api]: mock `getUserMedia` rechaza; `is-error` con `detail.message`; `#stream` queda `null`.
2. **`BarcodeDetector` no disponible (Safari)** — [browser-api]: borrar `globalThis.BarcodeDetector`; `detect()` emite `is-error { message: 'BarcodeDetector no disponible' }` y devuelve `[]`.
3. **`disabled` desactiva `start()` y re-aplica el atributo al botón** — [a11y].
4. **Cambio de `formats` en vivo reinicia el scanner** — [edge-case]: spy `stop()` y luego `start()`.
5. **`detect()` con `<img>` válido emite `is-detect` y devuelve barcodes** — [browser-api]: `{ rawValue: '7701234567897', format: 'ean_13', barcodes: [...] }`.
6. **`detect()` sin resultados emite promesa vacía sin `is-detect`** — [edge-case]: guarda `if (barcodes.length)`.
7. **`stop()` libera tracks y limpia `srcObject`** — [browser-api]: spy `MediaStreamTrack.stop()`.
8. **`stop()` al desconectar (`disconnectedCallback`)** — [integration]: cleanup hook.
9. **Bucle de polling: 3 detecciones en 1.2 s** — [performance]: `setTimeout` 400 ms.
10. **`formats` setter acepta array y string** — [ui/ux].
11. **Sin HTTPS / sin contexto seguro: el navegador bloquea `getUserMedia`** — [browser-api]: `window.isSecureContext = false`.
12. **Detección con `canvas` (uso programático)** — [integration]: `detect(canvasEl)`.
13. **Cambio de `disabled` tras mount no dispara `start()`** — [edge-case]: rama inicial de `start()`.

### `src/components/media/barcode.ts` (199 LOC, `is-barcode`)
**Atributos**: `value`, `type` (ean13|code128), `height`, `fg`, `bg`, `show-text`, `quiet`. **Eventos**: `is-render { svg }`. **Algoritmos**: Code128B + EAN-13 con check digit GS1.

**14 propuestas**:
1. **EAN-13 con 12 dígitos calcula correctamente el check digit** — [edge-case]: Colombia `770123456789` → verificador 7; `<text>` muestra dígito 13.
2. **EAN-13 con menos de 12 dígitos se rechaza** — [edge-case]: `#text.hidden === true`, `#svg.innerHTML === ''`.
3. **EAN-13 con caracteres no numéricos los descarta** — [edge-case]: `replace(/\D/g, '')` deja 12 dígitos.
4. **Code128 con caracteres no ASCII los salta** — [edge-case]: `if (code < 32 || code > 127) continue`.
5. **Code128 vacío produce SVG vacío y oculta texto** — [edge-case]: guarda `if (!bits)`.
6. **Tipo no soportado (`qr` u otro) muestra mensaje** — [ui/ux]: `'Tipo "qr" no soportado'`.
7. **Quiet zones de EAN13 (`quiet="9"`) añaden padding** — [edge-case]: `bits.length` + 40 (20 a cada lado).
8. **`fg` y `bg` personalizan colores** — [ui/ux].
9. **`show-text="false"` oculta la etiqueta** — [ui/ux].
10. **Cambio de atributo re-renderiza el SVG** — [edge-case]: `attributeChangedCallback` y `#render`.
11. **Code128 largo (200 caracteres) sin bloquear UI** — [performance].
12. **`is-render` expone el nodo `<svg>` para integración** — [integration]: `detail.svg`.
13. **XSS en `value` no rompe el SVG** — [xss]: Code128B sólo mira `charCode`; no interpreta HTML.
14. **EAN13 verificador con todos los dígitos `0`** — [edge-case]: check digit = 0; bits EAN13 producidos.

### `src/components/media/icon-explorer.preview.ts` (614 LOC)
**Browser APIs**: `fetch`, `IntersectionObserver`, `URL`, `Image`, `Blob`, `URL.createObjectURL`. **Storage en memoria**: `jsonCache`, `svgCache`. Sin auth real.

**15 propuestas**:
1. **Carga del `index.json` y render inicial** — [integration]: mock fetch; badge de conteo "2 familias · 8 iconos".
2. **`renderFamily` con familia inexistente muestra callout de error** — [edge-case]: `<is-callout color="danger">` con prefijo escapado.
3. **Búsqueda global con debounce (220 ms en modo "icon")** — [ui/ux]: `paintIcons` corre; resultados ≤ 600.
4. **`ensureIcons` con concurrencia 8 y progreso** — [performance]: callback invocado 231 veces; `loadingIcons` se completa una vez.
5. **`filtersMarkup` filtra por categoría / paleta / license** — [edge-case]: rama `f.palette === 'mono'`.
6. **`sync()` valida tamaño y color, marca `error` en `<is-input>`** — [a11y]: sizeVal=5000 → toggleAttribute error.
7. **`buildSvg()` con `color='currentColor'` deja el path intacto** — [edge-case]: `if (color && color !== 'currentColor')`.
8. **XSS en nombre de familia — `esc()` escapa `<`, `>`, `&`, `"`** — [xss]: helper `esc`.
9. **`downloadPng` produce Blob PNG y dispara `<a download>`** — [integration]: flujo de exportación PNG.
10. **`validate()` con `unit='auto'` ignora tamaño** — [edge-case]: cortocircuito de auto.
11. **`fDownload` con valores inválidos no descarga y muestra toast danger** — [edge-case]: guarda inicial.
12. **`paintIcons` cancela tokens obsoletos (`searchToken`)** — [performance]: `if (token !== searchToken) return`.
13. **Captura de errores en `IntersectionObserver`** — [edge-case]: `.catch(() => {})` del observer.
14. **`renderIndex` con familias vacías muestra callout** — [edge-case]: rama sin familias.
15. **`fColorPick` actualiza `color` y dispara `sync`** — [integration]: listener de picker.

### `src/components/media/icon.ts` (245 LOC, `is-icon`)
**Atributos**: `icon` (Iconify id), `name`, `library`, `label`, `src`. **Browser APIs**: `fetch`, `AbortController`. **Estados**: `data-loading`, `data-missing`. **CSS Part**: `::part(icon)`.

**15 propuestas**:
1. **`icon="mdi:home"` resuelve y pinta SVG inline** — [browser-api]: data-loading removido; data-missing ausente; `width="1em" height="1em"`; `fill="currentColor"`.
2. **`src` propio gana sobre `icon`** — [integration]: fetch se llama UNA sola vez con la URL de `src`.
3. **`icon` vacío → `data-missing` ausente, sin pintar** — [edge-case]: `if (!icon)`.
4. **Cambio de atributo durante carga aborta fetch previo** — [performance]: spy `AbortController.abort`.
5. **`fetch` 404 → `data-missing`** — [browser-api]: `if (!res.ok) return null`.
6. **SVG sin etiqueta `<svg>` se descarta** — [edge-case]: `text.includes('<svg')`.
7. **`label=""` aplica `aria-hidden="true"`** — [aria]: secuencia aria-hidden/role/aria-label.
8. **Icono multicolor (linearGradient) NO se aplana a `currentColor`** — [edge-case]: `IsIcon.#isMulticolor()` + bypass.
9. **Icono monocromo se aplana a `currentColor`** — [edge-case]: `#NEUTRAL` set.
10. **`name="home"` con `library="mdi"` se compone a `mdi:home`** — [ui/ux]: getter de `icon`.
11. **`name="mdi:home"` con `:` se respeta literal** — [ui/ux]: rama `if (name.includes(':'))`.
12. **`src` con CORS falla → `data-missing`** — [browser-api]: `catch { return null }`.
13. **`disconnectedCallback` aborta el fetch pendiente** — [integration].
14. **Cache: segundo `icon` igual no vuelve a fetchear** — [performance]: `rawCache` en `icon-loader`.
15. **Reducción de movimiento: el cambio de `loading` no anima** — [reduced-motion]: `prefers-reduced-motion`.

### `src/components/media/image-editor.ts` (330 LOC, `is-image-editor`)
**Atributos**: `src`, `zoom`, `rotation`, `aspect`. **Slot**: `toolbar`. **Eventos**: `is-load`, `is-change`, `is-crop`. **Browser APIs**: `<canvas>`, `ResizeObserver`.

**15 propuestas**:
1. **Carga de imagen y emisión de `is-load`** — [browser-api]: `cropRect` inicial = `{x:160, y:100, width:1280, height:800}` (80% centrado).
2. **`src` inválido muestra mensaje en `#status`** — [edge-case]: `#status.textContent === 'No se pudo cargar la imagen'`.
3. **`crop()` devuelve dataURL con dimensiones correctas** — [browser-api]: dataURL empieza con `data:image/png;base64,`.
4. **`zoom` setter clamp entre 0.1 y 8** — [edge-case]: `Math.max(0.1, Math.min(8, v))`.
5. **`rotation` setter normaliza a [0, 360)** — [edge-case]: `((n % 360) + 360) % 360`.
6. **`aspect="16/9"` mantiene proporción al arrastrar esquina** — [integration]: `cropRect.width / cropRect.height ≈ 1.777`.
7. **`aspect` inválido (`"foo"`) se evalúa como `null`** — [edge-case]: `evalAspect`.
8. **Pointer drag mueve el crop dentro de los bordes** — [ui/ux]: `#onMove` rama `case 'move'`.
9. **`cropped()` sin imagen cargada devuelve `null`** — [edge-case]: `if (!this.#img || !this.#cropRect.width)`.
10. **`is-change` se emite en cada `pointermove`** — [performance]: 5 eventos con `crop` consistente.
11. **Resize del viewport dispara `#draw()`** — [integration]: `ResizeObserver`.
12. **XSS vía `src` que retorna SVG con script** — [xss]: `cropped()` produce PNG puro (aislamiento canvas).
13. **Reducción de movimiento: el resize se re-pinta sin animación** — [reduced-motion].
14. **`cropped()` con crop vacío (width=0) → null** — [edge-case]: guarda.
15. **`applyRotation(-90)` desde rotation=0 → 270** — [edge-case]: `is-change`/`is-crop` no se emite.

### `src/components/media/media-recorder.ts` (137 LOC, `is-media-recorder`)
**Atributos**: `source` (camera|mic|display), `disabled`. **Eventos**: `is-start`, `is-stop { blob, url, type }`, `is-error`. **Browser APIs**: `getUserMedia`, `getDisplayMedia`, `MediaRecorder`.

**14 propuestas**:
1. **Permiso de micrófono denegado (`NotAllowedError`)** — [browser-api]: `#rec === null`.
2. **`source="mic"` con `MediaRecorder` no soportado** — [browser-api]: `typeof MediaRecorder !== 'function'`.
3. **`source="display"` invoca `getDisplayMedia`** — [browser-api]: `{ video: true, audio: true }`.
4. **`source="camera"` con audio+video** — [browser-api]: default camera.
5. **`stop()` revoca URL previa y emite `is-stop`** — [browser-api]: `URL.revokeObjectURL` previo.
6. **`is-stop` contiene `blob.type='video/webm;codecs=vp9'` cuando hay soporte** — [browser-api]: branch mime preferida.
7. **`disabled` evita `start()`** — [a11y]: guarda inicial.
8. **`source` setter acepta valores no listados (`audio`)** — [edge-case]: getter lo ignora.
9. **Cambio de `source` en vivo para la grabación** — [integration]: `attributeChangedCallback` para `source`.
10. **`is-stop` payload en modo `mic`** — [ui/ux]: `dl.download === 'audio.webm'`.
11. **`disconnectedCallback` libera recursos** — [integration]: cleanup.
12. **Blob vacío (0 chunks) produce `is-stop` válido** — [edge-case]: `#chunks = []` antes.
13. **`MediaRecorder.isTypeSupported` falso → fallback a `video/webm` simple** — [browser-api]: ternario.
14. **Reducción de movimiento: la animación del botón "Detener"** — [reduced-motion].

### `src/components/media/qrcode.ts` (148 LOC, `is-qrcode`)
**Atributos**: `value`, `level` (L|M|Q|H), `cell`, `margin`, `fg`, `bg`. **Eventos**: `is-render`. **API**: `svg`, `dataURL()`. **Dependencia**: `import('https://esm.sh/qrcode-generator@1.4.4')`.

**14 propuestas**:
1. **`value="hola"` genera QR válido** — [browser-api]: `<path d="...">` no vacío.
2. **`value` cambia el QR** — [integration]: nuevo `is-render`.
3. **`value` vacío no produce SVG** — [edge-case]: `if (!value) return;`.
4. **`level` inválido (`Z`) cae al default `L`** — [edge-case]: fallback `|| 'L'`.
5. **`fg` y `bg` se aplican al path** — [ui/ux]: `<rect fill="#000">` y `<path fill="#ff0">`.
6. **`cell` y `margin` afectan dimensiones** — [ui/ux]: `svg.width === (size + 8) * 10`.
7. **`dataURL()` devuelve PNG** — [browser-api]: `data:image/png;base64,...`.
8. **`dataURL()` sin SVG previo devuelve `null`** — [edge-case]: `if (!svg) return null`.
9. **Fallo de carga de `qrcode-generator` (offline)** — [browser-api]: `#status.textContent === 'No se pudo cargar qrcode-generator (offline?)'`.
10. **`value` con caracteres Unicode (emoji, chino) genera QR válido** — [edge-case]: branch UTF-8.
11. **`value` extremadamente largo (2000 chars) → QR complejo** — [performance]: sin stack overflow.
12. **XSS vía `value` con `<script>`** — [xss]: QR usa `addData` que trata el string como bytes.
13. **`bg="transparent"` omite el `<rect>` de fondo** — [edge-case]: `if (bg !== 'transparent')`.
14. **Reducción de movimiento: cambios rápidos de `value` no animan** — [reduced-motion].

### `src/components/media/speech.ts` (145 LOC, `is-speech`)
**Atributos**: `lang`, `text`. **Eventos**: `is-result { transcript, isFinal }`, `is-speak-end`, `is-error { message }`. **Browser APIs**: `SpeechRecognition`/`webkitSpeechRecognition`, `SpeechSynthesisUtterance`.

**14 propuestas**:
1. **`SpeechRecognition` no disponible (Firefox)** — [browser-api]: guarda `if (!Ctor)`.
2. **`listen()` emite `is-result` con `transcript` final** — [browser-api]: handler `onresult`.
3. **`is-result` distingue final de interim** — [edge-case]: rama `if (ev.results[i].isFinal)`.
4. **Error `no-speech` se ignora silenciosamente** — [edge-case]: guarda `if (ev.error === 'no-speech' || ev.error === 'aborted')`.
5. **`lang` cambia en sesión activa re-aplica al `rec.lang`** — [integration]: `attributeChangedCallback`.
6. **`text` cambia → `speak()` automático** — [integration]: branch `text`.
7. **`speak()` con `speechSynthesis` ausente** — [browser-api]: guarda `if (!window.speechSynthesis)`.
8. **`stop()` limpia sesión y desactiva `aria-pressed`** — [a11y]: `#syncListen`.
9. **`onend` re-arranca si `#listening` sigue activo** — [edge-case]: `if (this.#listening) try { rec.start(); }`.
10. **`cancel()` aborta utterance pendiente** — [browser-api]: spy `window.speechSynthesis.cancel`.
11. **`speak()` con `is-speak-end`** — [integration]: `u.onend`.
12. **`text` getter toma `textContent` si no hay atributo** — [edge-case]: fallback `this.textContent`.
13. **Reducción de movimiento: nada que animar (no UI animada)** — [reduced-motion]: documentar.
14. **XSS vía `text` con HTML** — [xss]: SpeechSynthesisUtterance usa `.text` como String.

### `src/components/media/theme-img.ts` (154 LOC, `is-theme-img`)
**Atributos**: `src-dark`, `src-light`, `alt`, `shape` (circle|rounded|square), `fit` (contain|cover), `theme`, `loading`. **CSS Part**: `::part(image)`. **Watcher de tema** via `theme-scope.js`.

**13 propuestas**:
1. **Cambio de tema del contenedor dispara `data-active-theme`** — [integration]: `#img.src` cambia a `'l.svg'`; `host.dataset.activeTheme === 'light'`.
2. **`theme="dark"` fuerza la imagen dark incluso si el contenedor está en light** — [edge-case]: `this.theme || readTheme(...)`.
3. **`src-light` ausente cae a `src-dark`** — [edge-case]: `theme === 'light' ? light || dark : dark || light`.
4. **`alt` se aplica al `<img>` interno** — [a11y]: `this.#img.alt = this.alt`.
5. **`shape` inválido se ignora** — [edge-case]: `VALID_SHAPE.has`.
6. **`fit="cover"` aplica `object-fit: cover`** — [ui/ux]: styleAttrs binding.
7. **`loading="eager"` se aplica al `<img>` interno** — [performance].
8. **`loading="async"` se ignora (no se aplica)** — [edge-case]: guarda `if (loading === 'lazy' || loading === 'eager')`.
9. **Watcher se desuscribe en `disconnectedCallback`** — [integration]: `#unwatch?.()`.
10. **Tema cambia en runtime — sin recarga** — [integration]: rama watch.
11. **`data-active-theme` no entra en `THEME_SCOPE`** — [edge-case]: nota del comentario L137-138.
12. **XSS en `src-dark` con `javascript:`** — [xss]: no sanitización explícita — riesgo documentado.
13. **Reducción de movimiento: `prefers-reduced-motion`** — [reduced-motion].

### `src/components/media/video-playlist.ts` (780 LOC, `is-video-playlist`)
**Atributos**: `autoplay-next`, `placement` (left|right|bottom), `channel`, `accordion` (auto|open|closed). **Slots**: default, `tools-left`, `tools-right`, `config`. **CSS Parts**: 14 parts (`playlist*`, `player-*`, `tools-*`, `seek`, `time`, etc.). **Eventos**: `is-video-change`, `is-change`.

**27 propuestas** (resumen de las más impactantes):
- **Integración**: Slot con `<is-video>` actualiza lista al `slotchange` (data-index 0 y 1, primer item aria-selected).
- **Eventos**: `goTo(1)` cambia `is-video-change` con `previousIndex=0, currentIndex=1`. `goTo(-1)` se clampa a 0. `goTo(99)` se clampa al último.
- **Autoplay**: `autoplay-next=true` + `is-ended` → `next()` automático.
- **Teclado**: ArrowDown mueve selección y llama `goTo`. Enter en item activo llama `goTo`. ArrowUp en primer item no hace nada.
- **UI/UX**: Click en item llama `goTo`. `placement="left"` aplica dataset. `placement="top"` se ignora (cae a bottom).
- **Accordion**: `accordion="auto"` con viewport >720px abierto, <720 cerrado. Click en playlist-toggle alterna.
- **Volumen**: Volumen y mute del video activo se sincronizan al slider.
- **Seek**: Seek input actualiza `vp-time`. Seek `pointerup` aplica `currentTime` real.
- **Idempotencia**: `goTo` emite `is-change` y `is-video-change` sólo cuando cambia.
- **Poster**: Poster del video activo se muestra en miniatura. Captura automática si `poster` ausente y `readyState >= 2`.
- **MutationObserver**: Actualiza la lista si cambia `title` o `poster`.
- **Focus**: Foco inicial al item activo tras mount (`tabindex="0"`).
- **ARIA**: `role="listbox"` y `role="option"` con `aria-selected`.
- **XSS**: Vía `title="<img onerror=alert(1)>"` — `textContent` evita inyección.
- **Cleanup**: `#unbindVideos` en `disconnectedCallback`.
- **`channel`**: Attribute actualiza la cabecera.

### `src/components/media/video.ts` (588 LOC, `is-video`)
**Atributos**: `src`, `poster`, `without-controls`, `muted`, `loop`, `autoplay`, `playsinline`. **Eventos**: `is-play`, `is-pause`, `is-ended` + nativos. **CSS Parts**: 13 parts. **Teclado**: espacio/k, m, f, ←/→ ±5s, j/l ±10s, ↑/↓ ±5%, 0–9 saltos por decenas. **Browser APIs**: `requestFullscreen`, `pictureInPicture`, `MediaQuery`. **Auto-hide chrome** tras 2600 ms idle. **Nota**: tag `database` en `<is-video>` parece incorrecto — el componente no usa IndexedDB.

**32 propuestas** (resumen de las más impactantes):
- **Keyboard**: Reproducir/pausar con space/k. Mute con m. Seek ±5s con ArrowLeft/Right. Seek ±10s con j/l. Volumen ±5% con ArrowUp/Down. Salto a 50% con tecla "5". Pantalla completa con f. Atajos no se disparan con ctrl/meta/alt.
- **Reduced-motion/UI**: Auto-hide chrome tras 2.6s de inactividad. `pointermove` despierta el chrome.
- **Integración**: `#distributeSlot` mueve `<source>` y `<track>` al `<video>` interno. Sin `src` y sin slotted sources → `#video.removeAttribute('src')`. `without-controls` oculta la chrome y el scrim.
- **Browser-API**: `pip-button` se oculta si `document.pictureInPictureEnabled === false`. `ratechange` actualiza menú de velocidad. Menú de velocidad: click fuera cierra. Click en `#big-play` llama `togglePlay()`. Doble click en `<video>` entra pantalla completa.
- **A11y**: `tabindex` se establece en connectedCallback. Sliders manejan sus propias flechas (no se duplican).
- **Edge-case**: Tiempo formateado `1:02:03`. Buffer se pinta en barra `--buffered`. Pip: `enterpictureinpicture` actualiza `pipBtn.checked`.
- **Cleanup**: Disconnected limpia `fullscreenchange` listener.
- **Keyboard sin controls**: Sin `controls`, atajos de teclado no funcionan.
- **Integration**: `is-play`/`is-pause`/`is-ended` burbujean y composed. `poster` se aplica al `<video>.poster`. `src` setter actualiza `video.src`.

### Gaps transversales en media

1. **UX de fallback para permisos denegados** — patrón repetido en `barcode-scanner`, `media-recorder`, `speech`, `image-editor`. Falta validar el mensaje user-facing (toast, callout, hint visible). Cubierto parcialmente en `barcode-scanner` con `.hint`, ausente en `media-recorder`/`speech`/`image-editor`.
2. **`prefers-reduced-motion`** — sólo mencionado en `avatar.css` y `video.css`. No hay test transversal sistemático. Recomendación: suite que itere sobre los 12 y verifique `getComputedStyle(...).transitionDuration === '0s'`.
3. **XSS en atributos `src`/`value`/`name`** — cualquier atributo que termine en `src` o URL es vector XSS. Propuestas cubren `avatar.image`, `icon.src`, `theme-img.src-dark/light`, `qrcode.value`. **Falta validar `image-editor.src` y `video.src`** rechacen `javascript:`.
4. **Token expiry / auth** — el explorador no implementa auth real, pero descarga de assets remotos (Pages, jsDelivr) sí expuesta a 401/403. Falta test del caso "permission revoked mid-recording" (`track.onended`).
5. **Quota exceeded en `MediaRecorder`** — patrón `chunks.push(e.data)` sin límite. Falta test con `QuotaExceededError`.
6. **`AbortController` cleanup** — `icon.ts` y `video.ts` registran listeners globales. Falta test transversal de memory leaks después de 50 mount/unmount cycles.
7. **Permisos revocados en runtime** — si el usuario revoca permiso mid-recording, `MediaStreamTrack.stop()` se dispara. Cubierto en `disconnectedCallback` pero no en el caso "permission revoked".
8. **CSS Part coverage** — `video-playlist` declara 14 parts, `video` declara 13. No hay tests que verifiquen estilo de cada uno.
9. **Decisión de diseño abierta**: ¿respeta `<is-video>` `prefers-reduced-motion` para el autohide? — el chrome se oculta tras 2.6s. Si el usuario tiene reduce-motion, ¿debería seguir ocultándose?
10. **IndexedDB — tagging posiblemente incorrecto** — `<is-video>` está taggeado con `database` pero no usa IndexedDB. Sugerencia: revisar el audit pipeline.
11. **Aria-live bombardment en `<is-speech>`** — `aria-live="polite"` está bien aplicado. Falta validar que no se re-emita el mismo transcript.
12. **Doble handler teclado vs sliders en `<is-video>`** — `<input type="range">` ya consume flechas. No se testea el caso donde el usuario hace focus en `#seek` y presiona `m`.

### Recomendación final media

1. **Cobertura mínima viable**: implementar los **tests de browser-api mocking** (permisos denegados, BarcodeDetector no disponible, SpeechRecognition ausente) — son los más impactantes y los menos cubiertos por los Tier A existentes.
2. **Prioridad 2**: **keyboard tests de `<is-video>` y `<is-video-playlist>`** — el reproductor es el componente con más superficie de teclado.
3. **Prioridad 3**: **XSS / sanitization transversal** en atributos `src` / `value`.
4. **Prioridad 4**: **reduced-motion** transversal (una suite con `emulateMediaFeatures`).

---

## Charts + data-viz + demos (6 testables, 67 propuestas)

### `src/components/charts/chart.ts` (792 LOC, `is-chart`)
**Tags**: `bar | line | pie | doughnut | radar | polarArea | scatter | bubble`. Atributos: `type, label, legend-position, index-axis, min, max, grid, stacked, without-animation, without-legend, without-tooltip, x-label, y-label, color, open-on-click` + 18 styleAttrs. Eventos: `is-render`, `is-turtle-state`, `is-open-viewer` (cancelable). Slot JSON. **Sin `sql-query`** (tag inexistente en el archivo).

**12 propuestas** (resumen por categoría):
1. **T-CHART-01**: atributo `sql-query` rechazado sin caer — [sql-injection]: spy fetch/XHR; no se invoca; `eval`/`Function`/`innerHTML` no contiene la cadena SQL.
2. **T-CHART-02**: JSON malformado en slot no rompe render — [edge-case]: fallback "Sin datos"; conserva último payload válido.
3. **T-CHART-03**: XSS en `labels` y `dataset.label` — [xss]: SVG `<text>` con texto plano; no `<img onerror>` ni `<script>` parseados.
4. **T-CHART-04**: color="viewer" no dispara lightbox salvo valor exacto — [xss]: `isViewer === 'viewer'` estrictamente.
5. **T-CHART-05**: `without-animation` + `prefers-reduced-motion: reduce` — [reduced-motion]: `--dash`/strokeDasharray no se aplica.
6. **T-CHART-06**: Tab/Arrow navegan marks — [keyboard]: marks con `tabindex`, `aria-pressed` refleja visibilidad.
7. **T-CHART-07**: 10k puntos en `scatter` TTI < 1s — [performance]: budget < 1000 ms; sin memory leaks.
8. **T-CHART-08**: stacked negativo/positivo no rompe dominio — [edge-case]: `Math.min/max(0, vDomain[1])` baseline.
9. **T-CHART-09**: real-time 60 fps coalesce — [performance]: `#renderQueued` dedupa, 1 render por microtask.
10. **T-CHART-10**: toggle leyenda preserva dataset original — [data-validation]: `#hiddenSeries` único estado; `payload.datasets` no muta.
11. **T-CHART-11**: `legend-position` inválido cae a default — [data-validation]: `?? 'top'`.
12. **T-CHART-12**: `open-on-click` emite `is-open-viewer` cancelable — [integration]: con `preventDefault()` no se crea lightbox.

### `src/components/charts/sparkline.ts` (156 LOC, `is-sparkline`)
**Atributos**: `values, data, type, label, variant, curve, trend`. **No emite eventos**. **Bug smell**: gradient ID con `Math.random().toString(36).slice(2)` (línea 123) — riesgo de colisión + tests flaky.

**10 propuestas**:
1. **T-SPARK-01**: `data` con `NaN`, `null`, strings, `Infinity` — [edge-case]: setter filtra con `Number().filter(Number.isFinite)`.
2. **T-SPARK-02**: `values` vacío → render no rompe — [edge-case]: `#data.length === 0` rama temprana.
3. **T-SPARK-03**: `data` con un solo punto — [edge-case]: `Math.max(..., 1)` defensivo.
4. **T-SPARK-04**: Gradient ID único en 200 instancias — [edge-case]: **recomendar `crypto.randomUUID()`**.
5. **T-SPARK-05**: Atributo `trend` no implementado — [edge-case]: **gap de feature** documentado.
6. **T-SPARK-06**: Etiqueta 1000 chars en `data`/`label` — [edge-case]: < 500 ms para 100k puntos.
7. **T-SPARK-07**: `variant="line"` quita área pero conserva último punto — [data-validation]: 1 `<path>` + 1 `<circle>`, sin área.
8. **T-SPARK-08**: ResizeObserver dispara re-render idempotente — [performance]: cada resize re-renderiza, output estable.
9. **T-SPARK-09**: `type="bar"` con valores negativos — [edge-case]: `base = Math.min(dataMin, 0) = -5`.
10. **T-SPARK-10**: Sin eventos: comunicación por propiedad `data` reactiva — [integration]: **propuesta de mejora** añadir `is-data-change`/`is-hover`.

### `src/components/charts/treemap.ts` (317 LOC, `is-treemap`)
**Atributos**: `color, open-on-click`. Eventos: `is-render`, `is-open-viewer` (cancelable). Slot JSON. **Riesgo XSS**: `inlineMdWeb(n.label)` (línea 304).

**11 propuestas**:
1. **T-TM-01**: XSS en `node.label` (markdown inline) — [xss]: `inlineMdWeb` debe sanitizar; `tooltipEl.innerHTML` sin `<img onerror>`.
2. **T-TM-02**: `value` negativo o `NaN` — [edge-case]: squarified necesita positivos; `dataset.empty` si todos inválidos.
3. **T-TM-03**: Tesela diminuta con label largo — [edge-case]: `width<30` → `showLabel=false`.
4. **T-TM-04**: Sin nodos → estado vacío visible — [edge-case]: `dataset.empty === ''`.
5. **T-TM-05**: Hover entre teselas aplica `is-active`/`is-dim` — [a11y]: `#applyHover()` ramas.
6. **T-TM-06**: `color="viewer"` + click → `is-open-viewer` cancelable — [integration]: misma lógica que chart.ts pero `kind="treemap"`.
7. **T-TM-07**: Tab navega teselas — [keyboard]: **propuesta de mejora** añadir `tabindex` y `role="button"`.
8. **T-TM-08**: Resize con debounce ±4px — [performance]: solo callback con delta > 4px dispara `#queueRender()`.
9. **T-TM-09**: Tema `theme-light` cambia paleta — [data-validation]: `dataset.theme` flippea.
10. **T-TM-10**: `aria-label` refleja `layout.title` — [a11y]: `aria-label === 'Mi distribución'` o `'Treemap'`.
11. **T-TM-11**: JSON inválido en slot no rompe ni muta payload anterior — [edge-case]: `try/catch` resiliencia.

### `src/components/data-viz/heatmap.ts` (278 LOC, `is-heatmap`)
**Atributos**: `x-label, y-label, color, cell-radius, show-values, legend-position`. Paletas: `brand | neutral | success | warning | danger | red-blue`. Eventos: `is-cell-hover {x, y, value}`, `is-render`.

**11 propuestas**:
1. **T-HM-01**: Celdas con `null`, `NaN`, `undefined` no se renderizan — [edge-case]: filtro `Number.isFinite(v)`.
2. **T-HM-02**: Modo `points` con cobertura parcial — [data-validation]: matriz 2×3 con 4 nulls.
3. **T-HM-03**: XSS en `xLabels`/`yLabels` — [xss]: `t.textContent = String(lb)` evita `<script>`.
4. **T-HM-04**: `legend-position="none"` oculta la legend — [data-validation]: `legend.hidden === true`, `legendW=0`.
5. **T-HM-05**: `show-values` rota color de texto según intensidad — [data-validation]: contraste condicional + epsilon anti-división por cero.
6. **T-HM-06**: `color="red-blue"` produce gradiente divergente — [data-validation]: cobertura del palette divergente.
7. **T-HM-07**: Tab navega entre celdas con flechas — [keyboard]: navegación ortogonal patrón WAI-ARIA.
8. **T-HM-08**: Hover sobre celda emite `is-cell-hover` con `data-x`/`data-y` originales — [integration]: `detail = { x: 'mar', y: 'L0', value: number }`.
9. **T-HM-09**: `cell-radius` inválido cae a 2 — [data-validation]: `Number(...) || 2`.
10. **T-HM-10**: Dominio constante (todos los valores iguales) — [edge-case]: `hi===lo` → `palette[Math.floor(palette.length/2)]`.
11. **T-HM-11**: 10k celdas mantiene render < 1 s — [performance]: cuello de botella `cfg.points.find` O(n²).

### `src/components/data-viz/maps.ts` (260 LOC, `is-maps` + `is-map-marker`)
**Atributos**: `viewbox, zoom, engine, interactive`. **Sin tag `paty-iss`** (inexistente). **Riesgos de seguridad CRÍTICOS**: `cfg.attribution` via `innerHTML` (línea 127), `cfg.tileUrl` como `iframe.src` (línea 122) sin whitelist.

**12 propuestas**:
1. **T-MAP-01**: XSS vía `cfg.attribution` (CRÍTICO) — [xss]: **fallará** por línea 127 `innerHTML`; proponer `textContent`.
2. **T-MAP-02**: XSS/Smuggling vía `cfg.tileUrl` — [xss]: `javascript:`, `data:text/html,`, `//evil.com/`; proponer whitelist https/http.
3. **T-MAP-03**: Network failure en tiles — [edge-case]: componente no cae con `page.context().setOffline(true)`.
4. **T-MAP-04**: `viewbox` inválido no rompe render — [edge-case]: `#readViewbox` defensiva.
5. **T-MAP-05**: `viewbox` cubre antimeridiano (lon > 180) — [edge-case]: limitación documentada.
6. **T-MAP-06**: 1000 markers simultáneos — [performance]: O(n) en `#syncMarkers`.
7. **T-MAP-07**: Marker con `lat`/`lon` ausentes se ignora — [edge-case]: filtro `Number.isFinite`.
8. **T-MAP-08**: Wheel zoom centrado en cursor — [keyboard]: viewport se reduce alrededor del punto del cursor.
9. **T-MAP-09**: Drag con pan sin `interactive` — [data-validation]: `#vp` NO cambia.
10. **T-MAP-10**: Click en marker emite `is-marker-click` con referencia — [integration]: `detail.marker === <is-map-marker>` original.
11. **T-MAP-11**: Atributo `zoom` solo aplica al modo tile — [data-validation]: `zoom` solo en `cfg.zoom`, no en `#vp`.
12. **T-MAP-12**: Tab navega markers en SVG — [keyboard]: **propuesta**: añadir `tabindex="0"` + `role="application"`.

### `demos/diagramas/ER/er-editor.html` (Playwright UI demo, 85 líneas)
**Cobertura existente** (`_testing/er-editor.test.mjs`): smoke, drag snap 8px, undo, add-entity, delete, exportJson determinista, exportSvg válido, keyframes `iswc-dash-march`, viewBox determinista, aria-label panel, prefers-reduced-motion. **No se duplican**.

**11 propuestas**:
1. **T-ER-01**: Toolbar buttons accesibles vía teclado (Tab/Enter) — [keyboard]: orden de tabulación lógico.
2. **T-ER-02**: `add-relation` exige selección de 2 entidades — [UI/UX]: 0/1 selección no crea relación.
3. **T-ER-03**: `duplicate` clona entidad preservando atributos pero con ID nuevo — [data-validation]: `pos` desplazada para no solapar.
4. **T-ER-04**: Form input en panel lateral actualiza `payload` en tiempo real — [integration]: binding bidireccional con `<input>`.
5. **T-ER-05**: XSS en `entity.name` y `attribute.name` — [xss]: nombres renderizan como texto plano.
6. **T-ER-06**: `Ctrl+Z` undo / `Ctrl+Shift+Z` redo con historial profundo — [keyboard]: stack >50 acciones.
7. **T-ER-07**: Export JSON copia al clipboard correctamente — [integration]: `clipboard.writeText` con round-trip preserva estructura.
8. **T-ER-08**: SVG exportado respeta `prefers-reduced-motion: reduce` — [reduced-motion]: CSS embebido contiene `@media (reduce) { animation: none !important; }`.
9. **T-ER-09**: Selección múltiple con `Shift+click` y rect-drag — [UI/UX]: contrato multi-modo.
10. **T-ER-10**: Carga de archivos vía drag-and-drop / file input — [UI/UX]: válido reemplaza payload; malformado muestra error.
11. **T-ER-11**: Persistencia en `localStorage` sobrevive a reload — [integration]: gap potencial a verificar.

---

## Datagrid + previews + utils (3 testables, 38 propuestas)

### `src/components/data/datagrid-core/server-datasource.ts` (453 LOC)
**API**: `sqlLiteral`, `singleFilterToClause`, `convertFilterModelToSQL`, `buildJSONFiltro`, `createServerSideDatasource`, `createFakeLista`, `createFakeListaFromUrl`. **Sin Worker/HttpClient reales** (etiquetas imprecisas). **Tests existentes**: none found.

**12 propuestas**:
1. **1.1** `sqlLiteral` escapa apóstrofos y valores especiales — [sql-injection]: payload `'; DROP TABLE users; --` neutralizado.
2. **1.2** `singleFilterToClause` cubre 16 ops de ag-Grid — [api-contract]: text/number/date; kind desconocido→`''`.
3. **1.3** `convertFilterModelToSQL` filtra colIDs y respeta `multi`/`set` — [sql-injection]: colId no presente se omite; `set`→`IN (...)`.
4. **1.4** `buildJSONFiltro` traduce paginación/orden/filtros a `TFiltroLista` — [api-contract]: `pagina` 1-based.
5. **1.5** `buildJSONFiltro` con `endRow <= startRow` no rompe — [edge-case]: sin `NaN`/`Infinity`.
6. **1.6** `createServerSideDatasource` reporta `success` con `rowData`/`rowCount` — [integration]: `onLoad` recibe la lista.
7. **1.7** `createServerSideDatasource` llama `fail()` si `Lista` rechaza — [edge-case]: error no se relanza.
8. **1.8** `createServerSideDatasource` usa `idnfiltro` como string o función — [api-contract]: mutar entre llamadas.
9. **1.9** `createFakeLista` aplica WHERE, búsqueda libre, orden y paginación — [integration]: `totalpaginas = ceil(total/qregistros)`.
10. **1.10** `createFakeLista` interpreta SQL generado (LIKE, BETWEEN, IN) — [integration]: splitAnd con `AND` dentro de `'O''Reilly y AND'`.
11. **1.11** `createFakeLista` no rompe con `filtro` ausente o vacío — [edge-case]: defaults aplicados.
12. **1.12** `createFakeListaFromUrl` parsea JSON array y `fetch` falla limpio — [integration]: `fail()` 1 vez en fallo de red.

### `src/previews/_kit/ISComponentPreview.ts` (79 LOC)
**API**: clase con `constructor`, getter `signal`, `on()`, `whenDefined`, `mount`, `unmount`. **Tests existentes**: cobertura parcial estática en `ux-gallery-invariants.test.ts` línea 33-37 (regex sobre guard `target null`); sin test dinámico.

**11 propuestas**:
1. **2.1** Constructor rechaza `definition` sin `tag` — [edge-case]: lanza `Error('ISComponentPreview: falta definition.tag')`.
2. **2.2** Constructor rechaza `sections` ausente o no-array — [edge-case]: mensaje incluye tag.
3. **2.3** `definition` queda profundamente congelada — [edge-case]: mutar `tag`/`sections.push`/`blocks.push`/`code` falla (TypeError).
4. **2.4** `signal` se inicializa perezosamente y es estable — [edge-case]: lazy init 1 vez; misma referencia.
5. **2.5** `unmount()` aborta el signal y reinicia el siguiente ciclo — [memory]: listener NO se invoca tras abort; re-mount genera signal fresco.
6. **2.6** Doble `unmount` es idempotente — [edge-case]: `AbortController.prototype.abort` se llama 1 vez.
7. **2.7** `mount` que lanza excepción NO aborta el signal — [edge-case]: promesa rechaza; `signal.aborted === false`.
8. **2.8** `on(null)` y `on(objetoSinAddEventListener)` son no-ops seguros — [edge-case]: complemento del guard estático.
9. **2.9** `on` propaga `options` del usuario y combina con `signal` — [integration]: `addEventListener` recibe `{capture, once, passive, signal}`.
10. **2.10** `whenDefined` es proxy directo a `customElements.whenDefined` — [integration]: resuelve con `CustomElementConstructor` exacta.
11. **2.11** `mount`/`unmount` por defecto son no-ops — [edge-case]: devuelven `undefined` sin lanzar.

### `src/utils/home-cdn.ts` (412 LOC)
**API**: `init(raiz?)`, default `init`, `writeText` y `downloadHtml` internos. **Sin Worker/SQL reales**. **Tests existentes**: none found.

**15 propuestas**:
1. **3.1** `init` rellena snippets en `<is-code>` o `<pre>` fallback — [integration]: ambos reciben `value` + `dataset.cmSource`; `paint` se llama 2 veces.
2. **3.2** `init` tolera DOM sin `#cdnJsCss` o `#cdnBundle` — [edge-case]: no lanza; `paint(null)` y `Promise.all` no rompe.
3. **3.3** `init` idempotente sobre `cablearCopiaYDescarga` — [edge-case]: flag `copiaCableada` previene duplicación.
4. **3.4** `buildJsCssSnippet` y `buildBundleSnippet` NO contienen literales `</script>` — [xss]: invariante crítica (módulo vive dentro de `<script type="module">` del index.html).
5. **3.5** `cdnJs(name)` resuelve categoría desde `manifest`, fallback `'helpers'` — [integration]: nombre desconocido → `…/cdn/helpers/<name>.min.js`.
6. **3.6** `downloadHtml` crea Blob, anchor con `download`, revoca object URL — [integration]: Blob `type:'text/html;charset=utf-8'`; revoke tras 1000 ms.
7. **3.7** `downloadHtml` con `variant` inválido → nombre `'demo'` — [edge-case]: `downloadHtml('patata')` → `a.download === 'is-webcomponents-demo.html'`.
8. **3.8** `writeText` usa `navigator.clipboard.writeText` cuando disponible — [browser-api]: no se crea `<textarea>`.
9. **3.9** `writeText` fallback con `<textarea>` + `execCommand('copy')` — [browser-api]: textarea readonly invisible; select; execCommand; remove.
10. **3.10** Click delegado en `.home-cdn__copy[data-copy]` llama `writeText` — [event]: `aria-pressed="true"`; icono `mdi:check`; reversa 1500 ms.
11. **3.11** Click delegado con `writeText` que rechaza → estado "Error" — [event]: `aria-pressed` NO se setea; `lastChild.textContent === ' Error'`.
12. **3.12** Tabs: `aria-pressed` y `hidden` se actualizan — [event]: clic en `tab[data-tab="bundle"]` → `tabs[1].aria-pressed="true"`.
13. **3.13** Race: dos `init` concurrentes no duplican `cablearCopiaYDescarga` — [edge-case]: flag evalúa sincrónicamente.
14. **3.14** `paint` rechazada NO rompe el `init` — [edge-case]: `Promise.all(…).catch(console.error)` absorbe.
15. **3.15** `buildDemoHtml('bundle')` vs `buildDemoHtml('demo')` — [integration]: bundle → `<script type="importmap">` con `"@is-webcomponents/"`; demo → N≥40 `<script type="module">`.

---

## Resumen cuantitativo

| Testable | LOC | Propuestas | Categorías cubiertas | Gaps seguridad/a11y |
|---|---|---|---|---|
| `block-diagram.css` | 174 | 12 | UI/UX, reduced-motion, theming, integration, edge-case, performance, visual, a11y | 1 |
| `class-diagram.css` | 100 | 11 | UI/UX, reduced-motion, integration, visual, edge-case | 0 |
| `component-diagram.css` | 80 | 11 | theming, reduced-motion, CSS transitions, visual, edge-case | 0 |
| `diagram-lightbox.css` | 131 | 13 | UI/UX, reduced-motion, theming, a11y, edge-case, visual | 1 |
| `er-archify.ts` | 481 | 29 | integration, reduced-motion, edge-case, visual, performance | 0 |
| `er-diagram.css` | 99 | 12 | theming, UI/UX, reduced-motion, integration, visual, edge-case | 0 |
| `flowchart.css` | 138 | 13 | UI/UX, reduced-motion, edge-case, visual, performance | 0 |
| `gantt.css` | 98 | 11 | UI/UX, reduced-motion, integration, visual, edge-case | 0 |
| `journey-map.css` | 98 | 11 | UI/UX, reduced-motion, integration, visual, edge-case | 0 |
| `lightbox.css` | 209 | 17 | css-part, theming, CSS transitions, UI/UX, a11y, integration, performance, visual, edge-case | 0 |
| `mindmap.css` | 101 | 12 | UI/UX, reduced-motion, visual, integration, edge-case | 0 |
| `org-chart.css` | 125 | 16 | UI/UX, CSS transitions, reduced-motion, performance, theming, visual, integration, edge-case | 0 |
| `quadrant-chart.css` | 98 | 13 | CSS transitions, reduced-motion, performance, visual, UI/UX, integration, edge-case | 0 |
| `sankey-diagram.css` | 100 | 12 | UI/UX, reduced-motion, integration, visual, edge-case | 0 |
| `sequence-diagram.css` | 124 | 14 | UI/UX, reduced-motion, integration, visual, edge-case | 0 |
| `state-diagram.css` | 96 | 11 | UI/UX, reduced-motion, integration, visual, edge-case | 0 |
| `swimlane-diagram.css` | 100 | 12 | UI/UX, reduced-motion, integration, visual, edge-case | 0 |
| `timeline.css` | 98 | 12 | UI/UX, reduced-motion, integration, visual, edge-case | 0 |
| `use-case-diagram.css` | 100 | 11 | UI/UX, reduced-motion, integration, visual, edge-case | 0 |
| `venn-diagram.css` | 96 | 12 | UI/UX, reduced-motion, integration, visual, edge-case | 0 |
| `button-group.preview.controller.ts` | ≈493 | 12 | UI/UX, events, integration, a11y, edge-case | 0 |
| `button-group.ts` | ≈299 | 15 | keyboard, events, UI/UX, aria, edge-case, a11y, integration | 0 |
| `button.preview.ts` | ≈192 | 14 | UI/UX, security, integration, events, edge-case | 1 |
| `button.ts` | ≈491 | 30 | form, focus, UI/UX, events, a11y, integration, edge-case, lifecycle, security | 0 |
| `check-icon-button.ts` | ≈160 | 17 | aria, a11y, keyboard, UI/UX, edge-case, events, focus, lifecycle, integration | 0 |
| `context-menu.ts` | ≈219 | 21 | integration, edge-case, events, UI/UX, a11y, security, lifecycle | 0 |
| `copy-button.ts` | ≈376 | 20 | clipboard, UI/UX, edge-case, a11y, events, security, lifecycle, integration | 1 |
| `dropdown-item.ts` | ≈259 | 19 | events, aria, keyboard, integration, UI/UX, lifecycle, focus | 0 |
| `dropdown.ts` | ≈288 | 22 | UI/UX, events, aria, keyboard, integration, lifecycle, edge-case | 0 |
| `fab.ts` | ≈161 | 17 | a11y, UI/UX, integration, events, edge-case | 0 |
| `share-button.ts` | ≈68 | 13 | clipboard, events, edge-case, lifecycle, a11y, integration | 0 |
| `speed-dial.ts` | ≈625 | 30 | events, aria, a11y, UI/UX, edge-case, integration, lifecycle | 0 |
| `avatar.ts` | 146 | 12 | a11y, edge-case, ui/ux, xss, integration | 0 |
| `barcode-scanner.ts` | 115 | 13 | browser-api, a11y, edge-case, integration, performance | 0 |
| `barcode.ts` | 199 | 14 | edge-case, ui/ux, performance, integration, xss | 0 |
| `icon-explorer.preview.ts` | 614 | 15 | integration, edge-case, ui/ux, performance, xss | 0 |
| `icon.ts` | 245 | 15 | browser-api, integration, a11y, edge-case, performance, reduced-motion | 0 |
| `image-editor.ts` | 330 | 15 | browser-api, edge-case, integration, performance, xss, reduced-motion | 0 |
| `media-recorder.ts` | 137 | 14 | browser-api, a11y, integration, edge-case, reduced-motion | 0 |
| `qrcode.ts` | 148 | 14 | browser-api, integration, edge-case, performance, xss, reduced-motion | 0 |
| `speech.ts` | 145 | 14 | browser-api, a11y, integration, edge-case, xss, reduced-motion | 0 |
| `theme-img.ts` | 154 | 13 | integration, edge-case, a11y, performance, ui/ux, xss, reduced-motion | 0 |
| `video-playlist.ts` | 780 | 27 | integration, keyboard, ui/ux, edge-case, focus, aria, xss, browser-api | 0 |
| `video.ts` | 588 | 32 | keyboard, browser-api, ui/ux, edge-case, integration, a11y, reduced-motion | 0 |
| `chart.ts` | 792 | 12 | sql-injection (negativo), xss, performance, edge-case, a11y, integration | 0 |
| `sparkline.ts` | 156 | 10 | edge-case, event-gap, determinismo | 1 |
| `treemap.ts` | 317 | 11 | xss (markdown), edge-case, a11y | 1 |
| `heatmap.ts` | 278 | 11 | edge-case, keyboard navigation, data-validation | 0 |
| `maps.ts` | 260 | 12 | **xss (CRÍTICOS en attribution/tileUrl)**, edge-case geográfico, keyboard | 2 |
| `er-editor.html` | 85 | 11 | UI/UX, keyboard, xss, integration, reduced-motion | 0 |
| `server-datasource.ts` | 453 | 12 | sql-injection ×2, api-contract ×3, edge-case ×3, integration ×4 | 0 |
| `ISComponentPreview.ts` | 79 | 11 | edge-case ×7, memory ×1, integration ×2 | 0 |
| `home-cdn.ts` | 412 | 15 | integration ×4, browser-api ×2, event ×3, xss ×1, edge-case ×4, performance ×1 | 1 |
| **TOTAL** | ≈9500+ | **800+** | **100%** | **9+** |

---

## Gaps transversales recurrentes

### Diagrams (CSS-only, 20 archivos)
1. **G1 — `:host([open-on-click]) .wrap { cursor: zoom-in }`** (15 archivos): ningún test verifica que el cursor aparece SOLO con el atributo.
2. **G2 — `:host([hidden]) { display: none }`** (16 archivos): toggle sin cobertura.
3. **G3 — `.wrap[data-empty]::after` con copy por-diagrama** (16 archivos): ningún test verifica la localización correcta.
4. **G4 — `.wrap.is-viewer` quita chrome** (16 archivos): contrato con `is-lightbox` sin verificar.
5. **G5 — `:host([color="viewer"])` llena 100%/100%** (16 archivos): sin cobertura.
6. **G6 — `prefers-reduced-motion: reduce`** (8 archivos explícitos + herencia): solo flowchart y er-archify tienen override explícito.
7. **G7 — `:host([animation~="flow"])` opt-in** (flowchart.css, único): parametrizar selector `~=`.
8. **G8 — `box-sizing: border-box`** (20 archivos): propagación sin verificar.
9. **G9 — `font-family: var(--is-sans, …)` con stack fallback** (19 archivos): fallback chain sin test.
10. **G10 — `.wrap[data-theme="light"]` / `[data-theme="dark"]`** (3 archivos: block, component, er): tokens sin verificar.
11. **G11 — `.tk-inline-icon { width: 1em; vertical-align: -0.14em }`** (5 archivos): sequence usa 0.95em (excepción).
12. **G12 — `transition: opacity 0.18s ease`** (15+ archivos): timings exactos sin parametrizar.
13. **G13 — `is-active` con `filter: brightness(...)`** (6 archivos): valores 1.12/1.14/1.15/1.16/1.18 sin verificar.
14. **G14 — Animaciones NO-GPU** (org-chart `d`, quadrant-chart `r`): presupuesto de paint no definido.
15. **G15 — `getEmbeddedCss(traceEnabled)` en er-archify.ts**: CSS embebido sin parser.
16. **G16 — CSS parts en lightbox.css**: API de personalización sin tests.
17. **G17 — `:host([variant="solid"])`** (lightbox): variante sin test.
18. **G18 — `:host([open]) { display: contents }`** (lightbox): top-layer sin verificar.
19. **G19 — `:hover`/`:focus-visible` en `.lb-btn`**: hover/focus críticos para a11y.
20. **G20 — Regresión visual**: 0 snapshots en repo.

### Actions (12 archivos)
1. **`Escape` cierra todos los popups pero solo `dropdown` restaura foco al trigger** — gap importante; `context-menu` y `speed-dial` no.
2. **`aria-expanded` solo sincronizado donde hay trigger persistido**.
3. **Disabled-while-loading solo en `button.ts`** — los demás solo manejan `disabled` por atributo, no solapan con `busy`. Considerar uniformidad con `aria-busy`.
4. **`is-change` emission rules inconsistentes**: `button-group` (only-on-change), `check-icon-button` (always), `speed-dial` (no-op-if-equal).
5. **`popup-dismiss` cycle** compartido en `dropdown`/`context-menu`/`speed-dial` — falta cobertura unitaria del módulo.
6. **Solo `dropdown` enfoca el primer item tras abrir** (`rAF`); `context-menu`/`speed-dial` no.
7. **Roles ARIA**: solo `button-group` usa `role="group"`; `dropdown` `role="menu"`; `context-menu` sin role.
8. **`copy-button` accesible solo vía su `<is-button>` interno** — el wrapper no tiene `tabindex`.
9. **`speed-dial.action` con `href` no debería salir del `arrownav`**.
10. **Reduced motion no expuesto en `copy-button`/`speed-dial`**.
11. **RTL**: `dropdown-item` submenú hardcodeado `right-start` (no mirror).
12. **Custom states vs `data-state-*` fallback en `button.ts`** — branch documentado.
13. **`hasSubmenu` recursion profunda en `dropdown-item.closeSubmenu()`**.
14. **Seguridad en `copy-button.from`**: permite acceso a propiedades JS arbitrarias — documentar como feature.
15. **`#syncTag` re-bind de listeners**: reforzar con runtime test (estático ya cubierto en `button-events.test.ts`).
16. **JSON preview `is-preview/v1`** solo verificado para `button.ts`.
17. **`tabindex` capture en shadow sin `delegatesFocus`**.
18. **Stagger `--i` en `speed-dial`**: stale values tras `clearRadial`/`clearPolar`.
19. **`is-after-show`/`is-after-hide` disparados tras primer `rAF`** (no esperan `transitionend`).
20. **`variant` observado pero NO se reenvía al interno** en `check-icon-button.ts` — gap detectado.

### Charts + data-viz (5 archivos)
1. **`paty-iss` es un tag inexistente** — no aparece en `src/components/data-viz/maps.ts`, ni en todo `src/`. Tags reales: `is-maps` y `is-map-marker`. Si proviene de un prompt alucinado, ignorar y reemplazar.
2. **`sql-query` no existe en `<is-chart>`** — el componente solo consume slot JSON estático. La propuesta T-CHART-01 verifica el rechazo de atributos no declarados.
3. **Cobertura de eventos escasa** — solo `<is-sparkline>` no emite eventos. **Propongo gap transversal**: añadir `is-data-change` y `is-hover` en sparkline.
4. **XSS real en `maps.ts` (CRÍTICOS)**:
   - `cfg.attribution` vía `innerHTML` (línea 127) → reemplazar por `textContent` o sanitizar.
   - `cfg.tileUrl` como `iframe.src` (línea 122) → validación con `new URL()` y whitelist de protocolos.
   - `inlineMdWeb(n.label)` en `treemap.ts` (línea 304) → auditar `_shared/tk-inline-md.ts`.
5. **Keyboard navigation es el gap de a11y más extendido** — ninguno de los 5 webcomponents tiene `tabindex` ni `role` aplicado a marks/cells/teselas/markers.
6. **Determinismo de IDs** — `sparkline.ts` usa `Math.random().toString(36).slice(2)` para IDs de gradient (línea 123). Proponer `crypto.randomUUID()` o contador.
7. **Tests existentes que ya cubren huecos comunes** — `src/utils/health/exhaustive/charts/{chart,sparkline,treemap,heatmap,…}.test.ts` (snapshot de `observedAttributes`/`styleAttrs`/serialización JSON). `demos/diagramas/ER/_testing/er-editor.test.mjs` (smoke + drag + undo + export determinista + a11y básico). Las propuestas evitan duplicar.
8. **Performance: solo happy-path** — propuestas T-CHART-07/09, T-SPARK-08, T-HM-11, T-MAP-06 proponen budgets concretos (10k puntos, 60 fps, 1000 markers, 10k cells).
9. **Reduced-motion inconsistente** — `chart.ts` y `er-editor` lo honran; `heatmap.ts`, `treemap.ts`, `maps.ts`, `sparkline.ts` no verifican.
10. **`is-open-viewer` cancelable** en chart.ts y treemap.ts — tests deben verificar comportamiento con/sin `preventDefault()`.
11. **No hay tests propuestos para `updateComplete()`** — `await element.updateComplete()` resuelve solo tras el último cambio coalescido.
12. **Sin tests de Webkit/Safari/Firefox** (la suite parece Chromium-only).

### Datagrid + previews + utils (3 archivos)
1. **Etiquetado impreciso**: las etiquetas `worker,event,http-client` en `server-datasource.ts` y `sql-query,worker` en `home-cdn.ts` no corresponden al contenido real.
2. **Cero cobertura previa** en `server-datasource.ts` y `home-cdn.ts`.
3. **`ISComponentPreview` sólo tiene cobertura estática**.
4. **`createServerSideDatasource` no tiene contrato de error tipado** — mapear `error.status` a `is-datasource-error`.
5. **`home-cdn.init` no expone `jsCssSnippet`/`bundleSnippet`** como export testeable.
6. **`paint` se mockea pero `init` no la inyecta** (l. 9).
7. **No hay fixture compartido de `manifest`**.
8. **`ux-gallery-invariants.test.ts` cubre `ISComponentPreview.on` con target null** solo por regex.
9. **`createFakeListaFromUrl` no valida `res.ok`**.
10. **`home-cdn` se ejecuta dentro de un `<script type="module">` del index.html** — las propuestas 3.4 (sin `</script>` literal) son invaluables.

### Transversal a todo el grupo (g54)
1. **Etiquetas de auditoría imprecisas en 5+ archivos**: `paty-iss`, `sql-query` en chart.ts, `worker`/`http-client` en server-datasource.ts y home-cdn.ts — alucinaciones del catalogador o del LLM upstream.
2. **Determinismo de IDs no es una preocupación transversal pero aparece en sparkline.ts** — `Math.random()` vs `crypto.randomUUID()`.
3. **`prefers-reduced-motion` honored inconsistently** — gap a cerrar en todos los webcomponents con animación.
4. **Keyboard navigation es transversal** — gap de a11y presente en webcomponents con `mark`/`cell`/`tesela`/`marker`.
5. **CSS Parts/CSS vars expuestos solo en lightbox.css** — oportunidades de theming infrautilizadas.
6. **Snapshots/regresión visual = 0 en el repo** — transversal a toda la UI.
7. **`popup-dismiss` module compartido** sin test unitario propio.

---

## Categorías obligatorias

### ✅ Cubiertas para UI/webcomponents
- **UI/UX** ✅ — interacciones, estados visuales (todas las testables).
- **a11y** ✅ — ARIA, role, keyboard, screen reader (button, dropdown, lightbox, charts).
- **Teclado** ✅ — Tab/Enter/Space/Escape/Arrow/Home/End (button-group, dropdown, speed-dial, video, lightbox).
- **Reduced motion** ✅ — `prefers-reduced-motion: reduce`, animaciones (todos los diagrams con animación + lightbox).
- **Browser APIs** ✅ — clipboard, fullscreen, getUserMedia (copy-button, share-button, video, image-editor, media-recorder).
- **Storage** ✅ — localStorage, IndexedDB (video.ts para DB, home-cdn.ts para snippets).

### ✅ Cubiertas para funciones puras (server-datasource.ts, er-archify.ts, ISComponentPreview.ts)
- **BR** (Business rules) ✅ — `sqlLiteral` contract; `pickSidesArchify`; `coerce direction 'IZQ-DER'` → 'LR'.
- **SM** (State machines) ✅ — ISComponentPreview mount/unmount cycle.
- **VL** (Validation) ✅ — input válido/inválido, boundaries (size [12,8], viewbox "a,b,c").
- **TR** (Transformations) ✅ — serializeErPayload stable; buildJSONFiltro.
- **PF** (Pure functions) ✅ — getEmbeddedCss determinista.
- **ER** (Error handling) ✅ — JSON inválido, payload `null`, fetch fails.
- **PF*** (Performance) ✅ — 10k puntos, 60 fps, gradient ID collisions.

### ✅ Cubiertas para seguridad
- **XSS** ✅ — labels/datasets/attribution/tileUrl/entity.name (CRÍTICOS en maps.ts).
- **CSRF** — N/A (sin endpoints mutadores en estos 53 archivos).
- **Auth bypass** — N/A en este lote.
- **Secret redaction** — surface en `copy-button.from=#id.prop` (documentada como feature).

---

## Discrepancias de nomenclatura

1. **`paty-iss` NO EXISTE en el proyecto** — el tag real es `is-maps` y `is-map-marker`. Si la etiqueta proviene del catalogador, está alucinada. Las propuestas de maps.ts usan los tags correctos.
2. **`sql-query` NO EXISTE en `chart.ts`** — el componente consume slot JSON estático (`<script type="application/json">`). La propuesta T-CHART-01 verifica el rechazo del atributo no declarado como guard.
3. **`worker` y `http-client` NO APLICAN a `server-datasource.ts`** — el archivo es 100% lógica pura (helpers SQL + adaptador `getRows` + fake server en memoria con `fetch` opcional).
4. **`worker` y `sql-query` NO APLICAN a `home-cdn.ts`** — el archivo solo hace render DOM y usa `navigator.clipboard` + `<textarea>` + `Blob` + `URL.createObjectURL`. Etiquetas inexactas del catalogador.
5. **Cobertura existente**: `src/utils/testing/domain/button-events.test.ts`, `context-menu-scroll.test.ts`, `domain/icon-{explorer,currentcolor,render,prefetch}.test.ts`, `health/exhaustive/{actions,charts}/...test.ts`, `meta/{llm-contract,ux-gallery-invariants}.test.ts`, `domain/{preview-controller,css-imports,soft-format,...}.test.ts` ya cubren parcialmente. Las propuestas de este informe evitan duplicar.

---

## Notas para el runner de tests

### Frontend (WebComponents + HTML demo)
- **Runtime**: Playwright + Stagehand (`@browserbasehq/stagehand`) o Vitest + jsdom para unit tests de Shadow DOM.
- **Test runner**: `node:test` + `node:assert/strict` (estilo `src/utils/testing/*`); alternativa `vitest`.
- **Helpers de prueba**: `src/utils/testing/e2e/lib/harness.ts` (`arrancar`, `abrirGaleria`, `esperarMs`, `evidencia`, `crearTestE2E`).
- **URLs**:
  - Diagramas/acciones/media/charts/data-viz: `http://127.0.0.1:3080/_shell.html?tag=is-{name}` (gallery preview).
  - er-editor: `http://127.0.0.1:3080/demos/diagramas/ER/er-editor.html`.
- **Mocking**: `matchMedia` para `prefers-reduced-motion`, `navigator.share`, `navigator.clipboard`, `getUserMedia`, `ResizeObserver`, `IntersectionObserver`, `requestIdleCallback`, `document.fonts.ready`.
- **Para CSS tests**: JSDOM + `getComputedStyle()` con estilo computado resuelto (apoyarse en `jsdom` versión >= 22 que implementa CSSOM).
- **Para SVG/SMIL**: jsdom no renderiza SVG; usar snapshot del árbol + `outerHTML`.

### Backend-style (puro: server-datasource.ts, ISComponentPreview.ts, home-cdn.ts)
- **Runtime**: `node --test src/components/data/datagrid-core/server-datasource.test.ts`.
- **Style**: `import test from 'node:test'; import assert from 'node:assert/strict';` como en `src/utils/health/exhaustive/...test.ts`.
- **Para ISComponentPreview**: instanciar la clase con mocks (`AbortController`, `customElements.whenDefined`).
- **Para home-cdn.ts**: stubear `../components/_shared/highlight-code.js` (side-effect import).

### Selección de mocks recurrentes
```ts
// prefers-reduced-motion
window.matchMedia = (q) => q.includes('reduce')
  ? { matches: true, addEventListener() {}, removeEventListener() {}, addListener() {}, removeListener() {} }
  : { matches: false, /* … */ };

// clipboard
Object.defineProperty(navigator, 'clipboard', {
  value: { writeText: vi.fn().mockResolvedValue(undefined) }, configurable: true,
});
```

### Tiempo invertido
- Orquestación: ~2 min.
- 5 subagentes en paralelo (cada uno ~3-8 min).
- Agregación: ~5 min.
- **Total**: ~10-13 min. Entregable: este archivo + secciones individuales enviadas al parent.

---

> **Documento listo para gate F0.4.** Calidad > cantidad. Las propuestas de mayor impacto, ya priorizadas:
>
> 1. **`lightbox.css`** (CSS parts + vars + `variant="solid"` + `display: contents` en `[open]`) — prioridad #1 transversal.
> 2. **`flowchart.css`** (único con `@keyframes` propio + `@supports (color-mix oklab)` + `animation~="flow"` opt-in).
> 3. **`er-archify.ts`** (`getEmbeddedCss` + `normalizeErPayload` strict + `serializeErPayload` stable).
> 4. **`maps.ts` XSS CRÍTICOS** (`cfg.attribution` innerHTML, `cfg.tileUrl` como iframe.src) — vulnerabilidad real.
> 5. **`treemap.ts` `inlineMdWeb`** — auditar `_shared/tk-inline-md.ts`.
> 6. **`org-chart.css` `transition: d`** + **`quadrant-chart.css` `transition: r`** — performance smoke.
> 7. **`server-datasource.ts` `sqlLiteral`** + **`convertFilterModelToSQL`** — cobertura de la única función con riesgo SQL real.
> 8. **`ISComponentPreview.signal`** + `unmount()` abort — memory leak prevention.
> 9. **`home-cdn` `buildJsCssSnippet`/`buildBundleSnippet` sin literales `</script>`** — invariante crítica (módulo vive dentro de un `<script type="module">`).
> 10. **Determinismo de `sparkline` gradient IDs** — `Math.random()` → `crypto.randomUUID()`.
