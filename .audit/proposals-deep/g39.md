# Auditoría F0.3 — diagrams+_shared+navigation+data+scripts+helpers+styles+utils (g39)

## Contexto

- **Testables del grupo** (52 items):
  - `src/components/diagrams/block-diagram.ts` (webcomponent,event)
  - `src/components/diagrams/class-diagram.ts` (webcomponent,event)
  - `src/components/diagrams/component-diagram.ts` (webcomponent,event)
  - `src/components/diagrams/diagram-lightbox.ts` (webcomponent,sql-query,worker,event)
  - `src/components/diagrams/er-diagram.ts` (webcomponent,event,animation)
  - `src/components/diagrams/er-editor.ts` (webcomponent,sql-query,worker,event,browser-api,animation)
  - `src/components/diagrams/flowchart.ts` (webcomponent,auth,event)
  - `src/components/diagrams/gantt.ts` (webcomponent,event)
  - `src/components/diagrams/journey-map.ts` (webcomponent,event)
  - `src/components/diagrams/lightbox.ts` (webcomponent,worker,event)
  - `src/components/diagrams/mindmap.ts` (webcomponent,event)
  - `src/components/diagrams/org-chart.ts` (webcomponent,sql-query,migration,worker,event,animation)
  - `src/components/diagrams/quadrant-chart.ts` (webcomponent,event)
  - `src/components/diagrams/sankey-diagram.ts` (webcomponent,event)
  - `src/components/diagrams/sequence-diagram.ts` (webcomponent,event)
  - `src/components/diagrams/state-diagram.ts` (webcomponent,event)
  - `src/components/diagrams/swimlane-diagram.ts` (webcomponent,event)
  - `src/components/diagrams/timeline.ts` (webcomponent,event)
  - `src/components/diagrams/use-case-diagram.ts` (webcomponent,event)
  - `src/components/diagrams/venn-diagram.ts` (webcomponent,event)
  - `src/components/_shared/date-field-element.ts` (webcomponent,sql-query,event)
  - `src/components/_shared/diagram-element-base.ts` (webcomponent,event)
  - `src/components/_shared/dom-utils.ts` (webcomponent,sql-query,browser-api)
  - `src/components/_shared/form-control-mixin.ts` (webcomponent)
  - `src/components/_shared/highlight-code.ts` (webcomponent)
  - `src/components/_shared/modal-base.ts` (webcomponent,migration,event)
  - `src/components/_shared/picker-element.ts` (webcomponent,event)
  - `src/components/_shared/position.ts` (webcomponent)
  - `src/components/_shared/theme-scope.ts` (webcomponent,event)
  - `src/components/navigation/breadcrumb-item.css` (animation)
  - `src/components/navigation/carousel.css` (animation,css-part)
  - `src/components/navigation/scroller.css` (animation,css-part)
  - `src/components/navigation/stepper.css` (animation,css-part)
  - `src/components/navigation/tab-group.css` (animation)
  - `src/components/navigation/tree.css` (animation,css-part)
  - `src/components/data/data-grid.css` (animation)
  - `src/components/data/gauge.css` (animation)
  - `src/components/data/kanban.css` (animation,css-part)
  - `src/components/data/stat.css` (animation)
  - `src/components/data/transfer.css` (animation)
  - `scripts/audit-is-code.ts` (event)
  - `scripts/cdn-panel.js` (event)
  - `scripts/docs-chrome.js` (event)
  - `scripts/highlight-pre.js` (event)
  - `src/components/helpers/observer.preview.ts` (event)
  - `src/components/helpers/relative-time.preview.ts` (event,i18n)
  - `src/components/helpers/resize-observer.preview.ts` (event)
  - `src/styles/presentation.css` (animation,css-part)
  - `src/styles/shell.css` (animation)
  - `scripts/inject-equiv-flows.ts` (auth)
  - `scripts/preview-boot.js` (storage)
  - `src/utils/testing/e2e/lib/server.ts` (database)
- **Lenguaje(s)**: TypeScript (.ts) + CSS (.css) + JavaScript (.js/.mjs).
- **Tests existentes**: parciales — diagrams tienen demos + tests en `demos/diagramas/<X>/_testing/`. CSS files sin tests directos (visual regression). `scripts/*` sin tests. `server.ts` (E2E) sin tests específicos.
- **Riesgos identificados** (pre-auditoría rápida):
  - Diagramas: `transition: d` y `transition: r` en CSS (org-chart, quadrant-chart) — animaciones no-GPU que bloquean main thread.
  - `diagram-lightbox.ts` consume SQL queries con `whenDefined` ausente — race condition.
  - `er-editor.ts` y `org-chart.ts` son webcomponents con migración + worker — sin tests de cancelación de operaciones largas.
  - `_shared/dom-utils.ts` con sql-query y browser-api sin tests de sanitización de XSS.
  - `_shared/modal-base.ts` con focus trap y `aria-modal` — sin tests de restauración de foco al cerrar.
  - 13 CSS sin tests de `prefers-reduced-motion` (los transitions deberían desactivarse).
  - `picker-element.ts` sin tests de teclado (Tab/Arrow/Escape en dropdown).
  - `_shared/theme-scope.ts` emite eventos sin tests de cleanup en `disconnectedCallback`.
  - `previews/*` (observer, relative-time, resize-observer): sin tests de `unmount()` (memory leak de listeners).
  - `server.ts` (Node E2E) sin tests de headers de seguridad (CSP, X-Frame-Options).
  - `scripts/audit-is-code.ts`, `cdn-panel.js`, `docs-chrome.js`, `highlight-pre.js`: eventos sin tests de cleanup.
  - `scripts/inject-equiv-flows.ts` con `auth` sin tests de tokens expirados.
  - `scripts/preview-boot.js` con `storage` sin tests de quota exceeded.
  - `relative-time.preview.ts` con `i18n` sin tests de localización.
- **Tipo de proyecto**: library (webcomponents) + scripts internos + Node E2E server + 13 CSS files.

---

## Propuestas por testable

### `block-diagram.ts` (src/components/diagrams/block-diagram.ts — 480 LOC)

**Tests existentes** (resumen): NINGUNO. Demo + test en `demos/diagramas/block-diagram/` pendiente.

**Propuestas nuevas** (12):

1. **Renderiza bloques con jerarquía padre-hijo** — [visual]
   - **Setup**: spec con 5 bloques anidados (root → 2 children → 2 grandchildren).
   - **Acción**: renderizar `<is-block-diagram>` con `spec=spec`.
   - **Assertion**: 5 elementos `<rect class="is-block">` en DOM; layout en árbol con padres sobre hijos (y(root) < y(child) < y(grandchild)).

2. **Edges con arrows dirección jerárquica** — [visual]
   - **Setup**: spec con 3 bloques conectados por 2 edges A→B y B→C.
   - **Acción**: renderizar.
   - **Assertion**: 2 elementos `<line class="is-edge">` con marker-end="url(#arrow)" en orden topológico.

3. **Custom event `is-block-select` al click** — [event]
   - **Setup**: spec con 2 bloques.
   - **Acción**: dispatch click sobre el primer bloque; capturar eventos en `addEventListener('is-block-select', ...)`.
   - **Assertion**: evento fired con `detail: { id: string, label: string }`; segundo click sobre el mismo bloque NO emite evento nuevo.

4. **Hover highlight: bloque + edges adyacentes** — [visual, focus]
   - **Setup**: spec con 3 bloques A, B, C conectados A→B, B→C.
   - **Acción**: dispatch mouseenter sobre B.
   - **Assertion**: bloque B tiene clase `is-hover`; edges A→B y B→C tienen clase `is-hover-edge`. mouseleave quita las clases.

5. **Teclado: Tab navega entre bloques** — [keyboard, a11y]
   - **Setup**: spec con 5 bloques.
   - **Acción**: focus root, presionar Tab 4 veces.
   - **Assertion**: foco se mueve secuencialmente por cada bloque en orden DOM. Escape quita el foco del bloque actual y lo devuelve al container.

6. **prefers-reduced-motion: desactiva transiciones de bloques** — [reduced-motion]
   - **Setup**: spec con 3 bloques. Playwright context con `reducedMotion: 'reduce'`.
   - **Acción**: dispatch hover sobre bloque; leer `getComputedStyle(el).transition`.
   - **Assertion**: `transition-duration` es `0s` o `none` cuando `prefers-reduced-motion: reduce`.

7. **CSS part `--is-block-bg` customizable** — [css-part]
   - **Setup**: spec con 1 bloque.
   - **Acción**: en shadow DOM, aplicar `::part(is-block-bg) { background: red; }`.
   - **Assertion**: `getComputedStyle(rect).backgroundColor` es `rgb(255, 0, 0)`.

8. **Edge label editable (doble-click)** — [event, focus]
   - **Setup**: spec con 1 edge con label "X".
   - **Acción**: dispatch dblclick sobre el label; cambiar texto; dispatch blur.
   - **Assertion**: `spec.edges[0].label` se actualiza con el nuevo texto; `is-edge-label-edit` event fired.

9. **Tamaños custom: width/height en spec** — [visual]
   - **Setup**: spec con 1 bloque `{ id: 'A', width: 200, height: 100 }`.
   - **Acción**: renderizar.
   - **Assertion**: `<rect width="200" height="100">` en SVG.

10. **Theme: respeta `data-theme="dark"`** — [visual, browser-api]
    - **Setup**: spec con 1 bloque. Body con `data-theme="dark"`.
    - **Acción**: renderizar; leer colores.
    - **Assertion**: stroke/fill usan valores del CSS variable `--is-diagram-stroke-dark` o equivalente.

11. **Dispose: removeChild limpia listeners** — [edge-case, browser-api]
    - **Setup**: spec con 3 bloques.
    - **Acción**: `parent.removeChild(diagram)`; dispatch click en window.
    - **Assertion**: ningún error thrown; ningún listener residual en `window.eventListeners`.

12. **Spec inválido: lanza error claro en consola** — [edge-case]
    - **Setup**: spec con `nodes: []` y `edges: [{ from: 'nonexistent', to: 'also-nonexistent' }]`.
    - **Acción**: renderizar.
    - **Assertion**: `console.error` con mensaje "Invalid edge: from 'nonexistent' not in nodes"; renderiza nodos válidos e ignora edges inválidos (no crash).

---

### `class-diagram.ts` (src/components/diagrams/class-diagram.ts — 460 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (12):

1. **Renderiza clases con atributos y métodos** — [visual]
   - **Setup**: spec con 1 clase `{ name: 'Foo', attributes: ['x: int'], methods: ['getX(): int'] }`.
   - **Acción**: renderizar.
   - **Assertion**: 3 secciones en el rect (header, attributes, methods); texto correcto en cada sección.

2. **Herencia: flecha con triángulo hueco** — [visual]
   - **Setup**: spec con 2 clases `Base` y `Derived extends Base`.
   - **Acción**: renderizar.
   - **Assertion**: 1 edge entre Derived y Base con marker-end de triángulo hueco (`marker-end="url(#inheritance)"`).

3. **Asociación: flecha simple con label** — [visual]
   - **Setup**: spec con 2 clases A y B, edge `{ from: A, to: B, label: 'uses', kind: 'association' }`.
   - **Acción**: renderizar.
   - **Assertion**: 1 edge con marker simple y label "uses" centrado.

4. **Visibilidad: -private, #protected, +public** — [visual]
   - **Setup**: spec con 1 clase y 3 atributos `+name: string`, `-age: int`, `#id: uuid`.
   - **Acción**: renderizar.
   - **Assertion**: cada atributo se renderiza con su prefijo (`+`, `-`, `#`) en el orden de declaración.

5. **Métodos estáticos subrayados** — [visual]
   - **Setup**: spec con 1 clase y método `static create(): Foo`.
   - **Acción**: renderizar.
   - **Assertion**: el método tiene `<u>` o `text-decoration: underline`.

6. **Click sobre clase emite `is-class-select`** — [event]
   - **Setup**: spec con 2 clases.
   - **Acción**: dispatch click sobre la primera.
   - **Assertion**: `is-class-select` event fired con `detail: { id: 'ClassA' }`.

7. **Drag para reposicionar clase** — [drag, visual]
   - **Setup**: spec con 1 clase en posición `{ x: 100, y: 100 }`.
   - **Acción**: mousedown sobre la clase, mover 50px derecha, mouseup.
   - **Assertion**: `spec.nodes[0].position` se actualiza a `{ x: 150, y: 100 }`; DOM redibuja con nueva posición.

8. **Resize handle en esquina inferior-derecha** — [drag, visual]
   - **Setup**: spec con 1 clase con tamaño inicial `{ width: 200, height: 100 }`.
   - **Acción**: drag del handle a (+30, +20).
   - **Assertion**: dimensiones se actualizan a `{ width: 230, height: 120 }`.

9. **prefers-reduced-motion: sin animación al resize** — [reduced-motion]
   - **Setup**: spec con 1 clase. Context con `reducedMotion: 'reduce'`.
   - **Acción**: dispatch drag de resize.
   - **Assertion**: sin `transition` aplicada durante el drag (cambio instantáneo).

10. **Theme oscuro: fill cambia con `data-theme="dark"`** — [visual]
    - **Setup**: spec con 1 clase.
    - **Acción**: aplicar `data-theme="dark"` al host.
    - **Assertion**: `getComputedShapeAttribute(rect, 'fill')` cambia a `--is-class-fill-dark`.

11. **CSS part `--is-class-border` customizable** — [css-part]
    - **Setup**: spec con 1 clase.
    - **Acción**: aplicar `::part(is-class-border) { stroke: purple; }`.
    - **Assertion**: stroke del rect es purple.

12. **Spec con clase abstracta: nombre en cursiva** — [visual]
    - **Setup**: spec con 1 clase `{ name: 'Animal', abstract: true }`.
    - **Acción**: renderizar.
    - **Assertion**: el texto del nombre tiene `font-style: italic`.

---

### `component-diagram.ts` (src/components/diagrams/component-diagram.ts — 440 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Renderiza componentes UML con interfaz (lollipop)** — [visual]
   - **Setup**: spec con 1 componente `{ name: 'AuthService', interfaces: ['ILogin'] }`.
   - **Acción**: renderizar.
   - **Assertion**: 1 rect principal + 1 círculo (lollipop) conectado por línea al rect.

2. **Composición: rombo relleno en target** — [visual]
   - **Setup**: spec con 2 componentes A y B, edge `{ kind: 'composition', from: A, to: B }`.
   - **Acción**: renderizar.
   - **Assertion**: 1 edge con marker-start rombo relleno en A.

3. **Agregación: rombo hueco** — [visual]
   - **Setup**: spec con edge `{ kind: 'aggregation' }`.
   - **Acción**: renderizar.
   - **Assertion**: marker-start con rombo hueco (fill="none").

4. **Custom event `is-component-select`** — [event]
   - **Setup**: spec con 2 componentes.
   - **Acción**: dispatch click sobre el primero.
   - **Assertion**: `is-component-select` fired con `detail.id`.

5. **Drag reposiciona componente** — [drag]
   - **Setup**: spec con 1 componente en `{x:0,y:0}`.
   - **Acción**: mousedown, move(+50,+30), mouseup.
   - **Assertion**: posición actualizada a `{x:50,y:30}`.

6. **Theme dark** — [visual]
   - **Setup**: spec con 1 componente, `data-theme="dark"`.
   - **Acción**: renderizar.
   - **Assertion**: stroke usa `--is-component-stroke-dark`.

7. **prefers-reduced-motion: sin animación al drag** — [reduced-motion]
   - **Setup**: Context con `reducedMotion: 'reduce'`. Spec con 1 componente.
   - **Acción**: drag.
   - **Assertion**: sin `transition` durante el drag.

8. **Spec con componente abstracto: nombre en cursiva** — [visual]
   - **Setup**: spec `{ name: 'BaseService', abstract: true }`.
   - **Acción**: renderizar.
   - **Assertion**: nombre con `font-style: italic`.

9. **CSS part customizable** — [css-part]
   - **Setup**: spec con 1 componente.
   - **Acción**: `::part(is-component-bg) { fill: orange; }`.
   - **Assertion**: rect con fill orange.

10. **Dispose cleanup** — [edge-case]
    - **Setup**: spec con 2 componentes.
    - **Acción**: `parent.removeChild(component)`.
    - **Assertion**: sin errores; sin listeners residuales.

---

### `diagram-lightbox.ts` (src/components/diagrams/diagram-lightbox.ts — 320 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (12):

1. **Abre lightbox al click en trigger** — [event]
   - **Setup**: spec con 1 imagen y atributo `trigger`.
   - **Acción**: dispatch click sobre el trigger.
   - **Assertion**: lightbox visible (`aria-hidden="false"`); imagen renderizada a tamaño completo.

2. **Cierra con Escape** — [keyboard, a11y]
   - **Setup**: lightbox abierto.
   - **Acción**: dispatch keydown Escape.
   - **Assertion**: lightbox cerrado; foco restaurado al trigger original.

3. **Focus trap dentro del lightbox** — [a11y, focus]
   - **Setup**: lightbox abierto con 3 botones.
   - **Acción**: Tab desde último botón.
   - **Assertion**: foco vuelve al primer botón. Shift+Tab desde primero va al último.

4. **Race condition: SQL query sin `whenDefined`** — [edge-case, sql-query]
   - **Setup**: spec con `data-sql-query="SELECT * FROM diagrams"` antes de que el custom element esté listo.
   - **Acción**: añadir al DOM antes de la definición de custom element.
   - **Assertion**: NO error en consola; cuando se define el CE, ejecuta la query y renderiza.

5. **Worker: cancelación de operación larga al cerrar** — [worker, edge-case]
   - **Setup**: spec con query pesada (>5s); lightbox abierto.
   - **Acción**: cerrar lightbox antes de 5s.
   - **Assertion**: worker recibe `cancel`; sin resultado aplicado al DOM tras cerrar.

6. **Click fuera del contenido cierra lightbox** — [event]
   - **Setup**: lightbox abierto.
   - **Acción**: click sobre backdrop.
   - **Assertion**: lightbox cerrado.

7. **Resize de la imagen al cambiar tamaño de ventana** — [visual, browser-api]
   - **Setup**: lightbox abierto con imagen 4K.
   - **Acción**: cambiar viewport a 800x600.
   - **Assertion**: imagen escala a `max-width: 100%`.

8. **prefers-reduced-motion: sin fade-in** — [reduced-motion]
   - **Setup**: Context con `reducedMotion: 'reduce'`.
   - **Acción**: abrir lightbox.
   - **Assertion**: `opacity` cambia instantáneamente; sin `transition`.

9. **Teclado: flechas navegan entre imágenes** — [keyboard]
   - **Setup**: gallery con 3 imágenes, índice 0 abierto.
   - **Acción**: ArrowRight.
   - **Assertion**: índice cambia a 1; imagen src actualizada.

10. **Theme oscuro** — [visual]
    - **Setup**: `data-theme="dark"` en host.
    - **Acción**: abrir lightbox.
    - **Assertion**: backdrop usa `--is-lightbox-backdrop-dark`.

11. **Dispose: removeChild limpia listeners y worker** — [edge-case, browser-api]
    - **Setup**: lightbox abierto con worker activo.
    - **Acción**: `parent.removeChild(lightbox)`.
    - **Assertion**: worker terminated; sin listeners residuales; sin memory leak.

12. **Múltiples lightboxes: solo uno abierto a la vez** — [edge-case]
    - **Setup**: 2 lightboxes en el DOM.
    - **Acción**: abrir el primero, luego abrir el segundo.
    - **Assertion**: el primero se cierra automáticamente antes de abrir el segundo.

---

### `er-diagram.ts` (src/components/diagrams/er-diagram.ts — 540 LOC)

**Tests existentes** (resumen): PARCIALES — tests en `demos/diagramas/ER/_testing/` (er-attributes, er-edge-cases, er-static, er-stagehand).

**Propuestas nuevas** (12):

1. **Renderiza entidades con atributos** — [visual]
   - **Setup**: spec con 1 entidad `{ name: 'User', attributes: ['id: int PK', 'name: string'] }`.
   - **Acción**: renderizar.
   - **Assertion**: 1 rect con header "User" + 2 filas de atributos.

2. **Cardinalidad 1:N en relaciones** — [visual]
   - **Setup**: 2 entidades User, Post con relación 1:N.
   - **Acción**: renderizar.
   - **Assertion**: edge con label "1" cerca de User y "N" cerca de Post.

3. **Primary key subrayada** — [visual]
   - **Setup**: spec con atributo PK.
   - **Acción**: renderizar.
   - **Assertion**: la fila del PK tiene `text-decoration: underline`.

4. **Click sobre entidad emite `is-entity-select`** — [event]
   - **Setup**: spec con 2 entidades.
   - **Acción**: click sobre User.
   - **Assertion**: event fired con `detail.id='User'`.

5. **Drag reposiciona entidad** — [drag]
   - **Setup**: entidad en `{x:100,y:100}`.
   - **Acción**: drag +50,+50.
   - **Assertion**: posición actualizada; edge se redibuja con nueva ruta.

6. **Animación de inserción de entidad** — [animation]
   - **Setup**: spec con 1 entidad, animación on-add.
   - **Acción**: añadir nueva entidad al spec.
   - **Assertion**: nueva entidad aparece con `scale(0)→scale(1)` o fade-in (≥200ms).

7. **prefers-reduced-motion: sin animación** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: añadir entidad.
   - **Assertion**: aparece sin `animation` o `animation-duration: 0s`.

8. **Theme dark** — [visual]
   - **Setup**: `data-theme="dark"`.
   - **Acción**: renderizar.
   - **Assertion**: stroke usa `--is-er-stroke-dark`.

9. **Spec inválido: entidad sin atributos es OK** — [edge-case]
   - **Setup**: spec `{ name: 'Foo', attributes: [] }`.
   - **Acción**: renderizar.
   - **Assertion**: entidad renderizada con header + sección de atributos vacía (no error).

10. **CSS part `--is-entity-bg`** — [css-part]
    - **Setup**: spec con 1 entidad.
    - **Acción**: `::part(is-entity-bg) { background: lightblue; }`.
    - **Assertion**: bg del rect es lightblue.

11. **Relación reflexiva: edge desde entidad a sí misma** — [visual]
    - **Setup**: spec con entidad User y edge `{from: 'User', to: 'User', label: 'manages'}`.
    - **Acción**: renderizar.
    - **Assertion**: edge en bucle curvado que sale y vuelve a la misma entidad.

12. **Dispose cleanup completo** — [edge-case]
    - **Setup**: spec con 3 entidades y 2 edges.
    - **Acción**: removeChild.
    - **Assertion**: sin listeners; sin timers activos; sin memory leaks observables.

---

### `er-editor.ts` (src/components/diagrams/er-editor.ts — 720 LOC)

**Tests existentes** (resumen): PARCIALES — tests en `demos/diagramas/ER/_testing/er-editor.test.mjs`.

**Propuestas nuevas** (12):

1. **Crear entidad nueva desde toolbar** — [event]
   - **Setup**: editor vacío.
   - **Acción**: click en botón "Add entity"; rellenar form con name="Dept".
   - **Acción**: submit.
   - **Assertion**: nueva entidad "Dept" añadida al spec; `is-entity-add` event fired.

2. **Editar atributo (doble-click)** — [event, focus]
   - **Setup**: editor con 1 entidad.
   - **Acción**: dblclick sobre atributo "id"; cambiar a "uuid"; blur.
   - **Assertion**: atributo actualizado a "uuid"; spec mutado.

3. **Eliminar entidad con Delete** — [keyboard]
   - **Setup**: entidad seleccionada.
   - **Acción**: presionar Delete.
   - **Assertion**: entidad removida del spec; DOM redibuja sin ella.

4. **Undo (Ctrl+Z)** — [keyboard]
   - **Setup**: 1 entidad recién añadida.
   - **Acción**: Ctrl+Z.
   - **Assertion**: entidad removida; stack de undo decrece.

5. **Redo (Ctrl+Shift+Z)** — [keyboard]
   - **Setup**: tras undo.
   - **Acción**: Ctrl+Shift+Z.
   - **Assertion**: entidad reaparece; stack de redo vacío tras aplicar.

6. **Worker: cancelación al cerrar antes de guardar** — [worker, edge-case]
   - **Setup**: editor con cambios sin guardar (worker en background).
   - **Acción**: cerrar editor (navigate-away o `disconnectedCallback`).
   - **Assertion**: worker recibe `cancel`; sin POST/PUT al backend con datos parciales.

7. **SQL query: race condition sin `whenDefined`** — [sql-query, edge-case]
   - **Setup**: editor con atributo `data-sql-query`.
   - **Acción**: añadir al DOM antes de la definición.
   - **Assertion**: query se ejecuta tras la definición; sin error en consola.

8. **Auth: token expirado → save falla con mensaje claro** — [auth, sql-query]
   - **Setup**: editor con cambios; simular respuesta 401 del backend.
   - **Acción**: trigger save.
   - **Assertion**: error toast "Session expired, please log in again"; cambios quedan en memoria local.

9. **Animación de edición (drag de atributo)** — [animation]
   - **Setup**: editor con entidad.
   - **Acción**: drag de atributo entre filas.
   - **Assertion**: movimiento suave (~200ms ease-out).

10. **prefers-reduced-motion: sin animación al reorder** — [reduced-motion]
    - **Setup**: Context reduced.
    - **Acción**: drag de atributo.
    - **Assertion**: movimiento instantáneo.

11. **Browser API: localStorage con autosave** — [browser-api, storage]
    - **Setup**: editor con cambios.
    - **Acción**: esperar 5s sin acción.
    - **Assertion**: spec guardado en `localStorage["er-editor:draft"]`; al recargar, se recupera.

12. **Spec con 1000 entidades: render perezoso** — [performance]
    - **Setup**: spec con 1000 entidades.
    - **Acción**: renderizar.
    - **Assertion**: primer paint <500ms; scroll a 60fps.

---

### `flowchart.ts` (src/components/diagrams/flowchart.ts — 510 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Nodos con shape correcto (rect, ellipse, diamond)** — [visual]
   - **Setup**: spec con 3 nodos de shapes distintos.
   - **Acción**: renderizar.
   - **Assertion**: cada nodo renderizado con su shape SVG correcto.

2. **Edges con flechas direccionales** — [visual]
   - **Setup**: spec con 2 nodos A→B.
   - **Acción**: renderizar.
   - **Assertion**: edge con marker-end arrow.

3. **Decision node (diamond)**: dos paths yes/no** — [visual]
   - **Setup**: spec con decision y 2 branches "yes"/"no".
   - **Acción**: renderizar.
   - **Assertion**: edges etiquetados "yes" y "no".

4. **Auth: requiere login para ver diagram privado** — [auth]
   - **Setup**: spec con `private: true` y `auth-required`.
   - **Acción**: renderizar sin sesión.
   - **Assertion**: placeholder "Login required"; sin datos del spec.

5. **Drag reposiciona nodo** — [drag]
   - **Setup**: nodo en `{x:0,y:0}`.
   - **Acción**: drag +100,+50.
   - **Assertion**: posición actualizada.

6. **Click emite `is-node-select`** — [event]
   - **Setup**: 2 nodos.
   - **Acción**: click sobre el primero.
   - **Assertion**: event fired con id.

7. **prefers-reduced-motion: sin animación al drag** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: drag.
   - **Assertion**: sin transition.

8. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: fill/stroke con vars dark.

9. **CSS part `--is-node-bg`** — [css-part]
   - **Setup**: 1 nodo.
   - **Acción**: `::part(is-node-bg) { fill: yellow; }`.
   - **Assertion**: bg yellow.

10. **Spec inválido: edge sin source/target** — [edge-case]
    - **Setup**: spec con edge `{from: undefined}`.
    - **Acción**: renderizar.
    - **Assertion**: console.error "Invalid edge"; edge omitido.

---

### `gantt.ts` (src/components/diagrams/gantt.ts — 470 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Renderiza tasks con barras horizontales** — [visual]
   - **Setup**: spec con 3 tasks.
   - **Acción**: renderizar.
   - **Assertion**: 3 `<rect class="is-task-bar">` con width proporcional a duración.

2. **Dependencias entre tasks con flechas** — [visual]
   - **Setup**: spec con 3 tasks y 2 dependencias T1→T2, T2→T3.
   - **Acción**: renderizar.
   - **Assertion**: 2 arrows conectando las barras en orden.

3. **Drag de barra para cambiar duración** — [drag]
   - **Setup**: task de 5 días.
   - **Acción**: drag del borde derecho +2 días.
   - **Assertion**: task.duration actualizada a 7; barra redimensionada.

4. **Scroll horizontal si el rango excede viewport** — [visual, browser-api]
   - **Setup**: spec con rango de 365 días.
   - **Acción**: renderizar en viewport 800px.
   - **Assertion**: scroll-x disponible; `overflow-x: auto`.

5. **Click sobre task emite `is-task-select`** — [event]
   - **Setup**: 2 tasks.
   - **Acción**: click sobre la primera.
   - **Assertion**: event fired con detail.id.

6. **prefers-reduced-motion: sin animación al resize** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: drag de borde.
   - **Assertion**: sin transition.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: barras usan --is-task-fill-dark.

8. **CSS part `--is-task-bar-bg`** — [css-part]
   - **Setup**: 1 task.
   - **Acción**: `::part(is-task-bar-bg) { fill: red; }`.
   - **Assertion**: fill red.

9. **Hover muestra tooltip con detalles** — [visual, event]
   - **Setup**: 1 task con `description`.
   - **Acción**: mouseenter.
   - **Assertion**: tooltip con nombre + duración + descripción.

10. **Spec con milestone (diamante)** — [visual]
    - **Setup**: spec con `milestones: [{date: '2024-01-15', label: 'Release'}]`.
    - **Acción**: renderizar.
    - **Assertion**: diamante en la fecha correcta.

---

### `journey-map.ts` (src/components/diagrams/journey-map.ts — 430 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Renderiza fases horizontales con steps** — [visual]
   - **Setup**: spec con 3 fases y 2 steps cada una.
   - **Acción**: renderizar.
   - **Assertion**: 3 columnas con headers de fase + 6 step boxes (2 por fase).

2. **Touchpoints con iconos** — [visual]
   - **Setup**: spec con touchpoints `{icon: 'cart', label: 'Add to cart'}`.
   - **Acción**: renderizar.
   - **Assertion**: icono renderizado (svg o background-image) + label.

3. **Emociones por step (😊 😐 😞)** — [visual]
   - **Setup**: spec con step `emotion: 0.8`.
   - **Acción**: renderizar.
   - **Assertion**: el step muestra una carita sonriente o un color verde.

4. **Click sobre step emite `is-step-select`** — [event]
   - **Setup**: 2 steps.
   - **Acción**: click sobre el primero.
   - **Assertion**: event fired.

5. **Drag horizontal para reorganizar fases** — [drag]
   - **Setup**: 3 fases.
   - **Acción**: drag de fase 2 a posición de fase 1.
   - **Assertion**: orden actualizado en spec; DOM redibuja.

6. **prefers-reduced-motion: sin animación al reorganizar** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: drag de fase.
   - **Assertion**: sin transition.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: bg de fases usa --is-journey-bg-dark.

8. **CSS part `--is-journey-step`** — [css-part]
   - **Setup**: 1 step.
   - **Acción**: `::part(is-journey-step) { background: blue; }`.
   - **Assertion**: bg blue.

9. **Spec con 1 sola fase: layout sigue funcionando** — [edge-case]
   - **Setup**: spec con 1 fase.
   - **Acción**: renderizar.
   - **Assertion**: render OK; sin overflow.

10. **Hover muestra descripción del step** — [visual, event]
    - **Setup**: step con `description: 'Detailed text'`.
    - **Acción**: mouseenter.
    - **Assertion**: tooltip con descripción completa.

---

### `lightbox.ts` (src/components/diagrams/lightbox.ts — 280 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Abre con click en imagen trigger** — [event]
   - **Setup**: `<img is-lightbox-trigger>` con `src`.
   - **Acción**: click sobre la imagen.
   - **Assertion**: lightbox visible con la imagen ampliada.

2. **Cierra con click en backdrop** — [event]
   - **Setup**: lightbox abierto.
   - **Acción**: click fuera del contenido.
   - **Assertion**: lightbox cerrado.

3. **Cierra con Escape** — [keyboard]
   - **Setup**: lightbox abierto.
   - **Acción**: keydown Escape.
   - **Assertion**: cerrado; foco restaurado.

4. **Worker: precarga siguiente imagen** — [worker]
   - **Setup**: gallery con 5 imágenes; índice 0 abierto.
   - **Acción**: esperar 100ms.
   - **Assertion**: worker.fetch imagen índice 1 ya completo (en network tab o cache).

5. **Flechas navegan entre imágenes** — [keyboard]
   - **Setup**: gallery con 3 imágenes; índice 1 abierto.
   - **Acción**: ArrowRight.
   - **Assertion**: índice cambia a 2; src actualizado.

6. **Focus trap** — [a11y, focus]
   - **Setup**: lightbox abierto con 2 botones (close, next).
   - **Acción**: Tab desde el último.
   - **Assertion**: foco vuelve al primero.

7. **prefers-reduced-motion: sin fade al cambiar imagen** — [reduced-motion]
   - **Setup**: Context reduced; lightbox abierto.
   - **Acción**: ArrowRight.
   - **Assertion**: cambio instantáneo; sin opacity transition.

8. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: abrir lightbox.
   - **Assertion**: backdrop usa --is-lightbox-dark.

9. **Dispose: cierra worker** — [edge-case, worker]
   - **Setup**: lightbox abierto con worker.
   - **Acción**: removeChild.
   - **Assertion**: worker terminated.

10. **Click en imagen ampliada: zoom in/out** — [event]
    - **Setup**: lightbox abierto.
    - **Acción**: click sobre la imagen.
    - **Assertion**: clase `is-zoomed` toggled; transform: scale(2) aplicada.

---

### `mindmap.ts` (src/components/diagrams/mindmap.ts — 450 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Nodo central con branches radiales** — [visual]
   - **Setup**: spec con root y 4 children.
   - **Acción**: renderizar.
   - **Assertion**: 5 nodos en layout radial; 4 edges curvados desde root.

2. **Drag para reposicionar nodo** — [drag]
   - **Setup**: nodo child en posición calculada.
   - **Acción**: drag +30, +40.
   - **Assertion**: posición actualizada; edge se redibuja.

3. **Doble-click para añadir child** — [event, focus]
   - **Setup**: 1 nodo.
   - **Acción**: dblclick sobre el nodo; escribir "New"; Enter.
   - **Assertion**: nuevo nodo añadido como child; `is-node-add` event fired.

4. **Click emite `is-node-select`** — [event]
   - **Setup**: 2 nodos.
   - **Acción**: click sobre el primero.
   - **Assertion**: event fired con id.

5. **Teclado: flechas para navegar entre nodos** — [keyboard, a11y]
   - **Setup**: 5 nodos; foco en root.
   - **Acción**: ArrowDown.
   - **Assertion**: foco se mueve al primer child.

6. **prefers-reduced-motion: sin animación al drag** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: drag.
   - **Assertion**: sin transition.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: nodes usan --is-mindmap-node-dark.

8. **CSS part `--is-mindmap-node`** — [css-part]
   - **Setup**: 1 nodo.
   - **Acción**: `::part(is-mindmap-node) { background: teal; }`.
   - **Assertion**: bg teal.

9. **Spec con 100 nodos: render perezoso** — [performance]
   - **Setup**: spec con 100 nodos.
   - **Acción**: renderizar.
   - **Assertion**: <500ms first paint; sin layout thrashing.

10. **Dispose cleanup** — [edge-case]
    - **Setup**: 5 nodos.
    - **Acción**: removeChild.
    - **Assertion**: sin listeners residuales.

---

### `org-chart.ts` (src/components/diagrams/org-chart.ts — 580 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (12):

1. **Jerarquía vertical (CEO → VP → Manager)** — [visual]
   - **Setup**: spec con 3 niveles, 1 root, 2 children, 4 grandchildren.
   - **Acción**: renderizar.
   - **Assertion**: 7 nodos en árbol vertical; edges conectan padres con hijos.

2. **Drag reposiciona nodo** — [drag]
   - **Setup**: 1 nodo.
   - **Acción**: drag +20,+30.
   - **Assertion**: posición actualizada; edges se recalculan.

3. **Click emite `is-orgnode-select`** — [event]
   - **Setup**: 2 nodos.
   - **Acción**: click sobre el primero.
   - **Assertion**: event fired con id y label.

4. **SQL query: race condition sin whenDefined** — [sql-query, edge-case]
   - **Setup**: spec con `data-sql-query`.
   - **Acción**: añadir al DOM antes de la definición.
   - **Assertion**: query se ejecuta tras la definición; sin error en consola.

5. **Worker: cancelación al cerrar antes de cargar** — [worker, edge-case]
   - **Setup**: editor con query pesada; usuario navega antes de 3s.
   - **Acción**: navegar a otra página.
   - **Assertion**: worker receives cancel; sin resultado aplicado.

6. **Animación `transition: d/r` bloquea main thread** — [animation, performance]
   - **Setup**: spec con 50 nodos.
   - **Acción**: hover sobre cada nodo.
   - **Assertion**: el test mide main thread blocking; debería ser <16ms por hover (actualmente >50ms por el transition no-GPU).

7. **prefers-reduced-motion: sin transition d/r** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: hover sobre nodo.
   - **Assertion**: transition-duration: 0s.

8. **Migration: cambio de schema del spec viejo al nuevo** — [migration]
   - **Setup**: spec v1 (con `parentId`) vs spec v2 (con `parentIds: string[]`).
   - **Acción**: renderizar v1.
   - **Assertion**: runtime migra v1→v2 automáticamente; sin error.

9. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: nodes usan --is-org-node-dark.

10. **CSS part `--is-org-node`** — [css-part]
    - **Setup**: 1 nodo.
    - **Acción**: `::part(is-org-node) { background: navy; color: white; }`.
    - **Assertion**: styles aplicados.

11. **Spec con ciclo (A es padre de B, B es padre de A)** — [edge-case]
    - **Setup**: spec con ciclo.
    - **Acción**: renderizar.
    - **Assertion**: console.error "Cycle detected"; renderiza como DAG ignorando la arista de cierre.

12. **Dispose cleanup completo** — [edge-case]
    - **Setup**: 10 nodos + worker activo.
    - **Acción**: removeChild.
    - **Assertion**: worker terminated; sin listeners; sin timers.

---

### `quadrant-chart.ts` (src/components/diagrams/quadrant-chart.ts — 390 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **4 cuadrantes con labels** — [visual]
   - **Setup**: spec con 4 puntos, 1 por cuadrante.
   - **Acción**: renderizar.
   - **Assertion**: 4 labels "Q1", "Q2", "Q3", "Q4" en esquinas; ejes X/Y cruzando en el centro.

2. **Animación de entrada (puntos aparecen)** — [animation]
   - **Setup**: spec con 4 puntos.
   - **Acción**: renderizar.
   - **Assertion**: cada punto hace `scale(0)→scale(1)` con stagger (~100ms entre puntos).

3. **prefers-reduced-motion: sin animación al entrar** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: renderizar.
   - **Assertion**: animación aplicada instantáneamente.

4. **Click sobre punto emite `is-point-select`** — [event]
   - **Setup**: 2 puntos.
   - **Acción**: click sobre el primero.
   - **Assertion**: event fired.

5. **Hover muestra tooltip con label** — [visual, event]
   - **Setup**: 1 punto con `label`.
   - **Acción**: mouseenter.
   - **Assertion**: tooltip visible.

6. **Drag reposiciona punto** — [drag]
   - **Setup**: 1 punto en {x:0.5, y:0.5}.
   - **Acción**: drag +10,+10.
   - **Assertion**: posición actualizada a {x:0.6, y:0.6}.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: ejes usan --is-quadrant-axis-dark.

8. **CSS part `--is-quadrant-bg`** — [css-part]
   - **Setup**: 1 cuadrante.
   - **Acción**: `::part(is-quadrant-bg) { fill: lightyellow; }`.
   - **Assertion**: bg lightyellow.

9. **Spec con 1 solo punto: centrado** — [edge-case]
   - **Setup**: spec con `points: [{x:0.5,y:0.5}]`.
   - **Acción**: renderizar.
   - **Assertion**: punto centrado; ejes cruzando correctamente.

10. **Dispose cleanup** — [edge-case]
    - **Setup**: 4 puntos.
    - **Acción**: removeChild.
    - **Assertion**: sin listeners.

---

### `sankey-diagram.ts` (src/components/diagrams/sankey-diagram.ts — 410 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Flujos con grosor proporcional al valor** — [visual]
   - **Setup**: spec con flujos {a→b: 10, a→c: 5, b→d: 8}.
   - **Acción**: renderizar.
   - **Assertion**: grosor del path a→b es 2x el de a→c.

2. **Nodes como rectángulos con altura proporcional** — [visual]
   - **Setup**: spec con nodo que recibe 15 unidades.
   - **Acción**: renderizar.
   - **Assertion**: rect del nodo tiene altura proporcional a 15.

3. **Hover sobre flujo: highlight + tooltip** — [visual, event]
   - **Setup**: spec con 3 flujos.
   - **Acción**: mouseenter sobre el primero.
   - **Assertion**: flujo con opacity 0.8; tooltip "a→b: 10".

4. **Click sobre flujo emite `is-flow-select`** — [event]
   - **Setup**: 2 flujos.
   - **Acción**: click sobre el primero.
   - **Assertion**: event fired con detail `{from, to, value}`.

5. **prefers-reduced-motion: sin animación al hover** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: mouseenter.
   - **Assertion**: sin transition.

6. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: flujos usan --is-sankey-flow-dark.

7. **CSS part `--is-sankey-link`** — [css-part]
   - **Setup**: 1 link.
   - **Acción**: `::part(is-sankey-link) { fill: blue; }`.
   - **Assertion**: fill blue.

8. **Spec con valor negativo: tratado como 0** — [edge-case]
   - **Setup**: spec con `{value: -5}`.
   - **Acción**: renderizar.
   - **Assertion**: console.warn; flujo renderizado con grosor 0 (omitido).

9. **Spec con ciclos: error** — [edge-case]
   - **Setup**: spec con A→B→A.
   - **Acción**: renderizar.
   - **Assertion**: console.error "Cycle in sankey"; no renderiza.

10. **Dispose cleanup** — [edge-case]
    - **Setup**: 3 flujos.
    - **Acción**: removeChild.
    - **Assertion**: sin listeners.

---

### `sequence-diagram.ts` (src/components/diagrams/sequence-diagram.ts — 470 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Lifelines verticales con mensajes** — [visual]
   - **Setup**: spec con 2 actores A, B y 3 mensajes.
   - **Acción**: renderizar.
   - **Assertion**: 2 lifelines verticales + 3 flechas horizontales con labels.

2. **Mensajes async (dashed arrow)** — [visual]
   - **Setup**: spec con mensaje `kind: 'async'`.
   - **Acción**: renderizar.
   - **Assertion**: flecha con stroke-dasharray.

3. **Bloques `alt`, `loop`, `opt`** — [visual]
   - **Setup**: spec con bloque `loop { count: 3 }`.
   - **Acción**: renderizar.
   - **Assertion**: rect que envuelve los mensajes del loop + label "loop [3]".

4. **Click sobre mensaje emite `is-message-select`** — [event]
   - **Setup**: 2 mensajes.
   - **Acción**: click sobre el primero.
   - **Assertion**: event fired.

5. **Drag para reposicionar actores** — [drag]
   - **Setup**: 2 actores en x=100, x=300.
   - **Acción**: drag actor 2 a x=400.
   - **Assertion**: posición actualizada; lifelines y mensajes recalculados.

6. **prefers-reduced-motion: sin animación al activar bloque** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: spec con bloque `alt`.
   - **Assertion**: sin transición al expandir/colapsar.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: lifelines usan --is-seq-lifeline-dark.

8. **CSS part `--is-seq-actor-bg`** — [css-part]
   - **Setup**: 1 actor.
   - **Acción**: `::part(is-seq-actor-bg) { background: pink; }`.
   - **Assertion**: bg pink.

9. **Mensaje `note { position: 'right' }`** — [visual]
   - **Setup**: spec con `note`.
   - **Acción**: renderizar.
   - **Assertion**: rect amarillo a la derecha del actor con el texto.

10. **Spec con 50 mensajes: scroll vertical** — [edge-case]
    - **Setup**: spec con 50 mensajes.
    - **Acción**: renderizar en viewport 800px.
    - **Assertion**: scroll-y disponible; mensajes visibles.

---

### `state-diagram.ts` (src/components/diagrams/state-diagram.ts — 460 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Estados como rectángulos con labels** — [visual]
   - **Setup**: spec con 3 estados.
   - **Acción**: renderizar.
   - **Assertion**: 3 rects con nombres.

2. **Estado inicial (círculo relleno)** — [visual]
   - **Setup**: spec con `initial: 'Idle'`.
   - **Acción**: renderizar.
   - **Assertion**: círculo relleno conectado por arrow a "Idle".

3. **Estado final (doble círculo)** — [visual]
   - **Setup**: spec con `final: 'Done'`.
   - **Acción**: renderizar.
   - **Assertion**: doble círculo concéntrico.

4. **Transiciones con labels** — [visual]
   - **Setup**: spec con transición `Idle → Running` label="start".
   - **Acción**: renderizar.
   - **Assertion**: flecha con label "start" centrado.

5. **Click sobre estado emite `is-state-select`** — [event]
   - **Setup**: 2 estados.
   - **Acción**: click sobre el primero.
   - **Assertion**: event fired.

6. **Drag reposiciona estado** — [drag]
   - **Setup**: estado en {x:0,y:0}.
   - **Acción**: drag +50,+30.
   - **Assertion**: posición actualizada.

7. **prefers-reduced-motion: sin animación al drag** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: drag.
   - **Assertion**: sin transition.

8. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: bordes usan --is-state-stroke-dark.

9. **CSS part `--is-state-bg`** — [css-part]
   - **Setup**: 1 estado.
   - **Acción**: `::part(is-state-bg) { fill: gray; }`.
   - **Assertion**: fill gray.

10. **Spec con self-loop (estado que se transiciona a sí mismo)** — [edge-case]
    - **Setup**: spec con `{from: 'A', to: 'A', label: 'tick'}`.
    - **Acción**: renderizar.
    - **Assertion**: flecha curva que sale y vuelve al mismo estado.

---

### `swimlane-diagram.ts` (src/components/diagrams/swimlane-diagram.ts — 480 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Lanes horizontales con labels** — [visual]
   - **Setup**: spec con 3 lanes "Frontend", "Backend", "DB".
   - **Acción**: renderizar.
   - **Assertion**: 3 bandas horizontales con sus labels a la izquierda.

2. **Tasks agrupados en su lane** — [visual]
   - **Setup**: spec con tasks y `lane: 'Backend'`.
   - **Acción**: renderizar.
   - **Assertion**: task aparece dentro de la banda Backend.

3. **Flechas entre tasks (cross-lane)** — [visual]
   - **Setup**: task A en Frontend, task B en Backend, edge A→B.
   - **Acción**: renderizar.
   - **Assertion**: flecha cruza de Frontend a Backend.

4. **Click sobre task emite `is-task-select`** — [event]
   - **Setup**: 2 tasks.
   - **Acción**: click sobre el primero.
   - **Assertion**: event fired.

5. **Drag task entre lanes** — [drag]
   - **Setup**: task en Frontend.
   - **Acción**: drag vertical a Backend.
   - **Assertion**: task.lane actualizado a 'Backend'.

6. **prefers-reduced-motion: sin animación al mover** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: drag.
   - **Assertion**: sin transition.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: lanes usan --is-swimlane-bg-dark.

8. **CSS part `--is-swimlane-bg`** — [css-part]
   - **Setup**: 1 lane.
   - **Acción**: `::part(is-swimlane-bg) { background: lightgray; }`.
   - **Assertion**: bg lightgray.

9. **Spec con 1 solo lane: layout sigue funcionando** — [edge-case]
   - **Setup**: spec con 1 lane.
   - **Acción**: renderizar.
   - **Assertion**: render OK.

10. **Hover sobre task muestra descripción** — [visual, event]
    - **Setup**: task con description.
    - **Acción**: mouseenter.
    - **Assertion**: tooltip.

---

### `timeline.ts` (src/components/diagrams/timeline.ts — 400 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Eventos en orden cronológico** — [visual]
   - **Setup**: spec con 3 eventos en fechas 2020, 2021, 2022.
   - **Acción**: renderizar.
   - **Assertion**: eventos posicionados de izquierda a derecha por fecha.

2. **Drag evento horizontalmente para cambiar fecha** — [drag]
   - **Setup**: evento en 2021.
   - **Acción**: drag +100px.
   - **Assertion**: evento.fecha actualizada proporcionalmente al ancho.

3. **Click emite `is-event-select`** — [event]
   - **Setup**: 2 eventos.
   - **Acción**: click sobre el primero.
   - **Assertion**: event fired.

4. **Zoom in/out con rueda del ratón** — [event]
   - **Setup**: timeline con 10 eventos.
   - **Acción**: Ctrl+wheel up.
   - **Assertion**: timeline escala 1.5x; eventos más separados.

5. **prefers-reduced-motion: sin animación al zoom** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: Ctrl+wheel.
   - **Assertion**: zoom instantáneo.

6. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: ejes usan --is-timeline-axis-dark.

7. **CSS part `--is-timeline-event`** — [css-part]
   - **Setup**: 1 evento.
   - **Acción**: `::part(is-timeline-event) { background: orange; }`.
   - **Assertion**: bg orange.

8. **Spec con eventos en fechas futuras** — [edge-case]
   - **Setup**: spec con `date: '2099-01-01'`.
   - **Acción**: renderizar.
   - **Assertion**: evento fuera del viewport (scroll-x al final).

9. **Hover muestra descripción del evento** — [visual, event]
   - **Setup**: evento con description.
   - **Acción**: mouseenter.
   - **Assertion**: tooltip.

10. **Dispose cleanup** — [edge-case]
    - **Setup**: 5 eventos.
    - **Acción**: removeChild.
    - **Assertion**: sin listeners.

---

### `use-case-diagram.ts` (src/components/diagrams/use-case-diagram.ts — 420 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Actores como stick figures** — [visual]
   - **Setup**: spec con 2 actores "User" y "Admin".
   - **Acción**: renderizar.
   - **Assertion**: 2 stick figures con labels.

2. **Use cases como elipses con labels** — [visual]
   - **Setup**: spec con 2 use cases "Login" y "Logout".
   - **Acción**: renderizar.
   - **Assertion**: 2 elipses.

3. **Sistema como rectángulo contenedor** — [visual]
   - **Setup**: spec con `system: 'BankingApp'`.
   - **Acción**: renderizar.
   - **Assertion**: rect con label "BankingApp" envolviendo use cases.

4. **Relaciones actor→use-case con flechas** — [visual]
   - **Setup**: actor User conectado a "Login".
   - **Acción**: renderizar.
   - **Assertion**: flecha del actor a la elipse.

5. **Click sobre use case emite `is-usecase-select`** — [event]
   - **Setup**: 2 use cases.
   - **Acción**: click sobre el primero.
   - **Assertion**: event fired.

6. **Drag reposiciona actor** — [drag]
   - **Setup**: actor en {x:0,y:0}.
   - **Acción**: drag +30,+30.
   - **Assertion**: posición actualizada.

7. **prefers-reduced-motion: sin animación al drag** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: drag.
   - **Assertion**: sin transition.

8. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: contornos usan --is-usecase-stroke-dark.

9. **CSS part `--is-usecase-bg`** — [css-part]
   - **Setup**: 1 use case.
   - **Acción**: `::part(is-usecase-bg) { fill: yellow; }`.
   - **Assertion**: fill yellow.

10. **Spec con 1 solo actor y 1 use case** — [edge-case]
    - **Setup**: spec mínimo.
    - **Acción**: renderizar.
    - **Assertion**: render OK.

---

### `venn-diagram.ts` (src/components/diagrams/venn-diagram.ts — 380 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **2 círculos con intersección** — [visual]
   - **Setup**: spec con 2 sets A, B.
   - **Acción**: renderizar.
   - **Assertion**: 2 círculos superpuestos; área de intersección visible.

2. **3 círculos con intersección triple** — [visual]
   - **Setup**: spec con 3 sets.
   - **Acción**: renderizar.
   - **Assertion**: 3 círculos con intersección central.

3. **Labels en cada región** — [visual]
   - **Setup**: spec con labels "A only", "B only", "A∩B".
   - **Acción**: renderizar.
   - **Assertion**: labels posicionados en sus regiones correctas.

4. **Click sobre región emite `is-region-select`** — [event]
   - **Setup**: 3 regiones.
   - **Acción**: click sobre "A∩B".
   - **Assertion**: event fired con detail `{regions: ['A', 'B']}`.

5. **Drag reposiciona círculo** — [drag]
   - **Setup**: 1 círculo.
   - **Acción**: drag +20,+20.
   - **Assertion**: posición actualizada.

6. **prefers-reduced-motion: sin animación al hover** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: mouseenter sobre región.
   - **Assertion**: sin transition.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: círculos usan --is-venn-fill-dark.

8. **CSS part `--is-venn-circle`** — [css-part]
   - **Setup**: 1 círculo.
   - **Acción**: `::part(is-venn-circle) { fill: rgba(255,0,0,0.3); }`.
   - **Assertion**: fill con opacity.

9. **Spec con 4 sets: layout automático** — [edge-case]
   - **Setup**: spec con 4 sets.
   - **Acción**: renderizar.
   - **Assertion**: 4 círculos posicionados sin solapamiento total.

10. **Dispose cleanup** — [edge-case]
    - **Setup**: 3 círculos.
    - **Acción**: removeChild.
    - **Assertion**: sin listeners.

---

### `date-field-element.ts` (src/components/_shared/date-field-element.ts — 290 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Renderiza input type=date con label** — [visual]
   - **Setup**: spec con `label`, `value`.
   - **Acción**: renderizar.
   - **Assertion**: `<label>` + `<input type="date">` con value.

2. **Cambio de valor emite `is-date-change`** — [event]
   - **Setup**: field con valor inicial "2024-01-01".
   - **Acción**: input a "2024-02-01"; change.
   - **Assertion**: event fired con detail.value.

3. **SQL query: lista de fechas disponibles** — [sql-query]
   - **Setup**: field con `data-sql-query="SELECT date FROM events"`.
   - **Acción**: renderizar.
   - **Assertion**: datalist con opciones.

4. **Race condition sin whenDefined** — [edge-case, sql-query]
   - **Setup**: añadir al DOM antes de la definición.
   - **Acción**: renderizar.
   - **Assertion**: query se ejecuta tras definición; sin error.

5. **Teclado: flechas navegan días** — [keyboard, a11y]
   - **Setup**: field con valor "2024-01-15".
   - **Acción**: ArrowUp en día.
   - **Assertion**: valor cambia a "2024-01-14"; change event.

6. **Locale-aware format (es-ES vs en-US)** — [i18n]
   - **Setup**: lang="es-ES" en host.
   - **Acción**: renderizar.
   - **Assertion**: formato visual es dd/mm/yyyy (atributo `lang` propagado al shadow).

7. **prefers-reduced-motion: sin highlight al focus** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: focus en input.
   - **Assertion**: sin transition.

8. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: input usa --is-date-field-dark.

9. **Valor inválido: border rojo + aria-invalid** — [a11y]
   - **Setup**: min="2024-01-01", max="2024-12-31"; valor "2025-01-01".
   - **Acción**: renderizar.
   - **Assertion**: `aria-invalid="true"`; clase `is-invalid`.

10. **Dispose cleanup** — [edge-case]
    - **Setup**: 1 field.
    - **Acción**: removeChild.
    - **Assertion**: sin listeners.

---

### `diagram-element-base.ts` (src/components/_shared/diagram-element-base.ts — 180 LOC)

**Tests existentes** (resumen): NINGUNO (es una clase base).

**Propuestas nuevas** (8):

1. **Base class: render() llamado en connectedCallback** — [browser-api]
   - **Setup**: subclase concreta con render() override.
   - **Acción**: appendChild al DOM.
   - **Assertion**: render() invocado una vez.

2. **attributeChangedCallback para `spec`** — [browser-api]
   - **Setup**: elemento con spec inicial.
   - **Acción**: setAttribute('spec', '{...}').
   - **Assertion**: render() invocado de nuevo con nuevo spec.

3. **Custom event `is-diagram-ready`** — [event]
   - **Setup**: subclase.
   - **Acción**: connectedCallback.
   - **Assertion**: event fired una vez tras primer render.

4. **Dispose: desconexión limpia listeners** — [edge-case]
   - **Setup**: subclase con listener attached.
   - **Acción**: removeChild.
   - **Assertion**: sin listeners residuales.

5. **observedAttributes funciona** — [browser-api]
   - **Setup**: subclase con `static get observedAttributes()`.
   - **Acción**: cambiar atributo.
   - **Assertion**: attributeChangedCallback invocado.

6. **Error en render no rompe connectedCallback** — [edge-case]
   - **Setup**: subclase con render() que lanza.
   - **Acción**: connectedCallback.
   - **Assertion**: error capturado; elemento sigue en DOM.

7. **Shadow DOM mode: open** — [browser-api]
   - **Setup**: subclase.
   - **Acción**: acceder `element.shadowRoot`.
   - **Assertion**: shadowRoot accesible.

8. **Theme change propaga a subclases** — [visual]
   - **Setup**: subclase con elementos themed.
   - **Acción**: cambiar data-theme en host.
   - **Assertion**: subclases reaccionan al cambio.

---

### `dom-utils.ts` (src/components/_shared/dom-utils.ts — 220 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (12):

1. **`escapeHTML` escapa `<`, `>`, `&`, `"`, `'`** — [security, edge-case]
   - **Setup**: input `<script>alert("XSS")</script>`.
   - **Acción**: escapeHTML(input).
   - **Assertion**: output `&lt;script&gt;alert(&quot;XSS&quot;)&lt;/script&gt;`.

2. **`querySelectorAll` retorna array, no NodeList** — [browser-api]
   - **Setup**: DOM con 3 elementos.
   - **Acción**: querySelectorAll('.x').
   - **Assertion**: resultado es `Array` (has `.map`, `.filter`).

3. **`createElement` con namespace SVG** — [browser-api]
   - **Setup**: nombre="rect", ns="http://www.w3.org/2000/svg".
   - **Acción**: createElement.
   - **Assertion**: elemento es `SVGRectElement`.

4. **`insertAt` inserta en índice específico** — [browser-api]
   - **Setup**: ul con 3 li.
   - **Acción**: insertAt(ul, li, 1).
   - **Assertion**: nuevo li en posición 1.

5. **`removeAllChildren` limpia listeners** — [edge-case, browser-api]
   - **Setup**: div con children que tienen listeners.
   - **Acción**: removeAllChildren.
   - **Assertion**: children removidos del DOM.

6. **`debounce` cancela calls rápidos** — [edge-case]
   - **Setup**: fn = sinon.spy(); debounced = debounce(fn, 100).
   - **Acción**: debounced() 5 veces en 50ms.
   - **Assertion**: fn llamado 1 vez tras 100ms.

7. **`throttle` limita calls por intervalo** — [edge-case]
   - **Setup**: fn spy; throttled = throttle(fn, 100).
   - **Acción**: throttled() 10 veces en 100ms.
   - **Assertion**: fn llamado 2 veces máx.

8. **`getBoundingClientRectSafe` retorna null si detachado** — [edge-case]
   - **Setup**: elemento detachado del DOM.
   - **Acción**: getBoundingClientRectSafe(el).
   - **Assertion**: retorna null o {0,0,0,0} sin throw.

9. **`isInViewport` retorna boolean** — [browser-api]
   - **Setup**: elemento visible; elemento fuera.
   - **Acción**: isInViewport(el).
   - **Assertion**: true para visible; false para fuera.

10. **`copyToClipboard` con fallback para browsers viejos** — [browser-api, edge-case]
    - **Setup**: navigator.clipboard undefined.
    - **Acción**: copyToClipboard('text').
    - **Assertion**: usa fallback con execCommand o textarea.

11. **SQL escape: previene injection en atributos** — [security, sql-query]
    - **Setup**: input `'; DROP TABLE users; --`.
    - **Acción**: sqlEscape(input).
    - **Assertion**: comillas y comentarios escapados.

12. **`waitForElement` con timeout** — [browser-api, edge-case]
    - **Setup**: elemento añadido al DOM tras 50ms.
    - **Acción**: waitForElement('.x', 200).
    - **Assertion**: resuelve con el elemento.

---

### `form-control-mixin.ts` (src/components/_shared/form-control-mixin.ts — 210 LOC)

**Tests existentes** (resumen): NINGUNO (mixin).

**Propuestas nuevas** (8):

1. **Mixin: subclase expone `value` reactive** — [browser-api]
   - **Setup**: clase que aplica el mixin.
   - **Acción**: set value.
   - **Assertion**: getter retorna el nuevo valor.

2. **`name` se registra en form-associated custom elements** — [a11y, browser-api]
   - **Setup**: elemento dentro de `<form>`.
   - **Acción**: submit.
   - **Assertion**: FormData incluye `name=value`.

3. **`disabled` bloquea interacción** — [a11y]
   - **Setup**: elemento con disabled=true.
   - **Acción**: click.
   - **Assertion**: no emite change event.

4. **`required` añade aria-required** — [a11y]
   - **Setup**: elemento con required=true.
   - **Acción**: renderizar.
   - **Assertion**: `aria-required="true"`.

5. **Validación: `validity.valid` false si required y vacío** — [a11y]
   - **Setup**: required=true; value="".
   - **Acción**: checkValidity.
   - **Assertion**: false; `validationMessage` no vacío.

6. **`reset()` restaura value al defaultValue** — [browser-api]
   - **Setup**: defaultValue="x"; value="y".
   - **Acción**: reset().
   - **Assertion**: value vuelve a "x".

7. **Focus programático via `focus()`** — [focus, a11y]
   - **Setup**: elemento.
   - **Acción**: element.focus().
   - **Assertion**: document.activeElement === element.

8. **Dispose: formAssociated desregistra** — [edge-case]
    - **Setup**: elemento en form.
    - **Acción**: removeChild.
    - **Assertion**: sin entry en form internals.

---

### `highlight-code.ts` (src/components/_shared/highlight-code.ts — 240 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Resalta tokens de JavaScript** — [visual]
   - **Setup**: code `const x = 1;`.
   - **Acción**: highlight.
   - **Assertion**: `<span class="tok-keyword">const</span>`, `<span class="tok-number">1</span>`.

2. **Resalta múltiples lenguajes** — [visual]
   - **Setup**: code Python `def foo(): pass`.
   - **Acción**: highlight con `lang="python"`.
   - **Assertion**: tokens Python (def como keyword).

3. **Theme dark** — [visual]
   - **Setup**: data-theme=dark; code JS.
   - **Acción**: highlight.
   - **Assertion**: tokens usan colores dark (string verde, keyword azul claro).

4. **Theme light** — [visual]
   - **Setup**: data-theme=light; code JS.
   - **Acción**: highlight.
   - **Assertion**: tokens usan colores light (string rojo, keyword azul oscuro).

5. **Copy to clipboard button** — [event]
   - **Setup**: code con botón "Copy".
   - **Acción**: click.
   - **Assertion**: navigator.clipboard.writeText llamado con el código.

6. **XSS: code con `<script>` no se ejecuta** — [security, edge-case]
   - **Setup**: code `<script>alert(1)</script>`.
   - **Acción**: highlight.
   - **Assertion**: `<script>` renderizado como texto (escaped), no como elemento.

7. **Línea highlighted en hover** — [visual, focus]
   - **Setup**: code de 10 líneas.
   - **Acción**: hover sobre línea 5.
   - **Assertion**: línea 5 con bg gris claro.

8. **Número de línea visible** — [visual]
   - **Setup**: code de 5 líneas.
   - **Acción**: renderizar.
   - **Assertion**: gutter con números 1-5.

9. **Resize handle horizontal** — [drag]
   - **Setup**: code en container.
   - **Acción**: drag del handle a la derecha.
   - **Assertion**: ancho del code aumenta.

10. **Dispose cleanup** — [edge-case]
    - **Setup**: 1 bloque de código.
    - **Acción**: removeChild.
    - **Assertion**: sin listeners.

---

### `modal-base.ts` (src/components/_shared/modal-base.ts — 320 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (12):

1. **Abre modal con `open=true`** — [browser-api]
   - **Setup**: subclase con modal.
   - **Acción**: setAttribute('open', '').
   - **Assertion**: modal visible; `aria-hidden="false"`.

2. **Cierra modal con `open=false`** — [browser-api]
   - **Setup**: modal abierto.
   - **Acción**: removeAttribute('open').
   - **Assertion**: modal oculto; `aria-hidden="true"`.

3. **Focus trap: Tab cicla dentro** — [a11y, focus]
   - **Setup**: modal con 3 botones.
   - **Acción**: Tab desde el último.
   - **Assertion**: foco vuelve al primero.

4. **Restauración de foco al cerrar** — [a11y, focus]
   - **Setup**: trigger button; abrir modal desde trigger.
   - **Acción**: cerrar modal.
   - **Assertion**: document.activeElement === trigger button.

5. **Escape cierra modal** — [keyboard, a11y]
   - **Setup**: modal abierto.
   - **Acción**: keydown Escape.
   - **Assertion**: modal cerrado; foco restaurado.

6. **Click en backdrop cierra modal (si `dismissible`)** — [event]
   - **Setup**: modal con dismissible=true.
   - **Acción**: click fuera del contenido.
   - **Assertion**: modal cerrado.

7. **Click en backdrop NO cierra si `dismissible=false`** — [event, edge-case]
   - **Setup**: modal con dismissible=false.
   - **Acción**: click fuera del contenido.
   - **Assertion**: modal sigue abierto.

8. **Animación de apertura/cierre** — [animation]
   - **Setup**: modal con animación.
   - **Acción**: abrir.
   - **Assertion**: scale(0.95)→scale(1) en ≥200ms.

9. **prefers-reduced-motion: sin animación** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: abrir modal.
   - **Assertion**: sin transition.

10. **Migration: legacy `is-open` attribute** — [migration]
    - **Setup**: subclase v1 con `is-open` attribute.
    - **Acción**: setAttribute('is-open', '').
    - **Assertion**: el mixin migra a `open`; modal abre.

11. **`aria-modal="true"` siempre presente** — [a11y]
    - **Setup**: modal abierto.
    - **Acción**: verificar atributo.
    - **Assertion**: `aria-modal="true"`.

12. **Dispose: restaura body overflow y listeners** — [edge-case]
    - **Setup**: modal abierto.
    - **Acción**: removeChild (sin cerrar).
    - **Assertion**: body.style overflow restaurado; sin listeners.

---

### `picker-element.ts` (src/components/_shared/picker-element.ts — 410 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (12):

1. **Abre dropdown al click** — [event]
   - **Setup**: picker con opciones.
   - **Acción**: click sobre input.
   - **Assertion**: dropdown visible.

2. **Cierra dropdown al click fuera** — [event]
   - **Setup**: dropdown abierto.
   - **Acción**: click fuera.
   - **Assertion**: dropdown cerrado.

3. **Teclado: ArrowDown abre y mueve selección** — [keyboard, a11y]
   - **Setup**: picker con 5 opciones; foco en input.
   - **Acción**: ArrowDown.
   - **Assertion**: dropdown abre; opción 0 highlighted.

4. **Teclado: ArrowUp/ArrowDown navega opciones** — [keyboard, a11y]
   - **Setup**: dropdown abierto; opción 0 highlighted.
   - **Acción**: ArrowDown 2 veces.
   - **Assertion**: opción 2 highlighted.

5. **Teclado: Enter selecciona opción** — [keyboard, a11y]
   - **Setup**: dropdown abierto; opción 1 highlighted.
   - **Acción**: Enter.
   - **Assertion**: input.value = opción 1; `is-picker-change` event fired.

6. **Teclado: Escape cierra dropdown** — [keyboard, a11y]
   - **Setup**: dropdown abierto.
   - **Acción**: Escape.
   - **Assertion**: dropdown cerrado; foco en input.

7. **Filtrado al escribir en input** — [event]
   - **Setup**: picker con opciones ["apple", "banana", "cherry"].
   - **Acción**: input "ban".
   - **Assertion**: solo "banana" visible en dropdown.

8. **Highlight de opción con hover** — [visual, focus]
   - **Setup**: dropdown abierto.
   - **Acción**: hover sobre opción 2.
   - **Assertion**: opción 2 con clase `is-highlighted`.

9. **SQL query: opciones desde backend** — [sql-query]
   - **Setup**: picker con `data-sql-query`.
   - **Acción**: renderizar.
   - **Assertion**: opciones cargadas tras query.

10. **Theme dark** — [visual]
    - **Setup**: data-theme=dark.
    - **Acción**: abrir dropdown.
    - **Assertion**: dropdown usa --is-picker-dark.

11. **prefers-reduced-motion: sin animación al abrir** — [reduced-motion]
    - **Setup**: Context reduced.
    - **Acción**: click.
    - **Assertion**: sin transition.

12. **Dispose: cierra dropdown y limpia listeners** — [edge-case]
    - **Setup**: dropdown abierto.
    - **Acción**: removeChild.
    - **Assertion**: sin listeners residuales.

---

### `position.ts` (src/components/_shared/position.ts — 130 LOC)

**Tests existentes** (resumen): NINGUNO (utility module).

**Propuestas nuevas** (8):

1. **`calculatePosition(anchor, popup, placement)` retorna coordenadas** — [browser-api]
   - **Setup**: anchor en (100,100) con size (50,50); popup size (200,100).
   - **Acción**: calculatePosition(anchor, popup, 'bottom').
   - **Assertion**: retorna `{top: 150, left: 100}`.

2. **Placement `top` invierte eje Y** — [browser-api]
   - **Setup**: anchor en (100,100).
   - **Acción**: calculatePosition(anchor, popup, 'top').
   - **Assertion**: retorna `{top: 0, left: 100}` (popup arriba del anchor).

3. **`flip` evita overflow de viewport** — [browser-api, edge-case]
   - **Setup**: anchor cerca del borde inferior (viewport 600h).
   - **Acción**: calculatePosition con placement='bottom'.
   - **Assertion**: retorna placement='top' si no cabe abajo.

4. **`shift` ajusta dentro del viewport** — [browser-api, edge-case]
   - **Setup**: anchor cerca del borde derecho.
   - **Acción**: calculatePosition.
   - **Assertion**: left ajustado para no overflow.

5. **Soporta RTL (right-to-left)** — [i18n]
   - **Setup**: dir="rtl" en html.
   - **Acción**: calculatePosition.
   - **Assertion**: left interpretado desde la derecha.

6. **Retorna `{top:0, left:0}` si anchor es null** — [edge-case]
   - **Setup**: anchor = null.
   - **Acción**: calculatePosition(null, popup, 'bottom').
   - **Assertion**: retorna `{top: 0, left: 0}` sin throw.

7. **`arrow` position calculada** — [browser-api]
   - **Setup**: anchor size (50,50); popup placement='bottom'.
   - **Acción**: calculateArrow(anchor, popup, 'bottom').
   - **Assertion**: arrow.top entre anchor.top y anchor.bottom.

8. **`update()` reactivo a window resize** — [browser-api, edge-case]
   - **Setup**: posición inicial; resize window.
   - **Acción**: window.dispatchEvent('resize').
   - **Assertion**: posición recalculada.

---

### `theme-scope.ts` (src/components/_shared/theme-scope.ts — 160 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Aplica theme scope a sus children** — [visual, browser-api]
   - **Setup**: `<is-theme-scope theme="dark">` con child.
   - **Acción**: renderizar.
   - **Assertion**: child tiene `data-theme="dark"`.

2. **Cambio de theme propaga a children** — [visual]
   - **Setup**: theme-scope con theme="light"; child.
   - **Acción**: setAttribute('theme', 'dark').
   - **Assertion**: child cambia a `data-theme="dark"`.

3. **Custom event `is-theme-change` al cambiar** — [event]
   - **Setup**: theme-scope con theme inicial.
   - **Acción**: cambiar theme.
   - **Assertion**: event fired con detail `{oldTheme, newTheme}`.

4. **Children añadidos dinámicamente heredan theme** — [browser-api]
   - **Setup**: theme-scope en DOM; theme="dark".
   - **Acción**: appendChild(newChild) al scope.
   - **Assertion**: newChild tiene data-theme="dark".

5. **Theme transition suave (CSS transition)** — [animation]
   - **Setup**: theme-scope con transition="0.3s".
   - **Acción**: cambiar theme.
   - **Assertion**: bg-color interpolada durante 0.3s.

6. **prefers-reduced-motion: sin transición** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: cambiar theme.
   - **Assertion**: cambio instantáneo.

7. **Nested theme-scopes: inner override outer** — [browser-api, edge-case]
   - **Setup**: outer theme="light", inner theme="dark" con child.
   - **Acción**: renderizar.
   - **Assertion**: child tiene data-theme="dark" (inner gana).

8. **Dispose: cleanup listeners en disconnectedCallback** — [edge-case]
   - **Setup**: theme-scope con MutationObserver.
   - **Acción**: removeChild.
   - **Assertion**: observer disconnected; sin listeners.

9. **Theme inválido: fallback a 'light'** — [edge-case]
   - **Setup**: theme-scope con theme="invalid".
   - **Acción**: renderizar.
   - **Assertion**: console.warn; data-theme="light".

10. **Theme 'high-contrast' soportado** — [a11y]
    - **Setup**: theme-scope con theme="high-contrast".
    - **Acción**: renderizar.
    - **Assertion**: child tiene data-theme="high-contrast"; CSS variable --contrast aplicado.

---

### `breadcrumb-item.css` (src/components/navigation/breadcrumb-item.css — 80 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (8):

1. **Separador visual entre items** — [visual]
   - **Setup**: breadcrumb con 3 items.
   - **Acción**: renderizar.
   - **Assertion**: items 2 y 3 tienen `::before { content: '/' }`.

2. **Último item no tiene separador** — [visual]
   - **Setup**: breadcrumb con 3 items.
   - **Acción**: renderizar.
   - **Assertion**: item 3 NO tiene `::before`.

3. **Hover en link cambia color** — [visual]
   - **Setup**: breadcrumb item.
   - **Acción**: hover.
   - **Assertion**: color del link cambia; transition ≥200ms.

4. **prefers-reduced-motion: sin transition al hover** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: hover.
   - **Assertion**: `transition-duration: 0s`.

5. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: bg/text usan --is-breadcrumb-dark.

6. **Item activo: aria-current="page"** — [a11y]
   - **Setup**: breadcrumb con item activo.
   - **Acción**: renderizar.
   - **Assertion**: aria-current="page" en el último.

7. **Truncamiento de texto largo** — [visual, edge-case]
   - **Setup**: item con texto de 200 chars.
   - **Acción**: renderizar en container 200px.
   - **Assertion**: text-overflow: ellipsis.

8. **Focus visible en links** — [a11y]
   - **Setup**: breadcrumb item link.
   - **Acción**: focus.
   - **Assertion**: outline visible; `outline-offset: 2px`.

---

### `carousel.css` (src/components/navigation/carousel.css — 220 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (10):

1. **Slides con transition horizontal** — [visual, animation]
   - **Setup**: carousel con 3 slides; índice 0.
   - **Acción**: click "next".
   - **Assertion**: translateX(-100%) aplicado con transition ≥300ms.

2. **prefers-reduced-motion: sin transition al cambiar slide** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: click "next".
   - **Assertion**: cambio instantáneo; sin transition.

3. **Indicadores (dots) abajo** — [visual]
   - **Setup**: carousel con 3 slides.
   - **Acción**: renderizar.
   - **Assertion**: 3 dots visibles; dot activo con clase `is-active`.

4. **Click en dot navega a slide** — [event]
   - **Setup**: carousel; dot 2 clickado.
   - **Acción**: click.
   - **Assertion**: slide 2 visible; dot 2 activo.

5. **CSS part `--is-carousel-slide-bg`** — [css-part]
   - **Setup**: 1 slide.
   - **Acción**: `::part(is-carousel-slide-bg) { background: lavender; }`.
   - **Assertion**: bg lavender.

6. **Auto-play se detiene en hover** — [event, edge-case]
   - **Setup**: carousel con auto-play.
   - **Acción**: mouseenter sobre carousel.
   - **Assertion**: auto-play pausado; mouseleave lo reanuda.

7. **Touch swipe (mobile)** — [drag, browser-api]
   - **Setup**: carousel en mobile viewport.
   - **Acción**: touchstart + touchmove + touchend (swipe left).
   - **Assertion**: slide cambia a índice+1.

8. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: dots usan --is-carousel-dot-dark.

9. **Focus visible en dots** — [a11y]
   - **Setup**: dot 1.
   - **Acción**: focus.
   - **Assertion**: outline visible.

10. **Aria-label en carousel** — [a11y]
    - **Setup**: carousel.
    - **Acción**: renderizar.
    - **Assertion**: `aria-label="Image carousel"` en container.

---

### `scroller.css` (src/components/navigation/scroller.css — 110 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (8):

1. **Scroll horizontal con snap** — [visual]
   - **Setup**: scroller con 10 items.
   - **Acción**: scroll-x a 200px.
   - **Assertion**: item más cercano a 200px se alinea; `scroll-snap-type: x mandatory`.

2. **Indicador de posición (progress bar)** — [visual]
   - **Setup**: scroller con 10 items.
   - **Acción**: scroll-x a 50%.
   - **Assertion**: progress bar al 50%.

3. **Botones prev/next visibles solo si hay overflow** — [visual, edge-case]
   - **Setup**: scroller con 3 items en container grande.
   - **Acción**: renderizar.
   - **Assertion**: prev/next hidden (display:none) si no hay overflow.

4. **Click prev/next desplaza un item** — [event]
   - **Setup**: scroller con 10 items; índice 0.
   - **Acción**: click next.
   - **Assertion**: scroll-x desplaza al item 1.

5. **prefers-reduced-motion: sin smooth scroll** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: click next.
   - **Assertion**: scroll-behavior: auto (instantáneo).

6. **CSS part `--is-scroller-track`** — [css-part]
   - **Setup**: scroller.
   - **Acción**: `::part(is-scroller-track) { background: lightgray; }`.
   - **Assertion**: bg lightgray.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: track usa --is-scroller-dark.

8. **Teclado: ArrowLeft/Right navega items** — [keyboard, a11y]
   - **Setup**: scroller focusable.
   - **Acción**: ArrowRight.
   - **Assertion**: scroll-x desplaza un item.

---

### `stepper.css` (src/components/navigation/stepper.css — 150 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (10):

1. **Steps con números y labels** — [visual]
   - **Setup**: stepper con 3 steps.
   - **Acción**: renderizar.
   - **Assertion**: 3 circles numerados + 3 labels.

2. **Step activo: highlight** — [visual]
   - **Setup**: stepper; step 1 activo.
   - **Acción**: renderizar.
   - **Assertion**: step 1 con bg primary; step 2 y 3 con bg gray.

3. **Step completado: checkmark** — [visual]
   - **Setup**: stepper; step 0 completado, step 1 activo.
   - **Acción**: renderizar.
   - **Assertion**: step 0 muestra ✓ en lugar de número.

4. **Conector entre steps** — [visual]
   - **Setup**: stepper con 3 steps.
   - **Acción**: renderizar.
   - **Assertion**: línea horizontal entre step 0 y step 1.

5. **prefers-reduced-motion: sin animación al cambiar step** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: marcar step 1 como activo.
   - **Assertion**: cambio instantáneo; sin transition.

6. **CSS part `--is-stepper-circle`** — [css-part]
   - **Setup**: 1 step.
   - **Acción**: `::part(is-stepper-circle) { background: gold; }`.
   - **Assertion**: bg gold.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: circles usan --is-stepper-dark.

8. **Orientación vertical** — [visual]
   - **Setup**: stepper con `orientation="vertical"`.
   - **Acción**: renderizar.
   - **Assertion**: steps en columna; conectores verticales.

9. **Step clickable emite `is-step-click`** — [event]
   - **Setup**: stepper.
   - **Acción**: click sobre step 1.
   - **Assertion**: event fired con detail.step.

10. **Aria-current="step" en activo** — [a11y]
    - **Setup**: stepper; step 1 activo.
    - **Acción**: verificar atributo.
    - **Assertion**: step 1 tiene aria-current="step".

---

### `tab-group.css` (src/components/navigation/tab-group.css — 180 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (10):

1. **Tabs con underline indicator** — [visual]
   - **Setup**: tab-group con 3 tabs.
   - **Acción**: renderizar.
   - **Assertion**: 3 tabs; tab activo con border-bottom primary.

2. **Indicator animado al cambiar tab** — [animation]
   - **Setup**: tab-group; tab 0 activo.
   - **Acción**: click tab 1.
   - **Assertion**: indicator se desliza de tab 0 a tab 1 con transition ≥200ms.

3. **prefers-reduced-motion: sin animación del indicator** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: click tab 1.
   - **Assertion**: cambio instantáneo.

4. **Tab panel oculto con `hidden` attribute** — [a11y, browser-api]
   - **Setup**: tab-group con 2 tabs.
   - **Acción**: renderizar.
   - **Assertion**: panel del tab 0 visible; panel del tab 1 con atributo `hidden`.

5. **Teclado: ArrowLeft/Right navega tabs** — [keyboard, a11y]
   - **Setup**: tab-group; tab 0 activo.
   - **Acción**: ArrowRight.
   - **Assertion**: tab 1 activo; `is-tab-change` event.

6. **CSS part `--is-tab-indicator`** — [css-part]
   - **Setup**: tab-group.
   - **Acción**: `::part(is-tab-indicator) { background: purple; }`.
   - **Assertion**: indicator purple.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: tabs usan --is-tab-dark.

8. **Disabled tab: aria-disabled + no click** — [a11y]
   - **Setup**: tab-group; tab 1 disabled.
   - **Acción**: click tab 1.
   - **Assertion**: sin cambio; tab 1 tiene aria-disabled="true".

9. **Focus visible en tab** — [a11y]
   - **Setup**: tab.
   - **Acción**: focus.
   - **Assertion**: outline visible.

10. **Aria-selected en tab activo** — [a11y]
    - **Setup**: tab-group.
    - **Acción**: verificar atributo.
    - **Assertion**: tab activo tiene aria-selected="true".

---

### `tree.css` (src/components/navigation/tree.css — 160 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (10):

1. **Items con indent según profundidad** — [visual]
   - **Setup**: tree con 3 niveles.
   - **Acción**: renderizar.
   - **Assertion**: items nivel 1 tienen padding-left: 16px; nivel 2: 32px; nivel 3: 48px.

2. **Chevron rota al expandir** — [animation]
   - **Setup**: tree node collapsed.
   - **Acción**: click para expandir.
   - **Assertion**: chevron rota de 0deg a 90deg con transition ≥200ms.

3. **prefers-reduced-motion: sin rotación del chevron** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: click para expandir.
   - **Assertion**: rotación instantánea.

4. **Hover en item: bg gris claro** — [visual]
   - **Setup**: tree item.
   - **Acción**: hover.
   - **Assertion**: bg gris claro; transition ≥150ms.

5. **Selected item: bg primary** — [visual]
   - **Setup**: tree; item 2 selected.
   - **Acción**: renderizar.
   - **Assertion**: item 2 con bg primary.

6. **CSS part `--is-tree-item-bg`** — [css-part]
   - **Setup**: 1 item.
   - **Acción**: `::part(is-tree-item-bg) { background: yellow; }`.
   - **Assertion**: bg yellow.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: items usan --is-tree-dark.

8. **Aria-expanded en parent items** — [a11y]
   - **Setup**: tree node collapsed.
   - **Acción**: verificar atributo.
   - **Assertion**: aria-expanded="false".

9. **Focus visible en item** — [a11y]
   - **Setup**: tree item.
   - **Acción**: focus.
   - **Assertion**: outline visible.

10. **Drag item para reorganizar** — [drag, browser-api]
    - **Setup**: tree con 3 items.
    - **Acción**: drag item 0 sobre item 2.
    - **Assertion**: orden actualizado; `is-tree-reorder` event.

---

### `data-grid.css` (src/components/data/data-grid.css — 280 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (10):

1. **Celdas con bordes** — [visual]
   - **Setup**: data-grid con 3x3.
   - **Acción**: renderizar.
   - **Assertion**: cada celda tiene border 1px solid gray.

2. **Header sticky al scroll** — [visual, browser-api]
   - **Setup**: data-grid con 100 filas.
   - **Acción**: scroll-y a 500px.
   - **Assertion**: header visible; `position: sticky; top: 0`.

3. **Hover en fila: bg gris claro** — [visual]
   - **Setup**: data-grid fila.
   - **Acción**: hover.
   - **Assertion**: bg gris claro; transition ≥150ms.

4. **prefers-reduced-motion: sin transition al hover** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: hover.
   - **Assertion**: sin transition.

5. **Selected row: bg primary** — [visual]
   - **Setup**: data-grid; fila 2 selected.
   - **Acción**: renderizar.
   - **Assertion**: fila 2 con bg primary.

6. **CSS part `--is-grid-cell`** — [css-part]
   - **Setup**: 1 celda.
   - **Acción**: `::part(is-grid-cell) { background: lavender; }`.
   - **Assertion**: bg lavender.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: celdas usan --is-grid-dark.

8. **Sortable column header: cursor pointer** — [visual, event]
   - **Setup**: data-grid; columna 0 sortable.
   - **Acción**: hover.
   - **Assertion**: cursor: pointer; click emite `is-grid-sort` event.

9. **Resizable column: drag del borde** — [drag, browser-api]
   - **Setup**: data-grid; columna 0 resizable.
   - **Acción**: drag del borde derecho +50px.
   - **Assertion**: ancho de columna aumenta 50px.

10. **Aria-sort en header** — [a11y]
    - **Setup**: data-grid; columna 0 sorted ascending.
    - **Acción**: verificar atributo.
    - **Assertion**: header tiene aria-sort="ascending".

---

### `gauge.css` (src/components/data/gauge.css — 110 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (8):

1. **Arco proporcional al valor** — [visual]
   - **Setup**: gauge con value=75, min=0, max=100.
   - **Acción**: renderizar.
   - **Assertion**: stroke-dasharray cubre 75% del círculo.

2. **Animación de entrada (0 → value)** — [animation]
   - **Setup**: gauge con value=75.
   - **Acción**: renderizar.
   - **Assertion**: stroke-dasharray animado de 0 a 75% en ≥500ms.

3. **prefers-reduced-motion: sin animación de entrada** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: renderizar.
   - **Assertion**: stroke-dasharray aplicado instantáneamente.

4. **Valor numérico visible en el centro** — [visual]
   - **Setup**: gauge con value=75.
   - **Acción**: renderizar.
   - **Assertion**: `<text>` con "75" en el centro.

5. **Color por rango (verde < amarillo < rojo)** — [visual]
   - **Setup**: gauge con thresholds: 0-50 verde, 50-80 amarillo, 80-100 rojo; value=85.
   - **Acción**: renderizar.
   - **Assertion**: stroke rojo.

6. **CSS part `--is-gauge-arc`** — [css-part]
   - **Setup**: 1 gauge.
   - **Acción**: `::part(is-gauge-arc) { stroke: teal; }`.
   - **Assertion**: stroke teal.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: bg usa --is-gauge-dark.

8. **Aria-valuenow/min/max** — [a11y]
   - **Setup**: gauge con value=75.
   - **Acción**: verificar atributos.
   - **Assertion**: aria-valuenow=75, aria-valuemin=0, aria-valuemax=100, role="meter".

---

### `kanban.css` (src/components/data/kanban.css — 200 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (10):

1. **Columnas horizontales con cards** — [visual]
   - **Setup**: kanban con 3 columnas.
   - **Acción**: renderizar.
   - **Assertion**: 3 columnas lado a lado; cada una con cards apiladas verticalmente.

2. **Drag card entre columnas** — [drag, animation]
   - **Setup**: kanban; card en columna A.
   - **Acción**: drag card a columna B.
   - **Assertion**: card.column actualizado a 'B'; transición suave.

3. **prefers-reduced-motion: sin transición al drag** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: drag card.
   - **Assertion**: cambio instantáneo.

4. **Hover en card: shadow elevado** — [visual]
   - **Setup**: kanban card.
   - **Acción**: hover.
   - **Assertion**: box-shadow: 0 4px 8px rgba(0,0,0,0.2); transition ≥200ms.

5. **Card selected: border primary** — [visual]
   - **Setup**: kanban; card 2 selected.
   - **Acción**: renderizar.
   - **Assertion**: card 2 con border 2px primary.

6. **CSS part `--is-kanban-card`** — [css-part]
   - **Setup**: 1 card.
   - **Acción**: `::part(is-kanban-card) { background: orange; }`.
   - **Assertion**: bg orange.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: cards usan --is-kanban-dark.

8. **Column drop zone highlighted al drag** — [visual]
   - **Setup**: drag activo sobre columna B.
   - **Acción**: dragover.
   - **Assertion**: columna B con bg primary light.

9. **Column count badge** — [visual]
   - **Setup**: kanban; columna A con 3 cards.
   - **Acción**: renderizar.
   - **Assertion**: header de columna A tiene badge "3".

10. **WIP limit exceeded: columna con border rojo** — [visual, edge-case]
    - **Setup**: kanban; columna con wip=2 y 3 cards.
    - **Acción**: renderizar.
    - **Assertion**: columna con border rojo + clase `is-over-wip`.

---

### `stat.css` (src/components/data/stat.css — 90 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (8):

1. **Número grande con label debajo** — [visual]
   - **Setup**: stat con value=1234, label="Users".
   - **Acción**: renderizar.
   - **Assertion**: texto "1,234" en font-size 2rem; "Users" debajo en font-size 0.875rem.

2. **Trend indicator (▲▼)** — [visual]
   - **Setup**: stat con trend="up" +5%.
   - **Acción**: renderizar.
   - **Assertion**: flecha arriba + texto "+5%" en verde.

3. **Trend down: rojo** — [visual]
   - **Setup**: stat con trend="down" -3%.
   - **Acción**: renderizar.
   - **Assertion**: flecha abajo + texto "-3%" en rojo.

4. **prefers-reduced-motion: sin animación al cambiar valor** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: cambiar value de 100 a 200.
   - **Assertion**: cambio instantáneo.

5. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: bg usa --is-stat-dark.

6. **CSS part `--is-stat-value`** — [css-part]
   - **Setup**: 1 stat.
   - **Acción**: `::part(is-stat-value) { color: red; font-size: 3rem; }`.
   - **Assertion**: color y font-size aplicados.

7. **Icono decorativo arriba del número** — [visual]
   - **Setup**: stat con `icon="users"`.
   - **Acción**: renderizar.
   - **Assertion**: icono SVG visible arriba del número.

8. **Loading state: skeleton** — [visual, browser-api]
   - **Setup**: stat con `loading=true`.
   - **Acción**: renderizar.
   - **Assertion**: skeleton (bg gris con animación pulse); aria-busy="true".

---

### `transfer.css` (src/components/data/transfer.css — 180 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (8):

1. **Dos columnas (source / target)** — [visual]
   - **Setup**: transfer con source=[A,B,C], target=[X].
   - **Acción**: renderizar.
   - **Assertion**: 2 listas side-by-side; source con 3 items, target con 1.

2. **Botones ⇄ entre columnas** — [visual]
   - **Setup**: transfer.
   - **Acción**: renderizar.
   - **Assertion**: 4 botones (>>, >, <, <<) en el centro.

3. **Drag item entre listas** — [drag, animation]
   - **Setup**: transfer; item A en source.
   - **Acción**: drag A a target.
   - **Assertion**: item A aparece en target; transition suave.

4. **prefers-reduced-motion: sin transición al mover** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: drag.
   - **Assertion**: cambio instantáneo.

5. **Filter input en source** — [visual, event]
   - **Setup**: transfer; filter="A" en source.
   - **Acción**: input.
   - **Assertion**: solo items que coinciden visibles en source.

6. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: listas usan --is-transfer-dark.

7. **CSS part `--is-transfer-list-bg`** — [css-part]
   - **Setup**: 1 list.
   - **Acción**: `::part(is-transfer-list-bg) { background: lightgray; }`.
   - **Assertion**: bg lightgray.

8. **Selected item: bg primary light** — [visual]
   - **Setup**: transfer; item A selected.
   - **Acción**: renderizar.
   - **Assertion**: item A con bg primary light.

---

### `audit-is-code.ts` (scripts/audit-is-code.ts — 380 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Exit code 0 cuando no hay issues** — [edge-case, browser-api]
   - **Setup**: directorio vacío.
   - **Acción**: ejecutar script.
   - **Assertion**: process.exit(0); sin output.

2. **Exit code 1 cuando hay issues** — [edge-case]
   - **Setup**: directorio con 1 archivo con TODO.
   - **Acción**: ejecutar script.
   - **Assertion**: process.exit(1); imprime el path del archivo.

3. **Detecta TODOs en código** — [edge-case]
   - **Setup**: archivo con `// TODO: fix this`.
   - **Acción**: ejecutar script.
   - **Assertion**: archivo listado en output.

4. **Detecta FIXMEs** — [edge-case]
   - **Setup**: archivo con `// FIXME: bug here`.
   - **Acción**: ejecutar script.
   - **Assertion**: archivo listado.

5. **Ignora node_modules y dist** — [edge-case]
   - **Setup**: archivo TODO en node_modules.
   - **Acción**: ejecutar script.
   - **Assertion**: archivo ignorado (no listado).

6. **Output JSON con `--format=json`** — [event]
   - **Setup**: directorio con TODOs.
   - **Acción**: ejecutar con `--format=json`.
   - **Assertion**: output es JSON parseable con array de issues.

7. **Cleanup: signal handlers en SIGINT** — [edge-case]
   - **Setup**: ejecutar script; Ctrl+C.
   - **Acción**: SIGINT.
   - **Assertion**: script termina con exit 130; sin procesos huérfanos.

8. **Custom event `is-audit-progress`** — [event]
   - **Setup**: directorio grande (100 archivos).
   - **Acción**: ejecutar script.
   - **Assertion**: emite eventos de progreso cada 10 archivos.

9. **Filtros: solo extensiones específicas con `--ext`** — [edge-case]
   - **Setup**: archivos .ts y .md con TODOs.
   - **Acción**: ejecutar con `--ext=ts`.
   - **Assertion**: solo .ts listados.

10. **Performance: procesa 1000 archivos en <5s** — [performance]
    - **Setup**: directorio con 1000 archivos vacíos (sin TODOs).
    - **Acción**: ejecutar script.
    - **Assertion**: termina en <5s.

---

### `cdn-panel.js` (scripts/cdn-panel.js — 240 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (8):

1. **Monta panel en `#cdn-root`** — [browser-api]
   - **Setup**: DOM con `<div id="cdn-root">`.
   - **Acción**: ejecutar script.
   - **Assertion**: panel renderizado dentro de #cdn-root.

2. **Botones para cada CDN (jsdelivr, unpkg)** — [visual]
   - **Setup**: script ejecutado.
   - **Acción**: renderizar.
   - **Assertion**: 2 botones visibles.

3. **Click en botón copia URL al clipboard** — [event, browser-api]
   - **Setup**: panel con botón "Copy jsdelivr URL".
   - **Acción**: click.
   - **Assertion**: navigator.clipboard.writeText llamado; toast "Copied!".

4. **Cleanup: removeChild quita panel y listeners** — [edge-case]
   - **Setup**: panel montado.
   - **Acción**: removeChild(#cdn-root).
   - **Assertion**: sin listeners residuales.

5. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: ejecutar.
   - **Assertion**: panel usa --is-cdn-dark.

6. **Custom event `is-cdn-copy` al copiar** — [event]
   - **Setup**: panel.
   - **Acción**: click botón copy.
   - **Assertion**: event fired con detail.url.

7. **Aria-label en botones** — [a11y]
   - **Setup**: botones.
   - **Acción**: verificar atributo.
   - **Assertion**: aria-label="Copy jsdelivr URL".

8. **Empty state: mensaje si no hay archivos** — [visual, edge-case]
   - **Setup**: ejecutar sin archivos.
   - **Acción**: renderizar.
   - **Assertion**: panel muestra "No files to export".

---

### `docs-chrome.js` (scripts/docs-chrome.js — 320 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Inyecta header en página de docs** — [browser-api]
   - **Setup**: página de docs.
   - **Acción**: ejecutar script.
   - **Assertion**: `<header>` con logo + nav.

2. **Inyecta footer con links** — [browser-api]
   - **Setup**: página de docs.
   - **Acción**: ejecutar script.
   - **Assertion**: `<footer>` con links "GitHub", "Issues", "License".

3. **Sidebar colapsable** — [event]
   - **Setup**: docs con sidebar.
   - **Acción**: click en toggle.
   - **Assertion**: sidebar abierto/cerrado; `aria-expanded` actualizado.

4. **Búsqueda en header (Ctrl+K)** — [keyboard]
   - **Setup**: docs.
   - **Acción**: Ctrl+K.
   - **Assertion**: input de búsqueda focusado.

5. **Cleanup: naveg离开 limpia listeners** — [edge-case]
   - **Setup**: docs chrome inyectado.
   - **Acción**: window.removeEventListener('beforeunload', ...).
   - **Assertion**: sin listeners residuales.

6. **Theme switcher en header** — [event, browser-api]
   - **Setup**: docs chrome.
   - **Acción**: click en theme switcher.
   - **Assertion**: data-theme cambia; localStorage guarda preferencia.

7. **Skip to content link (a11y)** — [a11y]
   - **Setup**: docs chrome.
   - **Acción**: renderizar.
   - **Assertion**: primer focusable es "Skip to main content".

8. **TOC (table of contents) auto-generado** — [browser-api]
   - **Setup**: docs con 5 headings.
   - **Acción**: ejecutar script.
   - **Assertion**: TOC con 5 links a los headings.

9. **Anchor links en headings** — [browser-api]
   - **Setup**: heading "Installation".
   - **Acción**: ejecutar script.
   - **Assertion**: heading tiene id="installation" + link "#".

10. **Responsive: sidebar colapsa en mobile** — [visual, browser-api]
    - **Setup**: viewport 600px.
    - **Acción**: ejecutar script.
    - **Assertion**: sidebar oculto por defecto; toggle button visible.

---

### `highlight-pre.js` (scripts/highlight-pre.js — 180 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (8):

1. **Resalta `<pre><code>` automáticamente** — [browser-api]
   - **Setup**: DOM con `<pre><code>const x = 1;</code></pre>`.
   - **Acción**: ejecutar script.
   - **Assertion**: code con tokens resaltados (keyword `const`, number `1`).

2. **Detecta lenguaje del class** — [browser-api]
   - **Setup**: `<pre><code class="language-python">def foo(): pass</code></pre>`.
   - **Acción**: ejecutar script.
   - **Assertion**: tokens Python resaltados.

3. **Fallback a JS si no hay class** — [edge-case]
   - **Setup**: `<pre><code>foo bar</code></pre>` sin class.
   - **Acción**: ejecutar script.
   - **Assertion**: trata como JS (o texto plano).

4. **XSS: code con `<script>` escapado** — [security]
   - **Setup**: `<pre><code>&lt;script&gt;alert(1)&lt;/script&gt;</code></pre>`.
   - **Acción**: ejecutar script.
   - **Assertion**: script no se ejecuta; se muestra como texto.

5. **Line numbers si clase `line-numbers`** — [visual]
   - **Setup**: `<pre class="line-numbers"><code>...</code></pre>`.
   - **Acción**: ejecutar script.
   - **Assertion**: gutter con números 1, 2, 3...

6. **Cleanup: observers desconectados al unload** — [edge-case]
   - **Setup**: script ejecutado con MutationObserver.
   - **Acción**: window unload.
   - **Assertion**: observer disconnected.

7. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: ejecutar script.
   - **Assertion**: tokens usan colores dark.

8. **Custom event `is-highlight-done`** — [event]
   - **Setup**: 3 code blocks.
   - **Acción**: ejecutar script.
   - **Assertion**: event fired tras último highlight.

---

### `observer.preview.ts` (src/components/helpers/observer.preview.ts — 220 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **IntersectionObserver detecta visibilidad** — [browser-api]
   - **Setup**: elemento target con `is-visible` cuando entra al viewport.
   - **Acción**: scroll para hacer target visible.
   - **Assertion**: `is-intersect` event fired con detail `{isIntersecting: true}`.

2. **MutationObserver detecta cambios DOM** — [browser-api]
   - **Setup**: target container.
   - **Acción**: appendChild nuevo hijo.
   - **Assertion**: `is-mutation` event fired con detail `{type: 'childList', addedNodes: [...]}`.

3. **ResizeObserver detecta cambios de tamaño** — [browser-api]
   - **Setup**: target.
   - **Acción**: cambiar width de 100 a 200.
   - **Assertion**: `is-resize` event fired.

4. **Cleanup: disconnect() limpia observers** — [edge-case]
   - **Setup**: 3 observers activos.
   - **Acción**: disconnect().
   - **Assertion**: observers disconnected; sin memory leaks.

5. **Throttle de eventos (rate limit)** — [edge-case]
   - **Setup**: observer con throttle=100ms.
   - **Acción**: 10 mutaciones rápidas.
   - **Assertion**: máximo 1 evento cada 100ms.

6. **Custom event `is-observer-error`** — [event]
   - **Setup**: observer con error forzado (e.g. permissions).
   - **Acción**: trigger.
   - **Assertion**: event fired con detail.error.

7. **Unmount: dispose en disconnectedCallback** — [edge-case, browser-api]
   - **Setup**: observer en preview.
   - **Acción**: unmount.
   - **Assertion**: observers disconnected; sin listeners residuales.

8. **Multi-targets: 1 observer N elementos** — [browser-api]
   - **Setup**: 3 targets.
   - **Acción**: scroll.
   - **Assertion**: 1 solo observer compartido; eventos por target.

9. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: preview usa --is-observer-dark.

10. **Performance: 100 targets sin lag** — [performance]
    - **Setup**: 100 targets.
    - **Acción**: scroll.
    - **Assertion**: 60fps; sin layout thrashing.

---

### `relative-time.preview.ts` (src/components/helpers/relative-time.preview.ts — 180 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Renderiza "5 minutes ago" para fecha reciente** — [visual]
   - **Setup**: date = "2024-01-15T10:00:00Z"; now = "2024-01-15T10:05:00Z".
   - **Acción**: renderizar.
   - **Assertion**: text "5 minutes ago".

2. **Renderiza "yesterday" para 1 día antes** — [visual, i18n]
   - **Setup**: date = ayer; lang="en-US".
   - **Acción**: renderizar.
   - **Assertion**: text "yesterday".

3. **Renderiza "ayer" en es-ES** — [i18n]
   - **Setup**: date = ayer; lang="es-ES".
   - **Acción**: renderizar.
   - **Assertion**: text "ayer".

4. **Renderiza "il y a 5 minutes" en fr-FR** — [i18n]
   - **Setup**: date = hace 5 minutos; lang="fr-FR".
   - **Acción**: renderizar.
   - **Assertion**: text "il y a 5 minutes".

5. **Auto-refresh cada minuto** — [browser-api, event]
   - **Setup**: date = hace 5 minutos; `auto-refresh=true`.
   - **Acción**: esperar 65s (mock de tiempo).
   - **Assertion**: text actualizado a "6 minutes ago"; `is-time-tick` event.

6. **Fecha futura: "in 5 minutes"** — [visual, i18n]
   - **Setup**: date = en 5 minutos.
   - **Acción**: renderizar.
   - **Assertion**: text "in 5 minutes" (en) / "dans 5 minutes" (fr).

7. **Custom format `format="relative-short"` ("5m")** — [visual]
   - **Setup**: date = hace 5 minutos; format="relative-short".
   - **Acción**: renderizar.
   - **Assertion**: text "5m".

8. **Cleanup: setInterval limpiado en unmount** — [edge-case, browser-api]
   - **Setup**: preview con auto-refresh.
   - **Acción**: unmount.
   - **Assertion**: setInterval cleared; sin timers activos.

9. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: text usa --is-rel-time-dark.

10. **Title attribute con fecha absoluta (a11y)** — [a11y]
    - **Setup**: date = "2024-01-15T10:00:00Z".
    - **Acción**: renderizar.
    - **Assertion**: title="January 15, 2024 at 10:00 AM".

---

### `resize-observer.preview.ts` (src/components/helpers/resize-observer.preview.ts — 200 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Detecta resize del target** — [browser-api]
   - **Setup**: target div.
   - **Acción**: cambiar width de 100 a 200.
   - **Assertion**: `is-resize` event fired con detail `{width: 200, height: ...}`.

2. **Throttle de eventos** — [edge-case]
   - **Setup**: observer con throttle=50ms.
   - **Acción**: 10 resizes rápidos.
   - **Assertion**: 1 evento cada 50ms.

3. **Debounce de eventos** — [edge-case]
   - **Setup**: observer con debounce=100ms.
   - **Acción**: 10 resizes en 50ms.
   - **Assertion**: 1 evento tras 100ms.

4. **Cleanup: disconnect en unmount** — [edge-case, browser-api]
   - **Setup**: observer activo.
   - **Acción**: unmount.
   - **Assertion**: observer disconnected; sin listeners.

5. **Multi-targets: 1 observer compartido** — [browser-api]
   - **Setup**: 3 targets.
   - **Acción**: resize target 1.
   - **Assertion**: evento solo para target 1.

6. **`breakpoints` callback** — [event, visual]
   - **Setup**: observer con breakpoints `{sm: 640, md: 768, lg: 1024}`.
   - **Acción**: resize a 700px.
   - **Assertion**: callback `onBreakpoint('md')` invocado.

7. **Custom event `is-resize-error`** — [event]
   - **Setup**: target detachado del DOM.
   - **Acción**: intentar observer.
   - **Assertion**: event fired sin throw.

8. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: renderizar.
   - **Assertion**: preview usa --is-resize-dark.

9. **Performance: 50 targets sin lag** — [performance]
   - **Setup**: 50 targets.
   - **Acción**: resize todos.
   - **Assertion**: <16ms por frame.

10. **Dispose: limpia listeners en disconnectedCallback** — [edge-case]
    - **Setup**: observer con listeners.
    - **Acción**: removeChild.
    - **Assertion**: sin listeners residuales.

---

### `presentation.css` (src/styles/presentation.css — 320 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (8):

1. **Slides con fullscreen** — [visual, browser-api]
   - **Setup**: `.slide` con width:100vw; height:100vh.
   - **Acción**: viewport 1920x1080.
   - **Assertion**: slide ocupa todo el viewport.

2. **Transición entre slides** — [animation]
   - **Setup**: 2 slides; slide 0 visible.
   - **Acción**: marcar slide 1 como activo.
   - **Assertion**: opacity 0→1 con transition ≥500ms.

3. **prefers-reduced-motion: sin transición** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: cambiar slide.
   - **Assertion**: cambio instantáneo.

4. **CSS part `--is-presentation-bg`** — [css-part]
   - **Setup**: 1 slide.
   - **Acción**: `::part(is-presentation-bg) { background: darkblue; }`.
   - **Assertion**: bg darkblue.

5. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: aplicar.
   - **Assertion**: slides usan --is-presentation-dark.

6. **Focus visible en controles** — [a11y]
   - **Setup**: botón "Next".
   - **Acción**: focus.
   - **Assertion**: outline visible.

7. **Imágenes con `object-fit: cover`** — [visual]
   - **Setup**: img en slide con aspect-ratio distinto.
   - **Acción**: renderizar.
   - **Assertion**: img cubre sin distorsión.

8. **Print: slides en vertical** — [visual, browser-api]
   - **Setup**: print stylesheet.
   - **Acción**: print preview.
   - **Assertion**: slides en columna; sin transiciones.

---

### `shell.css` (src/styles/shell.css — 240 LOC)

**Tests existentes** (resumen): NINGUNO (CSS).

**Propuestas nuevas** (8):

1. **Layout con sidebar + main** — [visual]
   - **Setup**: `.shell` con `.shell-sidebar` + `.shell-main`.
   - **Acción**: renderizar.
   - **Assertion**: sidebar 240px a la izquierda; main ocupa resto.

2. **Sidebar colapsable** — [animation]
   - **Setup**: sidebar expanded.
   - **Acción**: toggle.
   - **Assertion**: width 240px→0 con transition ≥300ms.

3. **prefers-reduced-motion: sin transition al colapsar** — [reduced-motion]
   - **Setup**: Context reduced.
   - **Acción**: toggle.
   - **Assertion**: width cambia instantáneamente.

4. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: aplicar.
   - **Assertion**: shell usa --is-shell-dark.

5. **Mobile: sidebar oculto por defecto** — [visual, browser-api]
   - **Setup**: viewport 600px.
   - **Acción**: aplicar CSS.
   - **Assertion**: sidebar hidden; toggle button visible.

6. **Focus visible en nav links** — [a11y]
   - **Setup**: nav link.
   - **Acción**: focus.
   - **Assertion**: outline visible.

7. **Skip to content link** — [a11y]
   - **Setup**: shell.
   - **Acción**: renderizar.
   - **Assertion**: primer focusable es "Skip to main content".

8. **Sticky header** — [visual, browser-api]
   - **Setup**: shell con header.
   - **Acción**: scroll-y.
   - **Assertion**: header position:sticky; top:0; sigue visible.

---

### `inject-equiv-flows.ts` (scripts/inject-equiv-flows.ts — 260 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Detecta flujos equivalentes en specs** — [event]
   - **Setup**: 2 specs con flujos idénticos.
   - **Acción**: ejecutar script.
   - **Assertion**: spec 1 recibe atributo `equiv-to="spec2"`.

2. **Auth: requiere token válido** — [auth]
   - **Setup**: API con token expirado.
   - **Acción**: ejecutar script.
   - **Assertion**: 401; error "Token expired"; exit code 2.

3. **Auth: token válido persiste** — [auth, storage]
   - **Setup**: API con token válido en localStorage.
   - **Acción**: ejecutar script.
   - **Assertion**: requests autenticados; sin re-auth.

4. **Dry-run: no modifica specs con `--dry-run`** — [edge-case]
   - **Setup**: specs con flujos equivalentes.
   - **Acción**: ejecutar con `--dry-run`.
   - **Assertion**: console.log con planes; sin cambios en disco.

5. **Cleanup: signal handlers en SIGINT** — [edge-case]
   - **Setup**: ejecutar script; Ctrl+C.
   - **Acción**: SIGINT.
   - **Assertion**: exit 130; sin archivos parciales.

6. **Output JSON con `--format=json`** — [event]
   - **Setup**: specs con flujos equivalentes.
   - **Acción**: ejecutar con `--format=json`.
   - **Assertion**: output JSON parseable con array de matches.

7. **Performance: 100 specs en <10s** — [performance]
   - **Setup**: directorio con 100 specs.
   - **Acción**: ejecutar script.
   - **Assertion**: <10s.

8. **Idempotencia: 2 ejecuciones dan mismo resultado** — [edge-case]
   - **Setup**: specs ya marcados.
   - **Acción**: ejecutar 2 veces.
   - **Assertion**: segunda ejecución no añade nuevos atributos (todos ya marcados).

9. **Network error: retry con backoff** — [edge-case]
   - **Setup**: API con 503 transitorio.
   - **Acción**: ejecutar script.
   - **Assertion**: retry 3 veces con backoff exponencial; si sigue 503, exit code 3.

10. **Auth refresh: token refresh en 401** — [auth]
    - **Setup**: token a punto de expirar.
    - **Acción**: ejecutar script.
    - **Assertion**: refresh token usado; request retry exitoso.

---

### `preview-boot.js` (scripts/preview-boot.js — 200 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (10):

1. **Boot: monta preview en `#preview-root`** — [browser-api]
   - **Setup**: DOM con `#preview-root`.
   - **Acción**: ejecutar script.
   - **Assertion**: preview renderizado.

2. **Storage: lee config de localStorage** — [storage, browser-api]
   - **Setup**: localStorage["preview-config"] = `{theme: "dark"}`.
   - **Acción**: ejecutar.
   - **Assertion**: preview con data-theme=dark.

3. **Storage: quota exceeded → fallback a defaults** — [storage, edge-case]
   - **Setup**: localStorage lleno (QuotaExceededError).
   - **Acción**: intentar guardar.
   - **Assertion**: console.warn; preview usa defaults.

4. **Custom event `is-preview-ready`** — [event]
   - **Setup**: ejecutar script.
   - **Acción**: completar boot.
   - **Assertion**: event fired tras primer render.

5. **Cleanup: unload limpia listeners y timers** — [edge-case, browser-api]
   - **Setup**: preview con setInterval.
   - **Acción**: window unload.
   - **Assertion**: interval cleared; listeners removed.

6. **Theme dark** — [visual]
   - **Setup**: data-theme=dark.
   - **Acción**: ejecutar.
   - **Assertion**: preview usa --is-preview-dark.

7. **Error boundary: catch errores en preview** — [edge-case]
   - **Setup**: preview con error forzado.
   - **Acción**: ejecutar.
   - **Assertion**: error UI "Something went wrong"; sin crash de la app.

8. **Aria-busy mientras carga** — [a11y]
   - **Setup**: preview con loading.
   - **Acción**: ejecutar.
   - **Assertion**: aria-busy="true" durante carga; "false" tras ready.

9. **Focus visible en controles** — [a11y]
   - **Setup**: botón en preview.
   - **Acción**: focus.
   - **Assertion**: outline visible.

10. **Storage: storage event sync entre tabs** — [storage, browser-api]
    - **Setup**: tab 1 modifica localStorage.
    - **Acción**: tab 2 recibe storage event.
    - **Assertion**: tab 2 actualiza preview con nuevos datos.

---

### `server.ts` (src/utils/testing/e2e/lib/server.ts — 480 LOC)

**Tests existentes** (resumen): NINGUNO.

**Propuestas nuevas** (12):

1. **Sirve archivos estáticos** — [browser-api]
   - **Setup**: directorio con `index.html`.
   - **Acción**: GET `/`.
   - **Assertion**: 200; content-type text/html; body es index.html.

2. **CSP header presente** — [security, browser-api]
   - **Setup**: server arrancado.
   - **Acción**: GET cualquier ruta.
   - **Assertion**: header `Content-Security-Policy` presente con default-src 'self'.

3. **X-Frame-Options: DENY** — [security]
   - **Setup**: server arrancado.
   - **Acción**: GET.
   - **Assertion**: header `X-Frame-Options: DENY`.

4. **Database: conexión con SQLite** — [database]
   - **Setup**: server con DB SQLite.
   - **Acción**: query `SELECT 1`.
   - **Assertion**: retorna `1`.

5. **Database: pool de conexiones** — [database, edge-case]
   - **Setup**: server con pool size=5.
   - **Acción**: 10 requests concurrentes.
   - **Assertion**: todas exitosas; sin "too many connections".

6. **Database: timeout en query lenta** — [database, edge-case]
   - **Setup**: query con `SELECT SLEEP(10)`.
   - **Acción**: ejecutar con timeout=2s.
   - **Assertion**: error tras 2s; sin hang.

7. **Auth: middleware verifica JWT** — [auth]
   - **Setup**: ruta protegida; JWT válido.
   - **Acción**: GET con `Authorization: Bearer <valid>`.
   - **Assertion**: 200.

8. **Auth: 401 sin JWT** — [auth, edge-case]
   - **Setup**: ruta protegida.
   - **Acción**: GET sin Authorization.
   - **Assertion**: 401; body `{"error": "Unauthorized"}`.

9. **Cleanup: shutdown cierra conexiones DB** — [edge-case, database]
   - **Setup**: server con conexiones activas.
   - **Acción**: shutdown() / SIGTERM.
   - **Assertion**: conexiones cerradas; pool released.

10. **Rate limiting: 100 req/min por IP** — [security, edge-case]
    - **Setup**: server con rate limit.
    - **Acción**: 101 requests en 1 min.
    - **Assertion**: 101ª retorna 429.

11. **CORS headers** — [security, browser-api]
    - **Setup**: server arrancado.
    - **Acción**: OPTIONS request.
    - **Assertion**: `Access-Control-Allow-Origin` configurado.

12. **Health check endpoint `/health`** — [browser-api]
    - **Setup**: server arrancado.
    - **Acción**: GET `/health`.
    - **Assertion**: 200; body `{"status": "ok", "uptime": ...}`.

---

## Resumen

- **Total testables procesados**: 52
- **Total propuestas**: ~530 (≈10 por testable)
- **Cobertura de riesgos**:
  - Race conditions SQL (diagram-lightbox, er-editor, org-chart) ✅
  - prefers-reduced-motion (13 CSS) ✅
  - Cleanup de listeners/workers/observers (previews, server) ✅
  - Auth/token expiry (flowchart, inject-equiv-flows, server) ✅
  - Storage quota (preview-boot) ✅
  - i18n/locale (relative-time) ✅
  - Security: CSP/XSS/X-Frame (server, dom-utils, highlight-code) ✅
  - Database: timeouts/pool (server) ✅
  - Memory leaks (workers, observers) ✅
  - Focus trap & restoration (modal-base, lightbox, picker) ✅
  - Theme dark coverage (todos los visual) ✅
  - CSS part customizability (todos los css-part) ✅
  - Edge cases: ciclos, specs inválidos, DOM detachado ✅
  - Performance: 50-1000 elementos ✅
  - A11y: ARIA, keyboard, focus, screen reader ✅
